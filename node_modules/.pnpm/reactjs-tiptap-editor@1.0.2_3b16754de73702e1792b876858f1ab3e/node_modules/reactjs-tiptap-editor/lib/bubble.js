var Sl = Object.defineProperty;
var Cl = (e, t, n) => t in e ? Sl(e, t, { enumerable: !0, configurable: !0, writable: !0, value: n }) : e[t] = n;
var nn = (e, t, n) => Cl(e, typeof t != "symbol" ? t + "" : t, n);
import { jsx as p, Fragment as M, jsxs as x } from "react/jsx-runtime";
import { a2 as kl, f as yr, E as Fe, a3 as El, a4 as _l, a5 as Ke, a6 as Tl, Q as Vi, O as vr, T as Ie, A as Dl, u as Al, S as Il, a7 as Ol, a8 as Ml, a9 as Rl } from "./index-Dx-Dh7KS.js";
import { f as Sr, s as Cr, o as kr, a as Er, b as _r, d as Tr, h as Dr, i as Ar, c as Hi } from "./index-Dbm_Mans.js";
import { Plugin as je, PluginKey as te, NodeSelection as wi, TextSelection as Nl, SelectionRange as Ll, Selection as Ir } from "@tiptap/pm/state";
import { C as Ul, T as Pl } from "./Table-DwseaFSD.js";
import { u as Or } from "./clsx-_bq4MQQ7.js";
import Mr, { useRef as ut, useState as U, useEffect as et, useLayoutEffect as Bl, useCallback as R, useMemo as pt, Fragment as Fi } from "react";
import { createPortal as Ki } from "react-dom";
import { j as nt, h as G, D as We, n as Ge, A as v, o as Ye, p as qe, L as zt, S as $l, q as zl, r as Vl, s as Hl, t as Fl, k as oe, v as Je, B as Vt, H as Rr, w as ji, K as Kl, M as jl, a2 as Wl, G as bi, z as Gl, a3 as Yl, I as at } from "./index-C52cuQ_Y.js";
import { Callout as sn } from "./Callout.js";
import { u as ee, K as xi, s as ql } from "./Katex-BPbXjcZQ.js";
import { u as Z, o as wn, V as vs } from "./index-DtyAGMbq.js";
import { Pencil as Jl, Trash2 as Nr, Check as yi, Copy as Xl } from "lucide-react";
import { P as Wi, a as Gi, b as Yi } from "./popover-BB-ZDnqk.js";
import { S as At } from "./separator-DuuOJRQd.js";
import { CodeBlock as Qn } from "./CodeBlock.js";
import { MultipleColumnNode as Ss } from "./Column.js";
import "./theme.js";
import { C as Zl, D as ti } from "./Drawer-DA0FLHs9.js";
import { Editor as Ql, makeDropdownToolbar as tc } from "easydrawer";
import { s as Lr, i as Ur } from "./shortId-WJVkrvml.js";
import { Excalidraw as ke } from "./Excalidraw.js";
import { I as Ee, g as ec } from "./Iframe-DZ3r3QzK.js";
import { ImageGif as nc } from "./ImageGif.js";
import ic from "katex";
import { L as Cs, a as sc, R as rc } from "./LinkEditBlock-BYWwH2_x.js";
import { DecorationSet as ks, Decoration as oc } from "@tiptap/pm/view";
import { NodeRange as lc } from "@tiptap/pm/model";
import { D as cc, a as ac, b as hc, f as Mt, d as dc, g as Es, h as _s, i as Ts, j as Ds } from "./dropdown-menu-BKSX4gvT.js";
import { Clear as uc } from "./Clear.js";
import { I as fc, a as As, s as Is } from "./Indent-kI4G-clX.js";
import { RichTextAlign as pc, TextAlign as gc } from "./TextAlign.js";
import { Mermaid as vi } from "./Mermaid.js";
import { RichTextBold as mc } from "./Bold.js";
import { RichTextCode as wc } from "./Code.js";
import "./index-CJMI7Fjf.js";
import { RichTextColor as bc } from "./Color.js";
import { RichTextHighlight as xc } from "./Highlight.js";
import { RichTextItalic as yc } from "./Italic.js";
import { r as vc } from "./SlashCommandNodeView-C0l3Fb_3.js";
import { RichTextStrike as Sc } from "./Strike.js";
import { RichTextUnderline as Cc } from "./TextUnderline.js";
import { T as Os, F as kc } from "./Twitter-DP26Lkn8.js";
function Ec(e, t) {
  const n = Math.min(e.top, t.top), i = Math.max(e.bottom, t.bottom), s = Math.min(e.left, t.left), o = Math.max(e.right, t.right) - s, l = i - n, c = s, a = n;
  return new DOMRect(c, a, o, l);
}
var _c = class {
  constructor({
    editor: e,
    element: t,
    view: n,
    pluginKey: i = "bubbleMenu",
    updateDelay: s = 250,
    resizeDelay: r = 60,
    shouldShow: o,
    appendTo: l,
    getReferencedVirtualElement: c,
    options: a
  }) {
    this.preventHide = !1, this.isVisible = !1, this.scrollTarget = window, this.floatingUIOptions = {
      strategy: "absolute",
      placement: "top",
      offset: 8,
      flip: {},
      shift: {},
      arrow: !1,
      size: !1,
      autoPlacement: !1,
      hide: !1,
      inline: !1,
      onShow: void 0,
      onHide: void 0,
      onUpdate: void 0,
      onDestroy: void 0
    }, this.shouldShow = ({
      view: h,
      state: u,
      from: f,
      to: g
    }) => {
      const { doc: m, selection: w } = u, { empty: b } = w, C = !m.textBetween(f, g).length && kl(u.selection), y = this.element.contains(document.activeElement);
      return !(!(h.hasFocus() || y) || b || C || !this.editor.isEditable);
    }, this.mousedownHandler = () => {
      this.preventHide = !0;
    }, this.dragstartHandler = () => {
      this.hide();
    }, this.resizeHandler = () => {
      this.resizeDebounceTimer && clearTimeout(this.resizeDebounceTimer), this.resizeDebounceTimer = window.setTimeout(() => {
        this.updatePosition();
      }, this.resizeDelay);
    }, this.focusHandler = () => {
      setTimeout(() => this.update(this.editor.view));
    }, this.blurHandler = ({ event: h }) => {
      var u;
      if (this.editor.isDestroyed) {
        this.destroy();
        return;
      }
      if (this.preventHide) {
        this.preventHide = !1;
        return;
      }
      h != null && h.relatedTarget && ((u = this.element.parentNode) != null && u.contains(h.relatedTarget)) || (h == null ? void 0 : h.relatedTarget) !== this.editor.view.dom && this.hide();
    }, this.handleDebouncedUpdate = (h, u) => {
      const f = !(u != null && u.selection.eq(h.state.selection)), g = !(u != null && u.doc.eq(h.state.doc));
      !f && !g || (this.updateDebounceTimer && clearTimeout(this.updateDebounceTimer), this.updateDebounceTimer = window.setTimeout(() => {
        this.updateHandler(h, f, g, u);
      }, this.updateDelay));
    }, this.updateHandler = (h, u, f, g) => {
      const { composing: m } = h;
      if (m || !u && !f)
        return;
      if (!this.getShouldShow(g)) {
        this.hide();
        return;
      }
      this.show(), this.updatePosition();
    }, this.transactionHandler = ({ transaction: h }) => {
      const u = h.getMeta(this.pluginKey);
      u === "updatePosition" ? this.updatePosition() : u && typeof u == "object" && u.type === "updateOptions" ? this.updateOptions(u.options) : u === "hide" ? this.hide() : u === "show" && (this.updatePosition(), this.show());
    };
    var d;
    this.editor = e, this.element = t, this.view = n, this.pluginKey = i, this.updateDelay = s, this.resizeDelay = r, this.appendTo = l, this.scrollTarget = (d = a == null ? void 0 : a.scrollTarget) != null ? d : window, this.getReferencedVirtualElement = c, this.floatingUIOptions = {
      ...this.floatingUIOptions,
      ...a
    }, this.element.tabIndex = 0, o && (this.shouldShow = o), this.element.addEventListener("mousedown", this.mousedownHandler, { capture: !0 }), this.view.dom.addEventListener("dragstart", this.dragstartHandler), this.editor.on("focus", this.focusHandler), this.editor.on("blur", this.blurHandler), this.editor.on("transaction", this.transactionHandler), window.addEventListener("resize", this.resizeHandler), this.scrollTarget.addEventListener("scroll", this.resizeHandler), this.update(n, n.state), this.getShouldShow() && (this.show(), this.updatePosition());
  }
  get middlewares() {
    const e = [];
    return this.floatingUIOptions.flip && e.push(
      Sr(
        typeof this.floatingUIOptions.flip != "boolean" ? this.floatingUIOptions.flip : void 0
      )
    ), this.floatingUIOptions.shift && e.push(
      Cr(
        typeof this.floatingUIOptions.shift != "boolean" ? this.floatingUIOptions.shift : void 0
      )
    ), this.floatingUIOptions.offset && e.push(
      kr(
        typeof this.floatingUIOptions.offset != "boolean" ? this.floatingUIOptions.offset : void 0
      )
    ), this.floatingUIOptions.arrow && e.push(Er(this.floatingUIOptions.arrow)), this.floatingUIOptions.size && e.push(
      _r(
        typeof this.floatingUIOptions.size != "boolean" ? this.floatingUIOptions.size : void 0
      )
    ), this.floatingUIOptions.autoPlacement && e.push(
      Tr(
        typeof this.floatingUIOptions.autoPlacement != "boolean" ? this.floatingUIOptions.autoPlacement : void 0
      )
    ), this.floatingUIOptions.hide && e.push(
      Dr(
        typeof this.floatingUIOptions.hide != "boolean" ? this.floatingUIOptions.hide : void 0
      )
    ), this.floatingUIOptions.inline && e.push(
      Ar(
        typeof this.floatingUIOptions.inline != "boolean" ? this.floatingUIOptions.inline : void 0
      )
    ), e;
  }
  get virtualElement() {
    var e, t, n;
    const { selection: i } = this.editor.state, s = (e = this.getReferencedVirtualElement) == null ? void 0 : e.call(this);
    if (s)
      return s;
    if (!((n = (t = this.view) == null ? void 0 : t.dom) != null && n.parentNode))
      return;
    const r = yr(this.view, i.from, i.to);
    let o = {
      getBoundingClientRect: () => r,
      getClientRects: () => [r]
    };
    if (i instanceof wi) {
      let l = this.view.nodeDOM(i.from);
      const c = l.dataset.nodeViewWrapper ? l : l.querySelector("[data-node-view-wrapper]");
      c && (l = c), l && (o = {
        getBoundingClientRect: () => l.getBoundingClientRect(),
        getClientRects: () => [l.getBoundingClientRect()]
      });
    }
    if (i instanceof Ul) {
      const { $anchorCell: l, $headCell: c } = i, a = l ? l.pos : c.pos, d = c ? c.pos : l.pos, h = this.view.nodeDOM(a), u = this.view.nodeDOM(d);
      if (!h || !u)
        return;
      const f = h === u ? h.getBoundingClientRect() : Ec(
        h.getBoundingClientRect(),
        u.getBoundingClientRect()
      );
      o = {
        getBoundingClientRect: () => f,
        getClientRects: () => [f]
      };
    }
    return o;
  }
  updatePosition() {
    if (!this.isVisible)
      return;
    const e = this.virtualElement;
    e && Hi(e, this.element, {
      placement: this.floatingUIOptions.placement,
      strategy: this.floatingUIOptions.strategy,
      middleware: this.middlewares
    }).then(({ x: t, y: n, strategy: i, middlewareData: s }) => {
      var r, o;
      if (!(!this.isVisible || this.editor.isDestroyed || !this.element.isConnected)) {
        if ((r = s.hide) != null && r.referenceHidden || (o = s.hide) != null && o.escaped) {
          this.element.style.visibility = "hidden";
          return;
        }
        this.element.style.visibility = "visible", this.element.style.width = "max-content", this.element.style.position = i, this.element.style.left = `${t}px`, this.element.style.top = `${n}px`, this.isVisible && this.floatingUIOptions.onUpdate && this.floatingUIOptions.onUpdate();
      }
    });
  }
  update(e, t) {
    const { state: n } = e, i = n.selection.from !== n.selection.to;
    if (this.updateDelay > 0 && i) {
      this.handleDebouncedUpdate(e, t);
      return;
    }
    const s = !(t != null && t.selection.eq(e.state.selection)), r = !(t != null && t.doc.eq(e.state.doc));
    this.updateHandler(e, s, r, t);
  }
  getShouldShow(e) {
    var t;
    const { state: n } = this.view, { selection: i } = n, { ranges: s } = i, r = Math.min(...s.map((c) => c.$from.pos)), o = Math.max(...s.map((c) => c.$to.pos));
    return ((t = this.shouldShow) == null ? void 0 : t.call(this, {
      editor: this.editor,
      element: this.element,
      view: this.view,
      state: n,
      oldState: e,
      from: r,
      to: o
    })) || !1;
  }
  show() {
    var e;
    if (this.isVisible)
      return;
    this.element.style.visibility = "visible", this.element.style.opacity = "1";
    const t = typeof this.appendTo == "function" ? this.appendTo() : this.appendTo;
    (e = t ?? this.view.dom.parentElement) == null || e.appendChild(this.element), this.floatingUIOptions.onShow && this.floatingUIOptions.onShow(), this.isVisible = !0;
  }
  hide() {
    this.isVisible && (this.element.style.visibility = "hidden", this.element.style.opacity = "0", this.element.remove(), this.floatingUIOptions.onHide && this.floatingUIOptions.onHide(), this.isVisible = !1);
  }
  updateOptions(e) {
    var t;
    if (e.updateDelay !== void 0 && (this.updateDelay = e.updateDelay), e.resizeDelay !== void 0 && (this.resizeDelay = e.resizeDelay), e.appendTo !== void 0 && (this.appendTo = e.appendTo), e.getReferencedVirtualElement !== void 0 && (this.getReferencedVirtualElement = e.getReferencedVirtualElement), e.shouldShow !== void 0 && e.shouldShow && (this.shouldShow = e.shouldShow), e.options !== void 0) {
      const n = (t = e.options.scrollTarget) != null ? t : window;
      n !== this.scrollTarget && (this.scrollTarget.removeEventListener("scroll", this.resizeHandler), this.scrollTarget = n, this.scrollTarget.addEventListener("scroll", this.resizeHandler)), this.floatingUIOptions = {
        ...this.floatingUIOptions,
        ...e.options
      };
    }
  }
  destroy() {
    this.hide(), this.element.removeEventListener("mousedown", this.mousedownHandler, { capture: !0 }), this.view.dom.removeEventListener("dragstart", this.dragstartHandler), window.removeEventListener("resize", this.resizeHandler), this.scrollTarget.removeEventListener("scroll", this.resizeHandler), this.editor.off("focus", this.focusHandler), this.editor.off("blur", this.blurHandler), this.editor.off("transaction", this.transactionHandler), this.floatingUIOptions.onDestroy && this.floatingUIOptions.onDestroy();
  }
}, Pr = (e) => new je({
  key: typeof e.pluginKey == "string" ? new te(e.pluginKey) : e.pluginKey,
  view: (t) => new _c({ view: t, ...e })
});
Fe.create({
  name: "bubbleMenu",
  addOptions() {
    return {
      element: null,
      pluginKey: "bubbleMenu",
      updateDelay: void 0,
      appendTo: void 0,
      shouldShow: null
    };
  },
  addProseMirrorPlugins() {
    return this.options.element ? [
      Pr({
        pluginKey: this.options.pluginKey,
        editor: this.editor,
        element: this.options.element,
        updateDelay: this.options.updateDelay,
        options: this.options.options,
        appendTo: this.options.appendTo,
        getReferencedVirtualElement: this.options.getReferencedVirtualElement,
        shouldShow: this.options.shouldShow
      })
    ] : [];
  }
});
var Tc = class {
  constructor({
    editor: e,
    element: t,
    view: n,
    pluginKey: i = "floatingMenu",
    updateDelay: s = 250,
    resizeDelay: r = 60,
    options: o,
    appendTo: l,
    shouldShow: c
  }) {
    this.preventHide = !1, this.isVisible = !1, this.scrollTarget = window, this.shouldShow = ({ view: d, state: h }) => {
      const { selection: u } = h, { $anchor: f, empty: g } = u, m = f.depth === 1, w = f.parent.isTextblock && !f.parent.type.spec.code && !f.parent.textContent && f.parent.childCount === 0 && !this.getTextContent(f.parent);
      return !(!d.hasFocus() || !g || !m || !w || !this.editor.isEditable);
    }, this.floatingUIOptions = {
      strategy: "absolute",
      placement: "right",
      offset: 8,
      flip: {},
      shift: {},
      arrow: !1,
      size: !1,
      autoPlacement: !1,
      hide: !1,
      inline: !1
    }, this.updateHandler = (d, h, u, f) => {
      const { composing: g } = d;
      if (g || !h && !u)
        return;
      if (!this.getShouldShow(f)) {
        this.hide();
        return;
      }
      this.updatePosition(), this.show();
    }, this.mousedownHandler = () => {
      this.preventHide = !0;
    }, this.focusHandler = () => {
      setTimeout(() => this.update(this.editor.view));
    }, this.blurHandler = ({ event: d }) => {
      var h;
      if (this.preventHide) {
        this.preventHide = !1;
        return;
      }
      d != null && d.relatedTarget && ((h = this.element.parentNode) != null && h.contains(d.relatedTarget)) || (d == null ? void 0 : d.relatedTarget) !== this.editor.view.dom && this.hide();
    }, this.transactionHandler = ({ transaction: d }) => {
      const h = d.getMeta(this.pluginKey);
      h === "updatePosition" ? this.updatePosition() : h && typeof h == "object" && h.type === "updateOptions" ? this.updateOptions(h.options) : h === "hide" ? this.hide() : h === "show" && (this.updatePosition(), this.show());
    }, this.resizeHandler = () => {
      this.resizeDebounceTimer && clearTimeout(this.resizeDebounceTimer), this.resizeDebounceTimer = window.setTimeout(() => {
        this.updatePosition();
      }, this.resizeDelay);
    };
    var a;
    this.editor = e, this.element = t, this.view = n, this.pluginKey = i, this.updateDelay = s, this.resizeDelay = r, this.appendTo = l, this.scrollTarget = (a = o == null ? void 0 : o.scrollTarget) != null ? a : window, this.floatingUIOptions = {
      ...this.floatingUIOptions,
      ...o
    }, this.element.tabIndex = 0, c && (this.shouldShow = c), this.element.addEventListener("mousedown", this.mousedownHandler, { capture: !0 }), this.editor.on("focus", this.focusHandler), this.editor.on("blur", this.blurHandler), this.editor.on("transaction", this.transactionHandler), window.addEventListener("resize", this.resizeHandler), this.scrollTarget.addEventListener("scroll", this.resizeHandler), this.update(n, n.state), this.getShouldShow() && (this.show(), this.updatePosition());
  }
  getTextContent(e) {
    return El(e, { textSerializers: _l(this.editor.schema) });
  }
  get middlewares() {
    const e = [];
    return this.floatingUIOptions.flip && e.push(
      Sr(
        typeof this.floatingUIOptions.flip != "boolean" ? this.floatingUIOptions.flip : void 0
      )
    ), this.floatingUIOptions.shift && e.push(
      Cr(
        typeof this.floatingUIOptions.shift != "boolean" ? this.floatingUIOptions.shift : void 0
      )
    ), this.floatingUIOptions.offset && e.push(
      kr(
        typeof this.floatingUIOptions.offset != "boolean" ? this.floatingUIOptions.offset : void 0
      )
    ), this.floatingUIOptions.arrow && e.push(Er(this.floatingUIOptions.arrow)), this.floatingUIOptions.size && e.push(
      _r(
        typeof this.floatingUIOptions.size != "boolean" ? this.floatingUIOptions.size : void 0
      )
    ), this.floatingUIOptions.autoPlacement && e.push(
      Tr(
        typeof this.floatingUIOptions.autoPlacement != "boolean" ? this.floatingUIOptions.autoPlacement : void 0
      )
    ), this.floatingUIOptions.hide && e.push(
      Dr(
        typeof this.floatingUIOptions.hide != "boolean" ? this.floatingUIOptions.hide : void 0
      )
    ), this.floatingUIOptions.inline && e.push(
      Ar(
        typeof this.floatingUIOptions.inline != "boolean" ? this.floatingUIOptions.inline : void 0
      )
    ), e;
  }
  getShouldShow(e) {
    var t;
    const { state: n } = this.view, { selection: i } = n, { ranges: s } = i, r = Math.min(...s.map((c) => c.$from.pos)), o = Math.max(...s.map((c) => c.$to.pos));
    return (t = this.shouldShow) == null ? void 0 : t.call(this, {
      editor: this.editor,
      view: this.view,
      state: n,
      oldState: e,
      from: r,
      to: o
    });
  }
  updateOptions(e) {
    var t;
    if (e.updateDelay !== void 0 && (this.updateDelay = e.updateDelay), e.resizeDelay !== void 0 && (this.resizeDelay = e.resizeDelay), e.appendTo !== void 0 && (this.appendTo = e.appendTo), e.shouldShow !== void 0 && e.shouldShow && (this.shouldShow = e.shouldShow), e.options !== void 0) {
      const n = (t = e.options.scrollTarget) != null ? t : window;
      n !== this.scrollTarget && (this.scrollTarget.removeEventListener("scroll", this.resizeHandler), this.scrollTarget = n, this.scrollTarget.addEventListener("scroll", this.resizeHandler)), this.floatingUIOptions = {
        ...this.floatingUIOptions,
        ...e.options
      };
    }
  }
  updatePosition() {
    var e, t;
    if (!((t = (e = this.view) == null ? void 0 : e.dom) != null && t.parentNode))
      return;
    const { selection: n } = this.editor.state, i = yr(this.view, n.from, n.to);
    Hi({
      getBoundingClientRect: () => i,
      getClientRects: () => [i]
    }, this.element, {
      placement: this.floatingUIOptions.placement,
      strategy: this.floatingUIOptions.strategy,
      middleware: this.middlewares
    }).then(({ x: r, y: o, strategy: l, middlewareData: c }) => {
      var a, d;
      if ((a = c.hide) != null && a.referenceHidden || (d = c.hide) != null && d.escaped) {
        this.element.style.visibility = "hidden";
        return;
      }
      this.element.style.visibility = "visible", this.element.style.width = "max-content", this.element.style.position = l, this.element.style.left = `${r}px`, this.element.style.top = `${o}px`, this.isVisible && this.floatingUIOptions.onUpdate && this.floatingUIOptions.onUpdate();
    });
  }
  update(e, t) {
    const n = !(t != null && t.selection.eq(e.state.selection)), i = !(t != null && t.doc.eq(e.state.doc));
    this.updateHandler(e, n, i, t);
  }
  show() {
    var e;
    if (this.isVisible)
      return;
    this.element.style.visibility = "visible", this.element.style.opacity = "1";
    const t = typeof this.appendTo == "function" ? this.appendTo() : this.appendTo;
    (e = t ?? this.view.dom.parentElement) == null || e.appendChild(this.element), this.floatingUIOptions.onShow && this.floatingUIOptions.onShow(), this.isVisible = !0;
  }
  hide() {
    this.isVisible && (this.element.style.visibility = "hidden", this.element.style.opacity = "0", this.element.remove(), this.floatingUIOptions.onHide && this.floatingUIOptions.onHide(), this.isVisible = !1);
  }
  destroy() {
    this.hide(), this.element.removeEventListener("mousedown", this.mousedownHandler, { capture: !0 }), window.removeEventListener("resize", this.resizeHandler), this.scrollTarget.removeEventListener("scroll", this.resizeHandler), this.editor.off("focus", this.focusHandler), this.editor.off("blur", this.blurHandler), this.editor.off("transaction", this.transactionHandler), this.floatingUIOptions.onDestroy && this.floatingUIOptions.onDestroy();
  }
}, Br = (e) => new je({
  key: typeof e.pluginKey == "string" ? new te(e.pluginKey) : e.pluginKey,
  view: (t) => new Tc({ view: t, ...e })
});
Fe.create({
  name: "floatingMenu",
  addOptions() {
    return {
      element: null,
      options: {},
      pluginKey: "floatingMenu",
      updateDelay: void 0,
      resizeDelay: void 0,
      appendTo: void 0,
      shouldShow: null
    };
  },
  addCommands() {
    return {
      updateFloatingMenuPosition: () => ({ tr: e, dispatch: t }) => (t && e.setMeta(this.options.pluginKey, "updatePosition"), !0)
    };
  },
  addProseMirrorPlugins() {
    return this.options.element ? [
      Br({
        pluginKey: this.options.pluginKey,
        editor: this.editor,
        element: this.options.element,
        updateDelay: this.options.updateDelay,
        resizeDelay: this.options.resizeDelay,
        options: this.options.options,
        appendTo: this.options.appendTo,
        shouldShow: this.options.shouldShow
      })
    ] : [];
  }
});
function $r(e, t) {
  return e ?? new te(t);
}
var Dc = typeof window < "u" ? Bl : et, zr = /* @__PURE__ */ new Set([
  "left",
  "opacity",
  "position",
  "top",
  "visibility",
  "width"
]), Ac = /* @__PURE__ */ new Set([
  "animationIterationCount",
  "aspectRatio",
  "borderImageOutset",
  "borderImageSlice",
  "borderImageWidth",
  "columnCount",
  "columns",
  "fillOpacity",
  "flex",
  "flexGrow",
  "flexShrink",
  "fontWeight",
  "gridArea",
  "gridColumn",
  "gridColumnEnd",
  "gridColumnStart",
  "gridRow",
  "gridRowEnd",
  "gridRowStart",
  "lineClamp",
  "lineHeight",
  "opacity",
  "order",
  "orphans",
  "scale",
  "stopOpacity",
  "strokeDasharray",
  "strokeDashoffset",
  "strokeMiterlimit",
  "strokeOpacity",
  "strokeWidth",
  "tabSize",
  "widows",
  "zIndex",
  "zoom"
]), Ic = /* @__PURE__ */ new Set(["children", "className", "style"]), Oc = /* @__PURE__ */ new Set(["tabIndex"]), Mc = /* @__PURE__ */ new Set([
  "accessKey",
  "autoCapitalize",
  "contentEditable",
  "contextMenu",
  "dir",
  "draggable",
  "enterKeyHint",
  "hidden",
  "id",
  "lang",
  "nonce",
  "role",
  "slot",
  "spellCheck",
  "tabIndex",
  "title",
  "translate"
]), Rc = {
  Blur: "focusout",
  DoubleClick: "dblclick",
  Focus: "focusin",
  MouseEnter: "mouseenter",
  MouseLeave: "mouseleave"
};
function Si(e, t) {
  return /^on[A-Z]/.test(e) && typeof t == "function";
}
function Nc(e) {
  return e.startsWith("aria-") || e.startsWith("data-"), e;
}
function Lc(e) {
  return e.startsWith("aria-") || e.startsWith("data-") || Mc.has(e);
}
function Vr(e) {
  return e.startsWith("--") ? e : e.replace(/[A-Z]/g, (t) => `-${t.toLowerCase()}`);
}
function Uc(e) {
  var t;
  const n = e.endsWith("Capture"), s = (n ? e.slice(0, -7) : e).slice(2);
  return {
    eventName: (t = Rc[s]) != null ? t : s.toLowerCase(),
    options: n ? { capture: !0 } : void 0
  };
}
function Pc(e, t) {
  let n = t.defaultPrevented, i = !1;
  const s = Object.create(t);
  return Object.defineProperties(s, {
    nativeEvent: { value: t },
    currentTarget: { value: e },
    target: { value: t.target },
    persist: { value: () => {
    } },
    isDefaultPrevented: { value: () => n },
    isPropagationStopped: { value: () => i },
    preventDefault: {
      value: () => {
        n = !0, t.preventDefault();
      }
    },
    stopPropagation: {
      value: () => {
        i = !0, t.stopPropagation();
      }
    }
  }), s;
}
function ei(e) {
  return Oc.has(e);
}
function Ms(e, t, n) {
  if (t === "tabIndex") {
    e.tabIndex = Number(n);
    return;
  }
  e[t] = n;
}
function Bc(e, t) {
  if (t === "tabIndex") {
    e.removeAttribute("tabindex");
    return;
  }
  const n = e[t];
  if (typeof n == "boolean") {
    e[t] = !1;
    return;
  }
  if (typeof n == "number") {
    e[t] = 0;
    return;
  }
  e[t] = "";
}
function $c(e, t) {
  return typeof t != "number" || t === 0 || e.startsWith("--") || Ac.has(e) ? String(t) : `${t}px`;
}
function zc(e, t) {
  zr.has(t) || e.style.removeProperty(Vr(t));
}
function Vc(e, t, n) {
  zr.has(t) || e.style.setProperty(Vr(t), $c(t, n));
}
function Hc(e, t, n) {
  (/* @__PURE__ */ new Set([...Object.keys(t), ...Object.keys(n)])).forEach((s) => {
    if (Ic.has(s) || !Lc(s) || Si(s, t[s]) || Si(s, n[s]))
      return;
    const r = t[s], o = n[s];
    if (r === o)
      return;
    const l = Nc(s);
    if (o == null || o === !1) {
      ei(s) && Bc(e, s), e.removeAttribute(l);
      return;
    }
    if (o === !0) {
      ei(s) && Ms(e, s, !0), e.setAttribute(l, "");
      return;
    }
    if (ei(s)) {
      Ms(e, s, o);
      return;
    }
    e.setAttribute(l, String(o));
  });
}
function Fc(e, t, n) {
  if (t !== n) {
    if (n) {
      e.className = n;
      return;
    }
    e.removeAttribute("class");
  }
}
function Kc(e, t, n) {
  const i = t ?? {}, s = n ?? {};
  (/* @__PURE__ */ new Set([...Object.keys(i), ...Object.keys(s)])).forEach((o) => {
    const l = i[o], c = s[o];
    if (l !== c) {
      if (c == null) {
        zc(e, o);
        return;
      }
      Vc(e, o, c);
    }
  });
}
function jc(e, t, n) {
  t.forEach(({ eventName: s, listener: r, options: o }) => {
    e.removeEventListener(s, r, o);
  });
  const i = [];
  return Object.entries(n).forEach(([s, r]) => {
    if (!Si(s, r))
      return;
    const { eventName: o, options: l } = Uc(s), c = (a) => {
      r(Pc(e, a));
    };
    e.addEventListener(o, c, l), i.push({ eventName: o, listener: c, options: l });
  }), i;
}
function Hr(e, t) {
  const n = ut({}), i = ut([]);
  Dc(() => {
    const s = n.current;
    return Fc(e, s.className, t.className), Kc(e, s.style, t.style), Hc(e, s, t), i.current = jc(e, i.current, t), n.current = t, () => {
      i.current.forEach(({ eventName: r, listener: o, options: l }) => {
        e.removeEventListener(r, o, l);
      }), i.current = [];
    };
  }, [e, t]);
}
var ct = Mr.forwardRef(
  ({
    pluginKey: e,
    editor: t,
    updateDelay: n,
    resizeDelay: i,
    appendTo: s,
    shouldShow: r = null,
    getReferencedVirtualElement: o,
    options: l,
    children: c,
    ...a
  }, d) => {
    const h = ut(document.createElement("div")), u = ut(
      $r(e, "bubbleMenu")
    ).current;
    Hr(h.current, a), typeof d == "function" ? d(h.current) : d && (d.current = h.current);
    const { editor: f } = Or(), g = t || f, m = {
      updateDelay: n,
      resizeDelay: i,
      appendTo: s,
      pluginKey: u,
      shouldShow: r,
      getReferencedVirtualElement: o,
      options: l
    }, w = ut(m);
    w.current = m;
    const [b, C] = U(!1), y = ut(!0);
    return et(() => {
      if (g != null && g.isDestroyed)
        return;
      if (!g) {
        console.warn(
          "BubbleMenu component is not rendered inside of an editor component or does not have editor prop."
        );
        return;
      }
      const T = h.current;
      T.style.visibility = "hidden", T.style.position = "absolute";
      const z = Pr({
        ...w.current,
        editor: g,
        element: T
      });
      g.registerPlugin(z);
      const I = w.current.pluginKey;
      return y.current = !0, C(!0), () => {
        C(!1), g.unregisterPlugin(I), window.requestAnimationFrame(() => {
          T.parentNode && T.parentNode.removeChild(T);
        });
      };
    }, [g]), et(() => {
      if (!(!b || !g || g.isDestroyed)) {
        if (y.current) {
          y.current = !1;
          return;
        }
        g.view.dispatch(
          g.state.tr.setMeta(u, {
            type: "updateOptions",
            options: w.current
          })
        );
      }
    }, [
      b,
      g,
      n,
      i,
      r,
      l,
      s,
      o,
      u
    ]), Ki(c, h.current);
  }
);
Mr.forwardRef(
  ({
    pluginKey: e,
    editor: t,
    updateDelay: n,
    resizeDelay: i,
    appendTo: s,
    shouldShow: r = null,
    options: o,
    children: l,
    ...c
  }, a) => {
    const d = ut(document.createElement("div")), h = ut(
      $r(e, "floatingMenu")
    ).current;
    Hr(d.current, c), typeof a == "function" ? a(d.current) : a && (a.current = d.current);
    const { editor: u } = Or(), f = t || u, g = {
      updateDelay: n,
      resizeDelay: i,
      appendTo: s,
      pluginKey: h,
      shouldShow: r,
      options: o
    }, m = ut(g);
    m.current = g;
    const [w, b] = U(!1), C = ut(!0);
    return et(() => {
      if (f != null && f.isDestroyed)
        return;
      if (!f) {
        console.warn(
          "FloatingMenu component is not rendered inside of an editor component or does not have editor prop."
        );
        return;
      }
      const y = d.current;
      y.style.visibility = "hidden", y.style.position = "absolute";
      const T = Br({
        ...m.current,
        editor: f,
        element: y
      });
      f.registerPlugin(T);
      const z = m.current.pluginKey;
      return C.current = !0, b(!0), () => {
        b(!1), f.unregisterPlugin(z), window.requestAnimationFrame(() => {
          y.parentNode && y.parentNode.removeChild(y);
        });
      };
    }, [f]), et(() => {
      if (!(!w || !f || f.isDestroyed)) {
        if (C.current) {
          C.current = !1;
          return;
        }
        f.view.dispatch(
          f.state.tr.setMeta(h, {
            type: "updateOptions",
            options: m.current
          })
        );
      }
    }, [
      w,
      f,
      n,
      i,
      r,
      o,
      s,
      h
    ]), Ki(l, d.current);
  }
);
function ne(e, t) {
  const { state: n } = t, i = n.selection.$anchor;
  let s = !1;
  if (i.depth)
    for (let r = i.depth; r > 0; r--)
      i.node(r).type.name === e && (t.dispatchTransaction && t.dispatchTransaction(
        n.tr.delete(i.before(r), i.after(r)).scrollIntoView()
      ), s = !0);
  else {
    const r = n.selection.node;
    r && r.type.name === e && (t.chain().deleteSelection().run(), s = !0);
  }
  if (!s) {
    const r = i.pos;
    if (r) {
      const o = n.tr.doc.nodeAt(r);
      o && o.type.name === e && (t.dispatchTransaction && t.dispatchTransaction(n.tr.delete(r, r + o.nodeSize)), s = !0);
    }
  }
  return s;
}
const Wc = [
  { value: "note", label: "Note", icon: "Info" },
  { value: "tip", label: "Tip", icon: "Lightbulb" },
  { value: "important", label: "Important", icon: "AlertCircle" },
  { value: "warning", label: "Warning", icon: "TriangleAlert" },
  { value: "caution", label: "Caution", icon: "OctagonAlert" }
];
function Lp() {
  const e = nt(), t = G(), { t: n } = Z(), { type: i, title: s, body: r } = ee(t, sn.name, {
    type: "note",
    title: "",
    body: ""
  }), [o, l] = U(!1), [c, a] = U("note"), [d, h] = U(""), [u, f] = U("");
  et(() => {
    o && (a(i || "note"), h(s || ""), f(r || ""));
  }, [o, i, s, r]);
  const g = R(() => {
    t && (t.chain().updateAttributes(sn.name, {
      type: c,
      title: d,
      body: u
    }).focus().run(), l(!1));
  }, [t, c, d, u]), m = R(() => {
    l(!1);
  }, []), w = R(() => {
    ne(sn.name, t);
  }, [t]), b = R(() => t.isActive(sn.name), [t]);
  return e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "top", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleCallout",
      shouldShow: b,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ x(We, { onOpenChange: l, open: o, children: [
          /* @__PURE__ */ p(Ge, { asChild: !0, children: /* @__PURE__ */ p(v, { icon: "Pencil", tooltip: n("editor.callout.edit.title") }) }),
          /* @__PURE__ */ x(Ye, { children: [
            /* @__PURE__ */ p(qe, { children: n("editor.callout.edit.title") }),
            /* @__PURE__ */ x("div", { className: "richtext-space-y-4 richtext-py-4", children: [
              /* @__PURE__ */ x("div", { className: "richtext-space-y-2", children: [
                /* @__PURE__ */ p(zt, { children: n("editor.callout.dialog.type") }),
                /* @__PURE__ */ x($l, { onValueChange: a, value: c, children: [
                  /* @__PURE__ */ p(zl, { children: /* @__PURE__ */ p(
                    Vl,
                    {
                      className: "richtext-text-accent",
                      placeholder: n("editor.callout.dialog.type.placeholder")
                    }
                  ) }),
                  /* @__PURE__ */ p(Hl, { children: Wc.map((C) => /* @__PURE__ */ p(Fl, { value: C.value, children: n(`editor.callout.type.${C.value}`) }, C.value)) })
                ] })
              ] }),
              /* @__PURE__ */ x("div", { className: "richtext-space-y-2", children: [
                /* @__PURE__ */ p(zt, { children: n("editor.callout.dialog.title.label") }),
                /* @__PURE__ */ p(
                  oe,
                  {
                    onChange: (C) => h(C.target.value),
                    placeholder: n("editor.callout.dialog.title.placeholder"),
                    type: "text",
                    value: d
                  }
                )
              ] }),
              /* @__PURE__ */ x("div", { className: "richtext-space-y-2", children: [
                /* @__PURE__ */ p(zt, { children: n("editor.callout.dialog.body.label") }),
                /* @__PURE__ */ p(
                  oe,
                  {
                    onChange: (C) => f(C.target.value),
                    placeholder: n("editor.callout.dialog.body.placeholder"),
                    type: "text",
                    value: u
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ x(Je, { children: [
              /* @__PURE__ */ p(Vt, { onClick: m, variant: "outline", children: n("editor.callout.dialog.button.cancel") }),
              /* @__PURE__ */ p(Vt, { onClick: g, children: n("editor.callout.dialog.button.apply") })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ p(v, { action: w, icon: "Trash2", tooltip: n("editor.delete") })
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Up() {
  const e = nt(), t = G(), { t: n } = Z(), i = R(() => t.isActive(Ss.name), [t]), s = R(() => ne(Ss.name, t), [t]), r = R(() => t.chain().focus().addColBefore().run(), [t]), o = R(() => t.chain().focus().addColAfter().run(), [t]), l = R(() => t.chain().focus().deleteCol().run(), [t]);
  return e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleColumns",
      shouldShow: i,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(
          v,
          {
            action: r,
            icon: "ColumnAddLeft",
            tooltip: n("editor.table.menu.insertColumnBefore")
          }
        ),
        /* @__PURE__ */ p(
          v,
          {
            action: o,
            icon: "ColumnAddRight",
            tooltip: n("editor.table.menu.insertColumnAfter")
          }
        ),
        /* @__PURE__ */ p(
          v,
          {
            action: l,
            icon: "DeleteColumn",
            tooltip: n("editor.table.menu.deleteColumn")
          }
        ),
        /* @__PURE__ */ p(
          v,
          {
            action: s,
            icon: "Trash2",
            tooltip: n("editor.table.menu.delete_column")
          }
        )
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Gc(e, t) {
  const n = ["size-small", "size-medium", "size-large"], i = [
    "SizeS",
    "SizeM",
    "SizeL"
  ];
  return n.map((s, r) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.${s.replace("-", ".")}.tooltip`),
      icon: i[r],
      action: () => e.commands.updateImage({ width: wn[s] }),
      isActive: () => e.isActive("image", { width: wn[s] })
    }
  }));
}
function Yc(e, t) {
  const n = ["size-small", "size-medium", "size-large"], i = [
    "SizeS",
    "SizeM",
    "SizeL"
  ];
  return n.map((s, r) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.${s.replace("-", ".")}.tooltip`),
      icon: i[r],
      action: () => e.commands.updateImageGif({ width: wn[s] }),
      isActive: () => e.isActive("image", { width: wn[s] })
    }
  }));
}
function qc(e, t) {
  const n = ["left", "center", "right"], i = {
    left: "AlignLeft",
    center: "AlignCenter",
    right: "AlignRight"
  };
  return n.map((s) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.textalign.${s}.tooltip`),
      icon: i[s],
      action: () => {
        var r, o;
        return (o = (r = e.commands) == null ? void 0 : r.setAlignImage) == null ? void 0 : o.call(r, s);
      },
      isActive: () => e.isActive({ align: s }) || !1,
      disabled: !1
    }
  }));
}
function Jc(e, t) {
  const n = ["left", "center", "right"], i = {
    left: "AlignLeft",
    center: "AlignCenter",
    right: "AlignRight"
  };
  return n.map((s) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.textalign.${s}.tooltip`),
      icon: i[s],
      action: () => {
        var r, o;
        return (o = (r = e.commands) == null ? void 0 : r.setAlignImageGif) == null ? void 0 : o.call(r, s);
      },
      isActive: () => e.isActive({ align: s }) || !1,
      disabled: !1
    }
  }));
}
function Xc(e, t) {
  const n = ["left", "center", "right"], i = {
    left: "AlignLeft",
    center: "AlignCenter",
    right: "AlignRight"
  };
  return n.map((s) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.textalign.${s}.tooltip`),
      icon: i[s],
      action: () => {
        var r, o;
        return (o = (r = e.commands) == null ? void 0 : r.setAlignImageMermaid) == null ? void 0 : o.call(r, s);
      },
      isActive: () => e.isActive({ align: s }) || !1,
      disabled: !1
    }
  }));
}
function Zc(e, t) {
  const n = ["left", "center", "right"], i = {
    left: "AlignLeft",
    center: "AlignCenter",
    right: "AlignRight"
  };
  return n.map((s) => ({
    type: `image-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.textalign.${s}.tooltip`),
      icon: i[s],
      action: () => {
        var r, o;
        return (o = (r = e.commands) == null ? void 0 : r.setAlignImageDrawer) == null ? void 0 : o.call(r, s);
      },
      isActive: () => e.isActive({ align: s }) || !1,
      disabled: !1
    }
  }));
}
function Qc(e) {
  return [
    { type: "flex-start", icon: "AlignLeft", tooltip: "Align left" },
    { type: "center", icon: "AlignCenter", tooltip: "Align center" },
    { type: "flex-end", icon: "AlignRight", tooltip: "Align right" }
  ].map((n) => ({
    type: `video-align-${n.type}`,
    component: v,
    componentProps: {
      tooltip: n.tooltip,
      icon: n.icon,
      action: () => e.commands.updateVideo({ align: n.type }),
      isActive: () => e.getAttributes("video").align === n.type
    }
  }));
}
function ta(e, t) {
  const n = ["size-small", "size-medium", "size-large"], i = [
    "SizeS",
    "SizeM",
    "SizeL"
  ];
  return n.map((s, r) => ({
    type: `video-${s}`,
    component: v,
    componentProps: {
      tooltip: t(`editor.${s.replace("-", ".")}.tooltip`),
      icon: i[r],
      action: () => e.commands.updateVideo({ width: vs[s] }),
      isActive: () => e.isActive("video", { width: vs[s] })
    }
  }));
}
function ea(e, t) {
  return [
    {
      type: "flipX",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.tooltip.flipX"),
        icon: "FlipX",
        action: () => {
          const n = e.getAttributes("image"), { flipX: i } = n;
          e.chain().focus(void 0, { scrollIntoView: !1 }).updateImage({
            flipX: !i
          }).run();
        }
      }
    },
    {
      type: "flipY",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.tooltip.flipY"),
        icon: "FlipY",
        action: () => {
          const n = e.getAttributes("image"), { flipY: i } = n;
          e.chain().focus(void 0, { scrollIntoView: !1 }).updateImage({
            flipY: !i
          }).run();
        }
      }
    },
    ...Gc(e, t),
    ...qc(e, t),
    {
      type: "remove",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.remove"),
        icon: "Trash2",
        action: () => {
          const { state: n, dispatch: i } = e.view;
          Ke(n, i);
        }
      }
    }
  ];
}
function na(e, t) {
  return [
    ...Yc(e, t),
    ...Jc(e, t),
    {
      type: "remove",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.remove"),
        icon: "Trash2",
        action: () => {
          const { state: n, dispatch: i } = e.view;
          Ke(n, i);
        }
      }
    }
  ];
}
function ia(e, t) {
  return [
    ...Xc(e, t),
    {
      type: "edit",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.edit"),
        icon: "Pencil",
        action: () => !0
      }
    },
    {
      type: "remove",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.remove"),
        icon: "Trash2",
        action: () => {
          const { state: n, dispatch: i } = e.view;
          Ke(n, i);
        }
      }
    }
  ];
}
function sa(e, t) {
  return [
    ...Zc(e, t),
    {
      type: "edit",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.edit"),
        icon: "Pencil",
        action: () => !0
      }
    },
    {
      type: "remove",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.remove"),
        icon: "Trash2",
        action: () => {
          const { state: n, dispatch: i } = e.view;
          Ke(n, i);
        }
      }
    }
  ];
}
function ra(e, t) {
  return [
    ...ta(e, t),
    ...Qc(e),
    {
      type: "remove",
      component: v,
      componentProps: {
        editor: e,
        tooltip: t("editor.remove"),
        icon: "Trash2",
        action: () => {
          const { state: n, dispatch: i } = e.view;
          Ke(n, i);
        }
      }
    }
  ];
}
let _e = !1;
function oa({ editor: e, attrs: t, extension: n }) {
  const [i, s] = U(!1), r = ut(null), o = ut(null), { alt: l, align: c } = t, a = n == null ? void 0 : n.options.upload, d = () => {
    (async () => {
      const E = document.querySelector("#easydrawer");
      E && (r.current = new Ql(E, {
        wheelEventsEnabled: !1,
        disableZoom: !0
      }), o.current = tc(r.current), o.current.addDefaultToolWidgets(), r.current.loadFromSVG(decodeURIComponent(l)));
    })();
  };
  et(() => {
    i && setTimeout(() => {
      d();
    }, 200);
  }, [i]);
  const h = async () => {
    if (r.current) {
      const I = r.current.toSVG(), E = I.outerHTML, O = `drawer-${Lr()}.svg`;
      let Q = Ur(I.outerHTML);
      if (a) {
        const st = Rr(Q, O);
        Q = await a(st);
      }
      e == null || e.chain().focus().setDrawer(
        {
          type: "drawer",
          src: Q,
          alt: encodeURIComponent(E),
          width: 426,
          height: 212
        },
        !!E
      ).run(), e == null || e.commands.setAlignImageDrawer(c);
    }
    s(!1);
  }, u = (I) => {
    const E = r.current.toolController.getPrimaryTools()[2], O = o.current.getWidgetById("pen-1");
    E && O && (E.setColor(I), O.serializeState());
  }, f = (I) => {
    const E = r.current.toolController.getPrimaryTools()[2], O = o.current.getWidgetById("pen-1");
    E && O && (E.setThickness(I), O.serializeState());
  }, g = (I) => {
    const E = r.current.toolController.getPrimaryTools()[3], O = o.current.getWidgetById("pen-2");
    E && O && (E.setColor(I), O.serializeState());
  }, m = (I) => {
    const E = o.current.getWidgetById("shape");
    E && E.setShapeType(I);
  }, w = (I) => {
    const E = r.current.toolController.getPrimaryTools()[5], O = o.current.getWidgetById("shape");
    E && O && (E.setColor(I), O.serializeState());
  }, b = (I) => {
    const E = r.current.toolController.getPrimaryTools()[5], O = o.current.getWidgetById("shape");
    E && O && (E.setThickness(I), O.serializeState());
  }, C = (I) => {
    const E = r.current.toolController.getPrimaryTools()[5], O = o.current.getWidgetById("shape");
    E && O && (E.setBorderColor(I), O.serializeState());
  }, y = () => {
    if (_e) {
      for (; r.current.history.redoStackSize > 0; )
        r.current.history.redo();
      _e = !1;
      return;
    }
    r.current.history.undo();
  };
  return /* @__PURE__ */ x(We, { onOpenChange: s, open: i, children: [
    /* @__PURE__ */ p(Ge, { asChild: !0, children: /* @__PURE__ */ p(v, { action: () => s(!0), icon: "Pencil", tooltip: "Edit Drawer" }) }),
    /* @__PURE__ */ x(Ye, { className: "richtext-z-[99999] !richtext-max-w-[1300px]", children: [
      /* @__PURE__ */ p(qe, { children: "Edit Drawer" }),
      /* @__PURE__ */ x(
        "div",
        {
          style: {
            height: "100%",
            borderWidth: 1,
            background: "white",
            position: "relative"
          },
          children: [
            /* @__PURE__ */ p("div", { className: "richtext-size-full", id: "easydrawer" }),
            /* @__PURE__ */ p(
              Zl,
              {
                changeBorderColorShape: C,
                changeColorShape: w,
                changeShape: m,
                onClear: () => {
                  if (!_e) {
                    for (; r.current.history.undoStackSize > 0; )
                      y();
                    _e = !0;
                  }
                },
                onRedo: () => {
                  _e || r.current.history.redo();
                },
                onThicknessChange: b,
                onUndo: y,
                refEditor: r,
                setColorHighlight: g,
                setColorPen: u,
                setThicknessPen: f
              }
            )
          ]
        }
      ),
      /* @__PURE__ */ p(Je, { children: /* @__PURE__ */ p(Vt, { onClick: h, type: "button", children: "Save changes" }) })
    ] })
  ] });
}
function la({ item: e, disabled: t, editor: n }) {
  var s;
  const i = e.component;
  return i ? /* @__PURE__ */ p(Fi, { children: e.type === "divider" ? /* @__PURE__ */ p(
    At,
    {
      className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
      orientation: "vertical"
    }
  ) : /* @__PURE__ */ p(
    i,
    {
      ...e.componentProps,
      disabled: t || ((s = e == null ? void 0 : e.componentProps) == null ? void 0 : s.disabled),
      editor: n
    }
  ) }) : /* @__PURE__ */ p(M, {});
}
function Pp() {
  const { lang: e, t } = Z(), n = nt(), i = G(), s = ee(i, ti.name), r = ji(ti.name), o = ({ editor: c }) => {
    const { selection: a } = c.view.state, { $from: d, to: h } = a;
    let u = !1;
    return c.view.state.doc.nodesBetween(d.pos, h, (f) => {
      if (f.type.name === ti.name)
        return u = !0, !1;
    }), u;
  }, l = pt(() => sa(i, t), [i, e, t]);
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: i,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleDrawer",
      shouldShow: o,
      children: l != null && l.length ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: l == null ? void 0 : l.map((c, a) => c.type === "edit" && (s != null && s.src) ? /* @__PURE__ */ p(
        oa,
        {
          attrs: s,
          editor: i,
          extension: r
        },
        `bubbleMenu-drawer-${a}`
      ) : /* @__PURE__ */ p(la, { editor: i, item: c }, `bubbleMenu-drawer-${a}`)) }) : /* @__PURE__ */ p(M, {})
    }
  ) : /* @__PURE__ */ p(M, {});
}
const Fr = ({ width: e, maxWidth: t, height: n, onOk: i, children: s }) => {
  const { t: r } = Z(), [o, l] = U({
    width: "",
    height: "",
    maxWidth: ""
  });
  et(() => {
    l({
      width: e,
      height: n,
      maxWidth: t
    });
  }, [n, t, e]);
  function c(a) {
    a.preventDefault(), a.stopPropagation(), i(o);
  }
  return /* @__PURE__ */ x(Wi, { modal: !0, children: [
    /* @__PURE__ */ p(Gi, { asChild: !0, children: s }),
    /* @__PURE__ */ p(Yi, { children: /* @__PURE__ */ x("form", { className: "richtext-flex richtext-flex-col richtext-gap-2", onSubmit: c, children: [
      /* @__PURE__ */ p(zt, { className: "mb-[6px]", children: "Width" }),
      /* @__PURE__ */ p("div", { className: "richtext-flex richtext-w-full richtext-max-w-sm richtext-items-center richtext-gap-1.5", children: /* @__PURE__ */ p("div", { className: "richtext-relative richtext-w-full richtext-max-w-sm richtext-items-center", children: /* @__PURE__ */ p(
        oe,
        {
          onChange: (a) => l({ ...o, width: a.target.value }),
          required: !0,
          type: "number",
          value: o.width
        }
      ) }) }),
      /* @__PURE__ */ p(zt, { className: "mb-[6px]", children: "Max Width" }),
      /* @__PURE__ */ p("div", { className: "richtext-flex richtext-w-full richtext-max-w-sm richtext-items-center richtext-gap-1.5", children: /* @__PURE__ */ p("div", { className: "richtext-relative richtext-w-full richtext-max-w-sm richtext-items-center", children: /* @__PURE__ */ p(
        oe,
        {
          onChange: (a) => l({ ...o, maxWidth: a.target.value }),
          required: !0,
          type: "number",
          value: o.maxWidth
        }
      ) }) }),
      /* @__PURE__ */ p(zt, { className: "mb-[6px]", children: "Height" }),
      /* @__PURE__ */ p("div", { className: "richtext-flex richtext-w-full richtext-max-w-sm richtext-items-center richtext-gap-1.5", children: /* @__PURE__ */ p("div", { className: "richtext-relative richtext-w-full richtext-max-w-sm richtext-items-center", children: /* @__PURE__ */ p(
        oe,
        {
          onChange: (a) => l({ ...o, height: a.target.value }),
          required: !0,
          type: "number",
          value: o.height
        }
      ) }) }),
      /* @__PURE__ */ p(Vt, { className: "richtext-mt-2 richtext-self-end", type: "submit", children: r("editor.link.dialog.button.apply") })
    ] }) })
  ] });
}, Wt = /* @__PURE__ */ new Map();
function ca(e) {
  const t = e.options.element;
  Wt.has("width") || Wt.set("width", t.clientWidth), Wt.has("width") && Wt.get("width") <= 0 && Wt.set("width", t.clientWidth);
  const n = { attributes: !0, childList: !0, subtree: !0 }, i = function() {
    Wt.set("width", t.clientWidth);
  }, s = new MutationObserver(i);
  return s.observe(t, n), e.on("destroy", () => {
    s.disconnect();
  }), { width: Wt.get("width") };
}
function Bp() {
  const e = nt(), t = G(), { t: n } = Z(), { width: i } = ca(t), s = ee(t, ke.name, {
    defaultShowPicker: !1,
    createUser: "",
    width: 0,
    height: 0
  }), { defaultShowPicker: r, createUser: o, width: l, height: c } = s, a = R(
    (f) => {
      t.chain().updateAttributes(ke.name, f).setNodeSelection(t.state.selection.from).focus().run();
    },
    [t]
  ), d = R(() => {
    const f = Kl.EXCALIDRAW(t.id);
    jl(f, s);
  }, [t, s]), h = R(() => t.isActive(ke.name), [t]), u = R(() => ne(ke.name, t), [t]);
  return et(() => {
    r && (d(), t.chain().updateAttributes(ke.name, { defaultShowPicker: !1 }).focus().run());
  }, [o, r, t, d]), e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleExcalidraw",
      shouldShow: h,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(v, { action: d, icon: "Pencil", tooltip: n("editor.edit") }),
        /* @__PURE__ */ p(Fr, { height: c, maxWidth: i, onOk: a, width: l, children: /* @__PURE__ */ p(v, { icon: "Settings", tooltip: n("editor.settings") }) }),
        /* @__PURE__ */ p(v, { action: u, icon: "Trash2", tooltip: n("editor.delete") })
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function $p() {
  const e = nt(), t = G(), { t: n } = Z(), { width: i, height: s, src: r } = ee(t, Ee.name, {
    width: 0,
    height: 0,
    src: "",
    defaultShowPicker: !1
  }), [o, l] = U(!1), [c, a] = U(""), d = R(
    (w) => {
      var b;
      (b = w == null ? void 0 : w.preventDefault) == null || b.call(w), l(!1);
    },
    [l]
  );
  et(() => {
    o && a(r);
  }, [o, r]);
  const h = R(
    (w) => {
      var C;
      (C = w == null ? void 0 : w.preventDefault) == null || C.call(w);
      const b = ec(c);
      t.chain().updateAttributes(Ee.name, {
        src: (b == null ? void 0 : b.src) || c
      }).setNodeSelection(t.state.selection.from).focus().run(), l(!1);
    },
    [t, c, l]
  ), u = R(() => {
    window.open(r, "_blank");
  }, [r]), f = R(
    (w) => {
      t.chain().updateAttributes(Ee.name, w).setNodeSelection(t.state.selection.from).focus().run();
    },
    [t]
  ), g = R(() => t.isActive(Ee.name) && !r, [t, r]), m = R(() => ne(Ee.name, t), [t]);
  return e ? /* @__PURE__ */ p(M, { children: /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleIframe",
      shouldShow: g,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(v, { action: u, icon: "Eye", tooltip: "Visit Link" }),
        /* @__PURE__ */ x(We, { onOpenChange: l, open: o, children: [
          /* @__PURE__ */ p(Ge, { asChild: !0, children: /* @__PURE__ */ p(v, { icon: "Pencil", tooltip: "Open Edit Link" }) }),
          /* @__PURE__ */ x(Ye, { children: [
            /* @__PURE__ */ p(Wl, { children: /* @__PURE__ */ p(qe, { children: "Edit Link Iframe" }) }),
            /* @__PURE__ */ p(
              oe,
              {
                autoFocus: !0,
                onInput: (w) => a(w.target.value),
                placeholder: "Enter link",
                type: "url",
                value: c
              }
            ),
            /* @__PURE__ */ x(Je, { children: [
              /* @__PURE__ */ p(Vt, { onClick: d, type: "button", children: "Cancel" }),
              /* @__PURE__ */ p(Vt, { onClick: h, type: "button", children: "OK" })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ p(Fr, { height: s, onOk: f, width: i, children: /* @__PURE__ */ p(v, { icon: "Settings", tooltip: n("editor.settings") }) }),
        /* @__PURE__ */ p(v, { action: m, icon: "Trash2", tooltip: n("editor.delete") })
      ] })
    }
  ) }) : /* @__PURE__ */ p(M, {});
}
function aa({ children: e, visible: t, toggleVisible: n }) {
  const { t: i } = Z(), s = G(), r = ee(s, xi.name, {
    text: "",
    macros: ""
  }), { text: o, macros: l } = r, [c, a] = U(decodeURIComponent(o || "")), [d, h] = U(decodeURIComponent(l || ""));
  et(() => {
    t && (a(decodeURIComponent(o || "")), h(decodeURIComponent(l || "")));
  }, [t]);
  const u = R(() => {
    s.chain().focus().setKatex({
      text: encodeURIComponent(c),
      macros: encodeURIComponent(d)
    }).run(), a(""), h(""), n(!1);
  }, [s, c, d, n]), f = pt(() => {
    try {
      return ic.renderToString(c, {
        macros: ql(d || "")
      });
    } catch {
      return c;
    }
  }, [c, d]), g = pt(() => `${c}`.trim() ? f : null, [c, f]);
  return /* @__PURE__ */ x(We, { onOpenChange: n, open: t, children: [
    /* @__PURE__ */ p(Ge, { asChild: !0, children: e }),
    /* @__PURE__ */ x(Ye, { className: "richtext-z-[99999] !richtext-max-w-[1300px]", children: [
      /* @__PURE__ */ p(qe, { children: i("editor.formula.dialog.text") }),
      /* @__PURE__ */ p("div", { style: { height: "100%", border: "1px solid hsl(var(--border))" }, children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-gap-[10px] richtext-rounded-[10px] richtext-p-[10px]", children: [
        /* @__PURE__ */ x("div", { className: "richtext-flex-1", children: [
          /* @__PURE__ */ p(zt, { className: "mb-[6px]", children: "Expression" }),
          /* @__PURE__ */ p(
            bi,
            {
              autoFocus: !0,
              className: "richtext-mb-[10px]",
              onChange: (m) => a(m.target.value),
              placeholder: "Text",
              required: !0,
              rows: 10,
              value: c,
              style: {
                color: "hsl(var(--foreground))"
              }
            }
          ),
          /* @__PURE__ */ p(zt, { className: "mb-[6px]", children: "Macros" }),
          /* @__PURE__ */ p(
            bi,
            {
              onChange: (m) => h(m.target.value),
              placeholder: "Macros",
              rows: 10,
              value: d,
              style: {
                color: "hsl(var(--foreground))"
              }
            }
          )
        ] }),
        /* @__PURE__ */ p(
          "div",
          {
            className: "richtext-flex richtext-flex-1 richtext-items-center richtext-justify-center richtext-rounded-[10px] richtext-p-[10px]",
            dangerouslySetInnerHTML: { __html: g || "" },
            style: {
              height: "100%",
              borderWidth: 1,
              minHeight: 500,
              background: "#fff"
            }
          }
        )
      ] }) }),
      /* @__PURE__ */ p(Je, { children: /* @__PURE__ */ p(Vt, { onClick: u, type: "button", children: "Save changes" }) })
    ] })
  ] });
}
function zp() {
  const e = nt(), t = G(), [n, i] = U(!1), s = R(() => t.isActive(xi.name), [t]), r = R(() => ne(xi.name, t), [t]);
  return e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleKatex",
      shouldShow: s,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(aa, { toggleVisible: i, visible: n, children: /* @__PURE__ */ p(v, { action: () => i(!n), tooltip: "Edit", children: /* @__PURE__ */ p(Jl, { size: 16 }) }) }),
        /* @__PURE__ */ p(v, { action: r, tooltip: "Delete", children: /* @__PURE__ */ p(Nr, { size: 16 }) })
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function ha(e) {
  const { t } = Z();
  return /* @__PURE__ */ x("div", { className: "richtext-flex richtext-flex-nowrap", children: [
    /* @__PURE__ */ p(
      v,
      {
        disabled: !(e != null && e.link),
        icon: "ExternalLink",
        tooltip: t("editor.link.open.tooltip"),
        tooltipOptions: { sideOffset: 15 },
        action: () => {
          window.open(e == null ? void 0 : e.link, "_blank");
        }
      }
    ),
    /* @__PURE__ */ p(
      v,
      {
        icon: "Pencil",
        tooltip: t("editor.link.edit.tooltip"),
        tooltipOptions: { sideOffset: 15 },
        action: () => {
          e == null || e.onEdit();
        }
      }
    ),
    /* @__PURE__ */ p(
      v,
      {
        icon: "Unlink",
        tooltip: t("editor.link.unlink.tooltip"),
        tooltipOptions: { sideOffset: 15 },
        action: () => {
          e == null || e.onClear();
        }
      }
    )
  ] });
}
function Vp() {
  const e = nt(), t = G(), [n, i] = U(!1), s = ee(t, Cs.name), r = s == null ? void 0 : s.href, o = R(({ editor: d }) => d.isActive(Cs.name), []), l = (d, h, u) => {
    const f = t.state.selection, { from: g } = f, m = (h == null ? void 0 : h.length) ?? 0, w = g + m;
    t.chain().extendMarkRange("link").insertContent({
      type: "text",
      text: h,
      marks: [
        {
          type: "link",
          attrs: {
            href: d,
            target: u ? "_blank" : ""
          }
        }
      ]
    }).setLink({ href: d }).setTextSelection({ from: g, to: w }).focus().run(), i(!1);
  }, c = R(() => {
    t.chain().extendMarkRange("link").unsetLink().focus().run(), i(!1);
  }, [t]), a = () => {
    i(!1);
  };
  return e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleLink",
      shouldShow: o,
      children: /* @__PURE__ */ p(M, { children: n ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-4 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: /* @__PURE__ */ p(sc, { editor: t, onClose: a, onSetLink: l }) }) : /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: /* @__PURE__ */ p(
        ha,
        {
          editor: t,
          link: r,
          onClear: c,
          onEdit: () => {
            i(!0);
          }
        }
      ) }) })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function qi({ item: e, disabled: t, editor: n }) {
  const i = e.component;
  return i ? /* @__PURE__ */ p(Fi, { children: e.type === "divider" ? /* @__PURE__ */ p(
    At,
    {
      className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
      orientation: "vertical"
    }
  ) : /* @__PURE__ */ p(
    i,
    {
      ...e.componentProps,
      disabled: t || (e == null ? void 0 : e.componentdisabled),
      editor: n
    }
  ) }) : /* @__PURE__ */ p(M, {});
}
function da(e) {
  return e.type.name === Gl.name;
}
function ua(e) {
  return e.type.name === nc.name;
}
function fa(e) {
  return e.type.name === Yl.name;
}
function Hp() {
  const { lang: e, t } = Z(), n = nt(), i = G(), s = ({ editor: o }) => {
    const { selection: l } = o.view.state, { $from: c, to: a } = l;
    let d = !1;
    return o.view.state.doc.nodesBetween(c.pos, a, (h) => {
      if (da(h))
        return d = !0, !1;
    }), d;
  }, r = pt(() => ea(i, t), [i, e, t]);
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: i,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleImage",
      shouldShow: s,
      children: r != null && r.length ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: r == null ? void 0 : r.map((o, l) => /* @__PURE__ */ p(qi, { editor: i, item: o }, `bubbleMenu-image-${l}`)) }) : /* @__PURE__ */ p(M, {})
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Fp() {
  const { lang: e, t } = Z(), n = nt(), i = G(), s = ({ editor: o }) => {
    const { selection: l } = o.view.state, { $from: c, to: a } = l;
    let d = !1;
    return o.view.state.doc.nodesBetween(c.pos, a, (h) => {
      if (ua(h))
        return d = !0, !1;
    }), d;
  }, r = pt(() => na(i, t), [i, t, e]);
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: i,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleImageGif",
      shouldShow: s,
      children: r != null && r.length ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: r == null ? void 0 : r.map((o, l) => /* @__PURE__ */ p(qi, { editor: i, item: o }, `bubbleMenu-image-gif-${l}`)) }) : /* @__PURE__ */ p(M, {})
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Kp() {
  const { t: e } = Z(), t = nt(), n = G(), i = ({ editor: r }) => {
    const { selection: o } = r.view.state, { $from: l, to: c } = o;
    let a = !1;
    return r.view.state.doc.nodesBetween(l.pos, c, (d) => {
      if (fa(d))
        return a = !0, !1;
    }), a;
  }, s = pt(() => ra(n, e), [n, e]);
  return t ? /* @__PURE__ */ p(
    ct,
    {
      editor: n,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleVideo",
      shouldShow: i,
      children: s != null && s.length ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: s == null ? void 0 : s.map((r, o) => /* @__PURE__ */ p(qi, { editor: n, item: r }, `bubbleMenu-video-${o}`)) }) : /* @__PURE__ */ p(M, {})
    }
  ) : /* @__PURE__ */ p(M, {});
}
const Rs = `graph TB
a-->b`, pa = ({ editor: e, attrs: t, extension: n }) => {
  const { alt: i, align: s } = t, [r, o] = U(decodeURIComponent(i ?? Rs)), [l, c] = U(""), [a, d] = U(!1), h = ut(null), [u, f] = U(null), g = n == null ? void 0 : n.options.upload, m = R((y) => {
    y && import("mermaid").then((T) => {
      f(T.default);
    });
  }, []), w = async (y) => {
    try {
      const { svg: T } = await u.render("mermaid-svg", y);
      c(T);
    } catch {
      c("");
    }
  }, b = () => {
    u.initialize({
      darkMode: !1,
      startOnLoad: !1,
      // fontFamily:'',
      fontSize: 12,
      theme: "base"
    }), w(r);
  };
  return et(() => {
    u && a && b();
  }, [u, a]), et(() => {
    u && a && w(r);
  }, [u, r]), /* @__PURE__ */ x(We, { onOpenChange: d, open: a, children: [
    /* @__PURE__ */ p(Ge, { asChild: !0, children: /* @__PURE__ */ p(v, { action: () => d(!0), icon: "Pencil", tooltip: "Edit Mermaid" }) }),
    /* @__PURE__ */ x(Ye, { className: "richtext-z-[99999] !richtext-max-w-[1300px]", children: [
      /* @__PURE__ */ p(qe, { children: "Edit Mermaid" }),
      /* @__PURE__ */ p("div", { ref: m, style: { height: "100%", border: "1px solid hsl(var(--border))" }, children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-gap-[10px] richtext-rounded-[10px] richtext-p-[10px]", children: [
        /* @__PURE__ */ p(
          bi,
          {
            autoFocus: !0,
            className: "richtext-flex-1",
            defaultValue: Rs,
            onChange: (y) => o(y.target.value),
            placeholder: "Text",
            required: !0,
            rows: 10,
            value: r,
            style: {
              color: "hsl(var(--foreground))"
            }
          }
        ),
        /* @__PURE__ */ p(
          "div",
          {
            className: "richtext-flex richtext-flex-1 richtext-items-center richtext-justify-center richtext-rounded-[10px] richtext-p-[10px]",
            dangerouslySetInnerHTML: { __html: l },
            ref: h,
            style: {
              height: "100%",
              border: "1px solid hsl(var(--border))",
              minHeight: 500
            }
          }
        )
      ] }) }),
      /* @__PURE__ */ p(Je, { children: /* @__PURE__ */ p(Vt, { onClick: async () => {
        if (r !== "") {
          if (r) {
            const y = h.current.querySelector("svg"), { width: T, height: z } = y.getBoundingClientRect(), I = `mermaid-${Lr()}.svg`;
            let E = Ur(y.outerHTML);
            if (g) {
              const O = Rr(E, I);
              E = await g(O);
            }
            e == null || e.chain().focus().setMermaid(
              {
                type: "mermaid",
                src: E,
                alt: encodeURIComponent(r),
                width: T,
                height: z
              },
              !!r
            ).run(), e == null || e.commands.setAlignImageMermaid(s);
          }
          d(!1);
        }
      }, type: "button", children: "Save changes" }) })
    ] })
  ] });
};
function ga({ item: e, disabled: t, editor: n }) {
  var s;
  const i = e.component;
  return i ? /* @__PURE__ */ p(Fi, { children: e.type === "divider" ? /* @__PURE__ */ p(
    At,
    {
      className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
      orientation: "vertical"
    }
  ) : /* @__PURE__ */ p(
    i,
    {
      ...e.componentProps,
      disabled: t || ((s = e == null ? void 0 : e.componentProps) == null ? void 0 : s.disabled),
      editor: n
    }
  ) }) : /* @__PURE__ */ p(M, {});
}
function ma(e) {
  return e.type.name === vi.name;
}
function jp() {
  const { lang: e, t } = Z(), n = nt(), i = G(), s = ee(i, vi.name), r = ji(vi.name), o = ({ editor: c }) => {
    const { selection: a } = c.view.state, { $from: d, to: h } = a;
    let u = !1;
    return c.view.state.doc.nodesBetween(d.pos, h, (f) => {
      if (ma(f))
        return u = !0, !1;
    }), u;
  }, l = pt(() => ia(i, t), [i, e, t]);
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: i,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleMermaid",
      shouldShow: o,
      children: l != null && l.length ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: l == null ? void 0 : l.map((c, a) => c.type === "edit" && (s != null && s.src) ? /* @__PURE__ */ p(
        pa,
        {
          attrs: s,
          editor: i,
          extension: r
        },
        `bubbleMenu-mermaid-${a}`
      ) : /* @__PURE__ */ p(ga, { editor: i, item: c }, `bubbleMenu-mermaid-${a}`)) }) : /* @__PURE__ */ p(M, {})
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Wp({ hiddenActions: e = [] }) {
  var m, w, b, C, y, T, z, I, E, O, Q, st, gt, k, D, S, j, H;
  const { t } = Z(), n = nt(), i = G(), s = ({ editor: W }) => Tl(W.view.state, Pl.name), r = (W) => e.includes(W);
  function o() {
    i.chain().focus().addColumnBefore().run();
  }
  function l() {
    i.chain().focus().addColumnAfter().run();
  }
  function c() {
    i.chain().focus().deleteColumn().run();
  }
  function a() {
    i.chain().focus().addRowBefore().run();
  }
  function d() {
    i.chain().focus().addRowAfter().run();
  }
  function h() {
    i.chain().focus().deleteRow().run();
  }
  function u() {
    i.chain().focus().mergeCells().run();
  }
  function f() {
    i == null || i.chain().focus().splitCell().run();
  }
  function g() {
    i.chain().focus().deleteTable().run();
  }
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: i,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleTable",
      shouldShow: s,
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        !r("addColumnBefore") && /* @__PURE__ */ p(
          v,
          {
            action: o,
            disabled: !((w = (m = i == null ? void 0 : i.can()) == null ? void 0 : m.addColumnBefore) != null && w.call(m)),
            icon: "BetweenHorizonalEnd",
            tooltip: t("editor.table.menu.insertColumnBefore"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        !r("addColumnAfter") && /* @__PURE__ */ p(
          v,
          {
            action: l,
            disabled: !((C = (b = i == null ? void 0 : i.can()) == null ? void 0 : b.addColumnAfter) != null && C.call(b)),
            icon: "BetweenHorizonalStart",
            tooltip: t("editor.table.menu.insertColumnAfter"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        !r("deleteColumn") && /* @__PURE__ */ p(
          v,
          {
            action: c,
            disabled: !((T = i == null ? void 0 : (y = i.can()).deleteColumn) != null && T.call(y)),
            icon: "DeleteColumn",
            tooltip: t("editor.table.menu.deleteColumn"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        /* @__PURE__ */ p(
          At,
          {
            className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
            orientation: "vertical"
          }
        ),
        !r("addRowAbove") && /* @__PURE__ */ p(
          v,
          {
            action: a,
            disabled: !((I = i == null ? void 0 : (z = i.can()).addRowBefore) != null && I.call(z)),
            icon: "BetweenVerticalEnd",
            tooltip: t("editor.table.menu.insertRowAbove"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        !r("addRowBelow") && /* @__PURE__ */ p(
          v,
          {
            action: d,
            disabled: !((O = (E = i == null ? void 0 : i.can()) == null ? void 0 : E.addRowAfter) != null && O.call(E)),
            icon: "BetweenVerticalStart",
            tooltip: t("editor.table.menu.insertRowBelow"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        !r("deleteRow") && /* @__PURE__ */ p(
          v,
          {
            action: h,
            disabled: !((st = (Q = i == null ? void 0 : i.can()) == null ? void 0 : Q.deleteRow) != null && st.call(Q)),
            icon: "DeleteRow",
            tooltip: t("editor.table.menu.deleteRow"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        /* @__PURE__ */ p(
          At,
          {
            className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
            orientation: "vertical"
          }
        ),
        !r("mergeCells") && /* @__PURE__ */ p(
          v,
          {
            action: u,
            disabled: !((k = (gt = i == null ? void 0 : i.can()) == null ? void 0 : gt.mergeCells) != null && k.call(gt)),
            icon: "TableCellsMerge",
            tooltip: t("editor.table.menu.mergeCells"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        !r("splitCells") && /* @__PURE__ */ p(
          v,
          {
            action: f,
            disabled: !((S = (D = i == null ? void 0 : i.can()) == null ? void 0 : D.splitCell) != null && S.call(D)),
            icon: "TableCellsSplit",
            tooltip: t("editor.table.menu.splitCells"),
            tooltipOptions: { sideOffset: 15 }
          }
        ),
        /* @__PURE__ */ p(
          At,
          {
            className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
            orientation: "vertical"
          }
        ),
        !r("deleteTable") && /* @__PURE__ */ p(
          v,
          {
            action: g,
            disabled: !((H = (j = i == null ? void 0 : i.can()) == null ? void 0 : j.deleteTable) != null && H.call(j)),
            icon: "Trash2",
            tooltip: t("editor.table.menu.deleteTable"),
            tooltipOptions: { sideOffset: 15 }
          }
        )
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function wa() {
  const { t: e } = Z(), [t, n] = U(!1), i = G(), s = pt(() => {
    var o, l;
    return (l = (o = vc({ t: e })) == null ? void 0 : o[0]) == null ? void 0 : l.commands;
  }, [e]), r = pt(() => {
    var l;
    return (l = s == null ? void 0 : s.find((c) => {
      var a;
      return (a = c == null ? void 0 : c.isActive) == null ? void 0 : a.call(c, i);
    })) == null ? void 0 : l.label;
  }, [i.state.selection.ranges, t, i, s, e]);
  return /* @__PURE__ */ x(Wi, { modal: !0, onOpenChange: n, open: t, children: [
    /* @__PURE__ */ p(
      Gi,
      {
        asChild: !0,
        className: "hover:richtext-bg-accent data-[state=on]:richtext-bg-accent",
        children: /* @__PURE__ */ x(v, { dataState: !!r, children: [
          r ? /* @__PURE__ */ p(M, { children: r }) : /* @__PURE__ */ p(M, { children: e("editor.paragraph.tooltip") }),
          /* @__PURE__ */ p(
            at,
            {
              className: "richtext-ml-1 richtext-size-3 richtext-text-zinc-500",
              name: "MenuDown"
            }
          )
        ] })
      }
    ),
    /* @__PURE__ */ p(
      Yi,
      {
        align: "start",
        className: "!richtext-w-[initial] !richtext-p-[4px]",
        hideWhenDetached: !0,
        side: "bottom",
        children: s == null ? void 0 : s.map((o) => {
          var c;
          const l = (c = o == null ? void 0 : o.isActive) == null ? void 0 : c.call(o, i);
          return /* @__PURE__ */ x(
            "div",
            {
              className: "richtext-flex richtext-w-full richtext-items-center richtext-gap-3 richtext-rounded-sm !richtext-border-none !richtext-bg-transparent richtext-px-2 richtext-py-1.5 richtext-text-left richtext-text-sm richtext-text-foreground !richtext-outline-none richtext-transition-colors hover:!richtext-bg-accent",
              onClick: (a) => {
                a.preventDefault(), o.action({
                  editor: i,
                  range: i.state.selection.ranges
                }), n(!1);
              },
              children: [
                /* @__PURE__ */ x("div", { className: "!richtext-min-w-[20px]", children: [
                  l && /* @__PURE__ */ p(yi, { size: 16 }),
                  !r && o.label === e("editor.paragraph.tooltip") && !l && /* @__PURE__ */ p(yi, { size: 16 })
                ] }),
                /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-1", children: [
                  o.iconName && /* @__PURE__ */ p(
                    at,
                    {
                      className: "!richtext-mr-1 !richtext-text-lg",
                      name: o.iconName
                    }
                  ),
                  o.label
                ] })
              ]
            },
            o.name
          );
        })
      }
    )
  ] });
}
function ba() {
  return /* @__PURE__ */ x(M, { children: [
    /* @__PURE__ */ p(wa, {}),
    /* @__PURE__ */ p(
      At,
      {
        className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
        orientation: "vertical"
      }
    ),
    /* @__PURE__ */ p(mc, {}),
    /* @__PURE__ */ p(yc, {}),
    /* @__PURE__ */ p(Cc, {}),
    /* @__PURE__ */ p(Sc, {}),
    /* @__PURE__ */ p(wc, {}),
    /* @__PURE__ */ p(rc, {}),
    /* @__PURE__ */ p(
      At,
      {
        className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
        orientation: "vertical"
      }
    ),
    /* @__PURE__ */ p(bc, {}),
    /* @__PURE__ */ p(xc, {}),
    /* @__PURE__ */ p(pc, {})
  ] });
}
function Gp({ buttonBubble: e }) {
  const t = G(), n = nt(), i = ({ editor: s }) => {
    const { selection: r } = s.view.state, { $from: o, to: l } = r;
    return s.isActive("codeBlock") || o.pos === l ? !1 : r instanceof Nl;
  };
  return n ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleText",
      shouldShow: i,
      children: e ? /* @__PURE__ */ p(M, { children: e }) : /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: /* @__PURE__ */ p(ba, {}) })
    }
  ) : /* @__PURE__ */ p(M, {});
}
function Yp() {
  const { t: e } = Z(), t = nt(), n = G(), [i, s] = U(!1), r = R(({ editor: c }) => c.isActive(Os.name), []), o = (c) => {
    n.commands.updateTweet({ src: c }), s(!1);
  }, l = R(() => ne(Os.name, n), [n]);
  return t ? /* @__PURE__ */ p(
    ct,
    {
      editor: n,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleTwitter",
      shouldShow: r,
      children: /* @__PURE__ */ p(M, { children: i ? /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-4 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: /* @__PURE__ */ p(kc, { editor: n, onSetLink: o }) }) : /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(
          v,
          {
            icon: "Pencil",
            tooltip: e("editor.link.edit.tooltip"),
            tooltipOptions: { sideOffset: 15 },
            action: () => {
              s(!0);
            }
          }
        ),
        /* @__PURE__ */ p(
          v,
          {
            action: l,
            icon: "Trash",
            tooltip: e("editor.delete"),
            tooltipOptions: { sideOffset: 15 }
          }
        )
      ] }) })
    }
  ) : /* @__PURE__ */ p(M, {});
}
const St = () => /* @__PURE__ */ new Map(), Ci = (e) => {
  const t = St();
  return e.forEach((n, i) => {
    t.set(i, n);
  }), t;
}, Ut = (e, t, n) => {
  let i = e.get(t);
  return i === void 0 && e.set(t, i = n()), i;
}, xa = (e, t) => {
  const n = [];
  for (const [i, s] of e)
    n.push(t(s, i));
  return n;
}, ya = (e, t) => {
  for (const [n, i] of e)
    if (t(i, n))
      return !0;
  return !1;
}, Jt = () => /* @__PURE__ */ new Set(), ni = (e) => e[e.length - 1], va = (e, t) => {
  for (let n = 0; n < t.length; n++)
    e.push(t[n]);
}, Xt = Array.from, Ji = (e, t) => {
  for (let n = 0; n < e.length; n++)
    if (!t(e[n], n, e))
      return !1;
  return !0;
}, Xi = (e, t) => {
  for (let n = 0; n < e.length; n++)
    if (t(e[n], n, e))
      return !0;
  return !1;
}, Sa = (e, t) => {
  const n = new Array(e);
  for (let i = 0; i < e; i++)
    n[i] = t(i, n);
  return n;
}, ue = Array.isArray;
class Kr {
  constructor() {
    this._observers = St();
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  on(t, n) {
    return Ut(
      this._observers,
      /** @type {string} */
      t,
      Jt
    ).add(n), n;
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  once(t, n) {
    const i = (...s) => {
      this.off(
        t,
        /** @type {any} */
        i
      ), n(...s);
    };
    this.on(
      t,
      /** @type {any} */
      i
    );
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  off(t, n) {
    const i = this._observers.get(t);
    i !== void 0 && (i.delete(n), i.size === 0 && this._observers.delete(t));
  }
  /**
   * Emit a named event. All registered event listeners that listen to the
   * specified name will receive the event.
   *
   * @todo This should catch exceptions
   *
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name The event name.
   * @param {Parameters<EVENTS[NAME]>} args The arguments that are applied to the event listener.
   */
  emit(t, n) {
    return Xt((this._observers.get(t) || St()).values()).forEach((i) => i(...n));
  }
  destroy() {
    this._observers = St();
  }
}
const Ct = Math.floor, hn = Math.abs, fe = (e, t) => e < t ? e : t, Ht = (e, t) => e > t ? e : t, jr = (e) => e !== 0 ? e < 0 : 1 / e < 0, Ns = 1, Ls = 2, ii = 4, si = 8, Oe = 32, Nt = 64, ot = 128, Ca = 1 << 29, Un = 31, ki = 63, qt = 127, ka = 2147483647, bn = Number.MAX_SAFE_INTEGER, Us = Number.MIN_SAFE_INTEGER, Ea = Number.isInteger || ((e) => typeof e == "number" && isFinite(e) && Ct(e) === e), Wr = String.fromCharCode, _a = (e) => e.toLowerCase(), Ta = /^\s*/g, Da = (e) => e.replace(Ta, ""), Aa = /([A-Z])/g, Ps = (e, t) => Da(e.replace(Aa, (n) => `${t}${_a(n)}`)), Ia = (e) => {
  const t = unescape(encodeURIComponent(e)), n = t.length, i = new Uint8Array(n);
  for (let s = 0; s < n; s++)
    i[s] = /** @type {number} */
    t.codePointAt(s);
  return i;
}, Me = (
  /** @type {TextEncoder} */
  typeof TextEncoder < "u" ? new TextEncoder() : null
), Oa = (e) => Me.encode(e), Ma = Me ? Oa : Ia;
let Ae = typeof TextDecoder > "u" ? null : new TextDecoder("utf-8", { fatal: !0, ignoreBOM: !0 });
Ae && Ae.decode(new Uint8Array()).length === 1 && (Ae = null);
const Ra = (e, t) => Sa(t, () => e).join("");
class Xe {
  constructor() {
    this.cpos = 0, this.cbuf = new Uint8Array(100), this.bufs = [];
  }
}
const Ze = () => new Xe(), Na = (e) => {
  const t = Ze();
  return e(t), vt(t);
}, La = (e) => {
  let t = e.cpos;
  for (let n = 0; n < e.bufs.length; n++)
    t += e.bufs[n].length;
  return t;
}, vt = (e) => {
  const t = new Uint8Array(La(e));
  let n = 0;
  for (let i = 0; i < e.bufs.length; i++) {
    const s = e.bufs[i];
    t.set(s, n), n += s.length;
  }
  return t.set(new Uint8Array(e.cbuf.buffer, 0, e.cpos), n), t;
}, Ua = (e, t) => {
  const n = e.cbuf.length;
  n - e.cpos < t && (e.bufs.push(new Uint8Array(e.cbuf.buffer, 0, e.cpos)), e.cbuf = new Uint8Array(Ht(n, t) * 2), e.cpos = 0);
}, Y = (e, t) => {
  const n = e.cbuf.length;
  e.cpos === n && (e.bufs.push(e.cbuf), e.cbuf = new Uint8Array(n * 2), e.cpos = 0), e.cbuf[e.cpos++] = t;
}, Ei = Y, A = (e, t) => {
  for (; t > qt; )
    Y(e, ot | qt & t), t = Ct(t / 128);
  Y(e, qt & t);
}, Zi = (e, t) => {
  const n = jr(t);
  for (n && (t = -t), Y(e, (t > ki ? ot : 0) | (n ? Nt : 0) | ki & t), t = Ct(t / 64); t > 0; )
    Y(e, (t > qt ? ot : 0) | qt & t), t = Ct(t / 128);
}, _i = new Uint8Array(3e4), Pa = _i.length / 3, Ba = (e, t) => {
  if (t.length < Pa) {
    const n = Me.encodeInto(t, _i).written || 0;
    A(e, n);
    for (let i = 0; i < n; i++)
      Y(e, _i[i]);
  } else
    dt(e, Ma(t));
}, $a = (e, t) => {
  const n = unescape(encodeURIComponent(t)), i = n.length;
  A(e, i);
  for (let s = 0; s < i; s++)
    Y(
      e,
      /** @type {number} */
      n.codePointAt(s)
    );
}, le = Me && /** @type {any} */
Me.encodeInto ? Ba : $a, Pn = (e, t) => {
  const n = e.cbuf.length, i = e.cpos, s = fe(n - i, t.length), r = t.length - s;
  e.cbuf.set(t.subarray(0, s), i), e.cpos += s, r > 0 && (e.bufs.push(e.cbuf), e.cbuf = new Uint8Array(Ht(n * 2, r)), e.cbuf.set(t.subarray(s)), e.cpos = r);
}, dt = (e, t) => {
  A(e, t.byteLength), Pn(e, t);
}, Qi = (e, t) => {
  Ua(e, t);
  const n = new DataView(e.cbuf.buffer, e.cpos, t);
  return e.cpos += t, n;
}, za = (e, t) => Qi(e, 4).setFloat32(0, t, !1), Va = (e, t) => Qi(e, 8).setFloat64(0, t, !1), Ha = (e, t) => (
  /** @type {any} */
  Qi(e, 8).setBigInt64(0, t, !1)
), Bs = new DataView(new ArrayBuffer(4)), Fa = (e) => (Bs.setFloat32(0, e), Bs.getFloat32(0) === e), pe = (e, t) => {
  switch (typeof t) {
    case "string":
      Y(e, 119), le(e, t);
      break;
    case "number":
      Ea(t) && hn(t) <= ka ? (Y(e, 125), Zi(e, t)) : Fa(t) ? (Y(e, 124), za(e, t)) : (Y(e, 123), Va(e, t));
      break;
    case "bigint":
      Y(e, 122), Ha(e, t);
      break;
    case "object":
      if (t === null)
        Y(e, 126);
      else if (ue(t)) {
        Y(e, 117), A(e, t.length);
        for (let n = 0; n < t.length; n++)
          pe(e, t[n]);
      } else if (t instanceof Uint8Array)
        Y(e, 116), dt(e, t);
      else {
        Y(e, 118);
        const n = Object.keys(t);
        A(e, n.length);
        for (let i = 0; i < n.length; i++) {
          const s = n[i];
          le(e, s), pe(e, t[s]);
        }
      }
      break;
    case "boolean":
      Y(e, t ? 120 : 121);
      break;
    default:
      Y(e, 127);
  }
};
class $s extends Xe {
  /**
   * @param {function(Encoder, T):void} writer
   */
  constructor(t) {
    super(), this.w = t, this.s = null, this.count = 0;
  }
  /**
   * @param {T} v
   */
  write(t) {
    this.s === t ? this.count++ : (this.count > 0 && A(this, this.count - 1), this.count = 1, this.w(this, t), this.s = t);
  }
}
const zs = (e) => {
  e.count > 0 && (Zi(e.encoder, e.count === 1 ? e.s : -e.s), e.count > 1 && A(e.encoder, e.count - 2));
};
class dn {
  constructor() {
    this.encoder = new Xe(), this.s = 0, this.count = 0;
  }
  /**
   * @param {number} v
   */
  write(t) {
    this.s === t ? this.count++ : (zs(this), this.count = 1, this.s = t);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return zs(this), vt(this.encoder);
  }
}
const Vs = (e) => {
  if (e.count > 0) {
    const t = e.diff * 2 + (e.count === 1 ? 0 : 1);
    Zi(e.encoder, t), e.count > 1 && A(e.encoder, e.count - 2);
  }
};
class ri {
  constructor() {
    this.encoder = new Xe(), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @param {number} v
   */
  write(t) {
    this.diff === t - this.s ? (this.s = t, this.count++) : (Vs(this), this.count = 1, this.diff = t - this.s, this.s = t);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return Vs(this), vt(this.encoder);
  }
}
class Ka {
  constructor() {
    this.sarr = [], this.s = "", this.lensE = new dn();
  }
  /**
   * @param {string} string
   */
  write(t) {
    this.s += t, this.s.length > 19 && (this.sarr.push(this.s), this.s = ""), this.lensE.write(t.length);
  }
  toUint8Array() {
    const t = new Xe();
    return this.sarr.push(this.s), this.s = "", le(t, this.sarr.join("")), Pn(t, this.lensE.toUint8Array()), vt(t);
  }
}
const Ot = (e) => new Error(e), xt = () => {
  throw Ot("Method unimplemented");
}, lt = () => {
  throw Ot("Unexpected case");
}, Gr = Ot("Unexpected end of array"), Yr = Ot("Integer out of Range");
class Bn {
  /**
   * @param {Uint8Array<Buf>} uint8Array Binary data to decode
   */
  constructor(t) {
    this.arr = t, this.pos = 0;
  }
}
const ts = (e) => new Bn(e), ja = (e) => e.pos !== e.arr.length, Wa = (e, t) => {
  const n = new Uint8Array(e.arr.buffer, e.pos + e.arr.byteOffset, t);
  return e.pos += t, n;
}, mt = (e) => Wa(e, P(e)), Re = (e) => e.arr[e.pos++], P = (e) => {
  let t = 0, n = 1;
  const i = e.arr.length;
  for (; e.pos < i; ) {
    const s = e.arr[e.pos++];
    if (t = t + (s & qt) * n, n *= 128, s < ot)
      return t;
    if (t > bn)
      throw Yr;
  }
  throw Gr;
}, es = (e) => {
  let t = e.arr[e.pos++], n = t & ki, i = 64;
  const s = (t & Nt) > 0 ? -1 : 1;
  if ((t & ot) === 0)
    return s * n;
  const r = e.arr.length;
  for (; e.pos < r; ) {
    if (t = e.arr[e.pos++], n = n + (t & qt) * i, i *= 128, t < ot)
      return s * n;
    if (n > bn)
      throw Yr;
  }
  throw Gr;
}, Ga = (e) => {
  let t = P(e);
  if (t === 0)
    return "";
  {
    let n = String.fromCodePoint(Re(e));
    if (--t < 100)
      for (; t--; )
        n += String.fromCodePoint(Re(e));
    else
      for (; t > 0; ) {
        const i = t < 1e4 ? t : 1e4, s = e.arr.subarray(e.pos, e.pos + i);
        e.pos += i, n += String.fromCodePoint.apply(
          null,
          /** @type {any} */
          s
        ), t -= i;
      }
    return decodeURIComponent(escape(n));
  }
}, Ya = (e) => (
  /** @type any */
  Ae.decode(mt(e))
), Ti = Ae ? Ya : Ga, ns = (e, t) => {
  const n = new DataView(e.arr.buffer, e.arr.byteOffset + e.pos, t);
  return e.pos += t, n;
}, qa = (e) => ns(e, 4).getFloat32(0, !1), Ja = (e) => ns(e, 8).getFloat64(0, !1), Xa = (e) => (
  /** @type {any} */
  ns(e, 8).getBigInt64(0, !1)
), Za = [
  (e) => {
  },
  // CASE 127: undefined
  (e) => null,
  // CASE 126: null
  es,
  // CASE 125: integer
  qa,
  // CASE 124: float32
  Ja,
  // CASE 123: float64
  Xa,
  // CASE 122: bigint
  (e) => !1,
  // CASE 121: boolean (false)
  (e) => !0,
  // CASE 120: boolean (true)
  Ti,
  // CASE 119: string
  (e) => {
    const t = P(e), n = {};
    for (let i = 0; i < t; i++) {
      const s = Ti(e);
      n[s] = xn(e);
    }
    return n;
  },
  (e) => {
    const t = P(e), n = [];
    for (let i = 0; i < t; i++)
      n.push(xn(e));
    return n;
  },
  mt
  // CASE 116: Uint8Array
], xn = (e) => Za[127 - Re(e)](e);
class Hs extends Bn {
  /**
   * @param {Uint8Array} uint8Array
   * @param {function(Decoder):T} reader
   */
  constructor(t, n) {
    super(t), this.reader = n, this.s = null, this.count = 0;
  }
  read() {
    return this.count === 0 && (this.s = this.reader(this), ja(this) ? this.count = P(this) + 1 : this.count = -1), this.count--, /** @type {T} */
    this.s;
  }
}
class un extends Bn {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(t) {
    super(t), this.s = 0, this.count = 0;
  }
  read() {
    if (this.count === 0) {
      this.s = es(this);
      const t = jr(this.s);
      this.count = 1, t && (this.s = -this.s, this.count = P(this) + 2);
    }
    return this.count--, /** @type {number} */
    this.s;
  }
}
class oi extends Bn {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(t) {
    super(t), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @return {number}
   */
  read() {
    if (this.count === 0) {
      const t = es(this), n = t & 1;
      this.diff = Ct(t / 2), this.count = 1, n && (this.count = P(this) + 2);
    }
    return this.s += this.diff, this.count--, this.s;
  }
}
class Qa {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(t) {
    this.decoder = new un(t), this.str = Ti(this.decoder), this.spos = 0;
  }
  /**
   * @return {string}
   */
  read() {
    const t = this.spos + this.decoder.read(), n = this.str.slice(this.spos, t);
    return this.spos = t, n;
  }
}
const th = crypto.getRandomValues.bind(crypto), eh = Math.random, qr = () => th(new Uint32Array(1))[0], nh = (e) => e[Ct(eh() * e.length)], ih = "10000000-1000-4000-8000" + -1e11, sh = () => ih.replace(
  /[018]/g,
  /** @param {number} c */
  (e) => (e ^ qr() & 15 >> e / 4).toString(16)
), rh = Date.now, Fs = (e) => (
  /** @type {Promise<T>} */
  new Promise(e)
);
Promise.all.bind(Promise);
const Ks = (e) => e === void 0 ? null : e;
class oh {
  constructor() {
    this.map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {string} key
   * @param {any} newValue
   */
  setItem(t, n) {
    this.map.set(t, n);
  }
  /**
   * @param {string} key
   */
  getItem(t) {
    return this.map.get(t);
  }
}
let Jr = new oh(), lh = !0;
try {
  typeof localStorage < "u" && localStorage && (Jr = localStorage, lh = !1);
} catch {
}
const ch = Jr, Ne = Symbol("Equality"), Xr = (e, t) => {
  var n;
  return e === t || !!((n = e == null ? void 0 : e[Ne]) != null && n.call(e, t)) || !1;
}, ah = (e) => typeof e == "object", hh = Object.assign, Zr = Object.keys, dh = (e, t) => {
  for (const n in e)
    t(e[n], n);
}, yn = (e) => Zr(e).length, uh = (e) => {
  for (const t in e)
    return !1;
  return !0;
}, be = (e, t) => {
  for (const n in e)
    if (!t(e[n], n))
      return !1;
  return !0;
}, is = (e, t) => Object.prototype.hasOwnProperty.call(e, t), fh = (e, t) => e === t || yn(e) === yn(t) && be(e, (n, i) => (n !== void 0 || is(t, i)) && Xr(t[i], n)), ph = Object.freeze, Qr = (e) => {
  for (const t in e) {
    const n = e[t];
    (typeof n == "object" || typeof n == "function") && Qr(e[t]);
  }
  return ph(e);
}, ss = (e, t, n = 0) => {
  try {
    for (; n < e.length; n++)
      e[n](...t);
  } finally {
    n < e.length && ss(e, t, n + 1);
  }
}, fn = (e, t) => {
  if (e === t)
    return !0;
  if (e == null || t == null || e.constructor !== t.constructor && (e.constructor || Object) !== (t.constructor || Object))
    return !1;
  if (e[Ne] != null)
    return e[Ne](t);
  switch (e.constructor) {
    case ArrayBuffer:
      e = new Uint8Array(e), t = new Uint8Array(t);
    // eslint-disable-next-line no-fallthrough
    case Uint8Array: {
      if (e.byteLength !== t.byteLength)
        return !1;
      for (let n = 0; n < e.length; n++)
        if (e[n] !== t[n])
          return !1;
      break;
    }
    case Set: {
      if (e.size !== t.size)
        return !1;
      for (const n of e)
        if (!t.has(n))
          return !1;
      break;
    }
    case Map: {
      if (e.size !== t.size)
        return !1;
      for (const n of e.keys())
        if (!t.has(n) || !fn(e.get(n), t.get(n)))
          return !1;
      break;
    }
    case void 0:
    case Object:
      if (yn(e) !== yn(t))
        return !1;
      for (const n in e)
        if (!is(e, n) || !fn(e[n], t[n]))
          return !1;
      break;
    case Array:
      if (e.length !== t.length)
        return !1;
      for (let n = 0; n < e.length; n++)
        if (!fn(e[n], t[n]))
          return !1;
      break;
    default:
      return !1;
  }
  return !0;
}, gh = (e, t) => t.includes(e), ge = typeof process < "u" && process.release && /node|io\.js/.test(process.release.name) && Object.prototype.toString.call(typeof process < "u" ? process : 0) === "[object process]", to = typeof window < "u" && typeof document < "u" && !ge;
let Tt;
const mh = () => {
  if (Tt === void 0)
    if (ge) {
      Tt = St();
      const e = process.argv;
      let t = null;
      for (let n = 0; n < e.length; n++) {
        const i = e[n];
        i[0] === "-" ? (t !== null && Tt.set(t, ""), t = i) : t !== null && (Tt.set(t, i), t = null);
      }
      t !== null && Tt.set(t, "");
    } else typeof location == "object" ? (Tt = St(), (location.search || "?").slice(1).split("&").forEach((e) => {
      if (e.length !== 0) {
        const [t, n] = e.split("=");
        Tt.set(`--${Ps(t, "-")}`, n), Tt.set(`-${Ps(t, "-")}`, n);
      }
    })) : Tt = St();
  return Tt;
}, Di = (e) => mh().has(e), vn = (e) => Ks(ge ? process.env[e.toUpperCase().replaceAll("-", "_")] : ch.getItem(e)), eo = (e) => Di("--" + e) || vn(e) !== null, wh = eo("production"), bh = ge && gh(process.env.FORCE_COLOR, ["true", "1", "2"]), xh = bh || !Di("--no-colors") && // @todo deprecate --no-colors
!eo("no-color") && (!ge || process.stdout.isTTY) && (!ge || Di("--color") || vn("COLORTERM") !== null || (vn("TERM") || "").includes("color")), yh = (e) => {
  let t = "";
  for (let n = 0; n < e.byteLength; n++)
    t += Wr(e[n]);
  return btoa(t);
}, vh = (e) => Buffer.from(e.buffer, e.byteOffset, e.byteLength).toString("base64"), Sh = to ? yh : vh, Ch = (e) => Na((t) => pe(t, e));
class kh {
  /**
   * @param {L} left
   * @param {R} right
   */
  constructor(t, n) {
    this.left = t, this.right = n;
  }
}
const Rt = (e, t) => new kh(e, t), js = (e) => e.next() >= 0.5, li = (e, t, n) => Ct(e.next() * (n + 1 - t) + t), no = (e, t, n) => Ct(e.next() * (n + 1 - t) + t), rs = (e, t, n) => no(e, t, n), Eh = (e) => Wr(rs(e, 97, 122)), _h = (e, t = 0, n = 20) => {
  const i = rs(e, t, n);
  let s = "";
  for (let r = 0; r < i; r++)
    s += Eh(e);
  return s;
}, ci = (e, t) => t[rs(e, 0, t.length - 1)], Th = Symbol("0schema");
class Dh {
  constructor() {
    this._rerrs = [];
  }
  /**
   * @param {string?} path
   * @param {string} expected
   * @param {string} has
   * @param {string?} message
   */
  extend(t, n, i, s = null) {
    this._rerrs.push({ path: t, expected: n, has: i, message: s });
  }
  toString() {
    const t = [];
    for (let n = this._rerrs.length - 1; n > 0; n--) {
      const i = this._rerrs[n];
      t.push(Ra(" ", (this._rerrs.length - n) * 2) + `${i.path != null ? `[${i.path}] ` : ""}${i.has} doesn't match ${i.expected}. ${i.message}`);
    }
    return t.join(`
`);
  }
}
const Ai = (e, t) => e === t ? !0 : e == null || t == null || e.constructor !== t.constructor ? !1 : e[Ne] ? Xr(e, t) : ue(e) ? Ji(
  e,
  (n) => Xi(t, (i) => Ai(n, i))
) : ah(e) ? be(
  e,
  (n, i) => Ai(n, t[i])
) : !1;
class it {
  /**
   * @param {Schema<any>} other
   */
  extends(t) {
    let [n, i] = [
      /** @type {any} */
      this.shape,
      /** @type {any} */
      t.shape
    ];
    return (
      /** @type {typeof Schema<any>} */
      this.constructor._dilutes && ([i, n] = [n, i]), Ai(n, i)
    );
  }
  /**
   * Overwrite this when necessary. By default, we only check the `shape` property which every shape
   * should have.
   * @param {Schema<any>} other
   */
  equals(t) {
    return this.constructor === t.constructor && fn(this.shape, t.shape);
  }
  [Th]() {
    return !0;
  }
  /**
   * @param {object} other
   */
  [Ne](t) {
    return this.equals(
      /** @type {any} */
      t
    );
  }
  /**
   * Use `schema.validate(obj)` with a typed parameter that is already of typed to be an instance of
   * Schema. Validate will check the structure of the parameter and return true iff the instance
   * really is an instance of Schema.
   *
   * @param {T} o
   * @return {boolean}
   */
  validate(t) {
    return this.check(t);
  }
  /* c8 ignore start */
  /**
   * Similar to validate, but this method accepts untyped parameters.
   *
   * @param {any} _o
   * @param {ValidationError} [_err]
   * @return {_o is T}
   */
  check(t, n) {
    xt();
  }
  /* c8 ignore stop */
  /**
   * @type {Schema<T?>}
   */
  get nullable() {
    return xe(this, Fn);
  }
  /**
   * @type {$Optional<Schema<T>>}
   */
  get optional() {
    return new ro(
      /** @type {Schema<T>} */
      this
    );
  }
  /**
   * Cast a variable to a specific type. Returns the casted value, or throws an exception otherwise.
   * Use this if you know that the type is of a specific type and you just want to convince the type
   * system.
   *
   * **Do not rely on these error messages!**
   * Performs an assertion check only if not in a production environment.
   *
   * @template OO
   * @param {OO} o
   * @return {Extract<OO, T> extends never ? T : (OO extends Array<never> ? T : Extract<OO,T>)}
   */
  cast(t) {
    return Ws(t, this), /** @type {any} */
    t;
  }
  /**
   * EXPECTO PATRONUM!! 🪄
   * This function protects against type errors. Though it may not work in the real world.
   *
   * "After all this time?"
   * "Always." - Snape, talking about type safety
   *
   * Ensures that a variable is a a specific type. Returns the value, or throws an exception if the assertion check failed.
   * Use this if you know that the type is of a specific type and you just want to convince the type
   * system.
   *
   * Can be useful when defining lambdas: `s.lambda(s.$number, s.$void).expect((n) => n + 1)`
   *
   * **Do not rely on these error messages!**
   * Performs an assertion check if not in a production environment.
   *
   * @param {T} o
   * @return {o extends T ? T : never}
   */
  expect(t) {
    return Ws(t, this), t;
  }
}
// this.shape must not be defined on Schema. Otherwise typecheck on metatypes (e.g. $$object) won't work as expected anymore
/**
 * If true, the more things are added to the shape the more objects this schema will accept (e.g.
 * union). By default, the more objects are added, the the fewer objects this schema will accept.
 * @protected
 */
nn(it, "_dilutes", !1);
class os extends it {
  /**
   * @param {C} c
   * @param {((o:Instance<C>)=>boolean)|null} check
   */
  constructor(t, n) {
    super(), this.shape = t, this._c = n;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is C extends ((...args:any[]) => infer T) ? T : (C extends (new (...args:any[]) => any) ? InstanceType<C> : never)} o
   */
  check(t, n = void 0) {
    const i = (t == null ? void 0 : t.constructor) === this.shape && (this._c == null || this._c(t));
    return !i && (n == null || n.extend(null, this.shape.name, t == null ? void 0 : t.constructor.name, (t == null ? void 0 : t.constructor) !== this.shape ? "Constructor match failed" : "Check failed")), i;
  }
}
const K = (e, t = null) => new os(e, t);
K(os);
class ls extends it {
  /**
   * @param {(o:any) => boolean} check
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is any}
   */
  check(t, n) {
    const i = this.shape(t);
    return !i && (n == null || n.extend(null, "custom prop", t == null ? void 0 : t.constructor.name, "failed to check custom prop")), i;
  }
}
const J = (e) => new ls(e);
K(ls);
class $n extends it {
  /**
   * @param {Array<T>} literals
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   *
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is T}
   */
  check(t, n) {
    const i = this.shape.some((s) => s === t);
    return !i && (n == null || n.extend(null, this.shape.join(" | "), t.toString())), i;
  }
}
const zn = (...e) => new $n(e), io = K($n), Ah = (
  /** @type {any} */
  RegExp.escape || /** @type {(str:string) => string} */
  ((e) => e.replace(/[().|&,$^[\]]/g, (t) => "\\" + t))
), so = (e) => {
  if (me.check(e))
    return [Ah(e)];
  if (io.check(e))
    return (
      /** @type {Array<string|number>} */
      e.shape.map((t) => t + "")
    );
  if (go.check(e))
    return ["[+-]?\\d+.?\\d*"];
  if (mo.check(e))
    return [".*"];
  if (Cn.check(e))
    return e.shape.map(so).flat(1);
  lt();
};
class Ih extends it {
  /**
   * @param {T} shape
   */
  constructor(t) {
    super(), this.shape = t, this._r = new RegExp("^" + t.map(so).map((n) => `(${n.join("|")})`).join("") + "$");
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is CastStringTemplateArgsToTemplate<T>}
   */
  check(t, n) {
    const i = this._r.exec(t) != null;
    return !i && (n == null || n.extend(null, this._r.toString(), t.toString(), "String doesn't match string template.")), i;
  }
}
K(Ih);
const Oh = Symbol("optional");
class ro extends it {
  /**
   * @param {S} shape
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is (Unwrap<S>|undefined)}
   */
  check(t, n) {
    const i = t === void 0 || this.shape.check(t);
    return !i && (n == null || n.extend(null, "undefined (optional)", "()")), i;
  }
  get [Oh]() {
    return !0;
  }
}
const Mh = K(ro);
class Rh extends it {
  /**
   * @param {any} _o
   * @param {ValidationError} [err]
   * @return {_o is never}
   */
  check(t, n) {
    return n == null || n.extend(null, "never", typeof t), !1;
  }
}
K(Rh);
const Ln = class Ln extends it {
  /**
   * @param {S} shape
   * @param {boolean} partial
   */
  constructor(t, n = !1) {
    super(), this.shape = t, this._isPartial = n;
  }
  /**
   * @type {Schema<Partial<$ObjectToType<S>>>}
   */
  get partial() {
    return new Ln(this.shape, !0);
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is $ObjectToType<S>}
   */
  check(t, n) {
    return t == null ? (n == null || n.extend(null, "object", "null"), !1) : be(this.shape, (i, s) => {
      const r = this._isPartial && !is(t, s) || i.check(t[s], n);
      return !r && (n == null || n.extend(s.toString(), i.toString(), typeof t[s], "Object property does not match")), r;
    });
  }
};
nn(Ln, "_dilutes", !0);
let Sn = Ln;
const Nh = (e) => (
  /** @type {any} */
  new Sn(e)
), Lh = K(Sn), Uh = J((e) => e != null && (e.constructor === Object || e.constructor == null));
class oo extends it {
  /**
   * @param {Keys} keys
   * @param {Values} values
   */
  constructor(t, n) {
    super(), this.shape = {
      keys: t,
      values: n
    };
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is { [key in Unwrap<Keys>]: Unwrap<Values> }}
   */
  check(t, n) {
    return t != null && be(t, (i, s) => {
      const r = this.shape.keys.check(s, n);
      return !r && (n == null || n.extend(s + "", "Record", typeof t, r ? "Key doesn't match schema" : "Value doesn't match value")), r && this.shape.values.check(i, n);
    });
  }
}
const lo = (e, t) => new oo(e, t), Ph = K(oo);
class co extends it {
  /**
   * @param {S} shape
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is { [K in keyof S]: S[K] extends Schema<infer Type> ? Type : never }}
   */
  check(t, n) {
    return t != null && be(this.shape, (i, s) => {
      const r = (
        /** @type {Schema<any>} */
        i.check(t[s], n)
      );
      return !r && (n == null || n.extend(s.toString(), "Tuple", typeof i)), r;
    });
  }
}
const Bh = (...e) => new co(e);
K(co);
class ao extends it {
  /**
   * @param {Array<S>} v
   */
  constructor(t) {
    super(), this.shape = t.length === 1 ? t[0] : new Vn(t);
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is Array<S extends Schema<infer T> ? T : never>} o
   */
  check(t, n) {
    const i = ue(t) && Ji(t, (s) => this.shape.check(s));
    return !i && (n == null || n.extend(null, "Array", "")), i;
  }
}
const ho = (...e) => new ao(e), $h = K(ao), zh = J((e) => ue(e));
class uo extends it {
  /**
   * @param {new (...args:any) => T} constructor
   * @param {((o:T) => boolean)|null} check
   */
  constructor(t, n) {
    super(), this.shape = t, this._c = n;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is T}
   */
  check(t, n) {
    const i = t instanceof this.shape && (this._c == null || this._c(t));
    return !i && (n == null || n.extend(null, this.shape.name, t == null ? void 0 : t.constructor.name)), i;
  }
}
const Vh = (e, t = null) => new uo(e, t);
K(uo);
const Hh = Vh(it);
class Fh extends it {
  /**
   * @param {Args} args
   */
  constructor(t) {
    super(), this.len = t.length - 1, this.args = Bh(...t.slice(-1)), this.res = t[this.len];
  }
  /**
   * @param {any} f
   * @param {ValidationError} err
   * @return {f is _LArgsToLambdaDef<Args>}
   */
  check(t, n) {
    const i = t.constructor === Function && t.length <= this.len;
    return !i && (n == null || n.extend(null, "function", typeof t)), i;
  }
}
const Kh = K(Fh), jh = J((e) => typeof e == "function");
class Wh extends it {
  /**
   * @param {T} v
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is Intersect<UnwrapArray<T>>}
   */
  check(t, n) {
    const i = Ji(this.shape, (s) => s.check(t, n));
    return !i && (n == null || n.extend(null, "Intersectinon", typeof t)), i;
  }
}
K(Wh, (e) => e.shape.length > 0);
class Vn extends it {
  /**
   * @param {Array<Schema<S>>} v
   */
  constructor(t) {
    super(), this.shape = t;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is S}
   */
  check(t, n) {
    const i = Xi(this.shape, (s) => s.check(t, n));
    return n == null || n.extend(null, "Union", typeof t), i;
  }
}
nn(Vn, "_dilutes", !0);
const xe = (...e) => e.findIndex((t) => Cn.check(t)) >= 0 ? xe(...e.map((t) => Le(t)).map((t) => Cn.check(t) ? t.shape : [t]).flat(1)) : e.length === 1 ? e[0] : new Vn(e), Cn = (
  /** @type {Schema<$Union<any>>} */
  K(Vn)
), fo = () => !0, kn = J(fo), Gh = (
  /** @type {Schema<Schema<any>>} */
  K(ls, (e) => e.shape === fo)
), cs = J((e) => typeof e == "bigint"), Yh = (
  /** @type {Schema<Schema<BigInt>>} */
  J((e) => e === cs)
), po = J((e) => typeof e == "symbol");
J((e) => e === po);
const ce = J((e) => typeof e == "number"), go = (
  /** @type {Schema<Schema<number>>} */
  J((e) => e === ce)
), me = J((e) => typeof e == "string"), mo = (
  /** @type {Schema<Schema<string>>} */
  J((e) => e === me)
), Hn = J((e) => typeof e == "boolean"), qh = (
  /** @type {Schema<Schema<Boolean>>} */
  J((e) => e === Hn)
), wo = zn(void 0);
K($n, (e) => e.shape.length === 1 && e.shape[0] === void 0);
zn(void 0);
const Fn = zn(null), Jh = (
  /** @type {Schema<Schema<null>>} */
  K($n, (e) => e.shape.length === 1 && e.shape[0] === null)
);
K(Uint8Array);
K(os, (e) => e.shape === Uint8Array);
const Xh = xe(ce, me, Fn, wo, cs, Hn, po);
(() => {
  const e = (
    /** @type {$Array<$any>} */
    ho(kn)
  ), t = (
    /** @type {$Record<$string,$any>} */
    lo(me, kn)
  ), n = xe(ce, me, Fn, Hn, e, t);
  return e.shape = n, t.shape.values = n, n;
})();
const Le = (e) => {
  if (Hh.check(e))
    return (
      /** @type {any} */
      e
    );
  if (Uh.check(e)) {
    const t = {};
    for (const n in e)
      t[n] = Le(e[n]);
    return (
      /** @type {any} */
      Nh(t)
    );
  } else {
    if (zh.check(e))
      return (
        /** @type {any} */
        xe(...e.map(Le))
      );
    if (Xh.check(e))
      return (
        /** @type {any} */
        zn(e)
      );
    if (jh.check(e))
      return (
        /** @type {any} */
        K(
          /** @type {any} */
          e
        )
      );
  }
  lt();
}, Ws = wh ? () => {
} : (e, t) => {
  const n = new Dh();
  if (!t.check(e, n))
    throw Ot(`Expected value to be of type ${t.constructor.name}.
${n.toString()}`);
};
class Zh {
  /**
   * @param {Schema<State>} [$state]
   */
  constructor(t) {
    this.patterns = [], this.$state = t;
  }
  /**
   * @template P
   * @template R
   * @param {P} pattern
   * @param {(o:NoInfer<Unwrap<ReadSchema<P>>>,s:State)=>R} handler
   * @return {PatternMatcher<State,Patterns|Pattern<Unwrap<ReadSchema<P>>,R>>}
   */
  if(t, n) {
    return this.patterns.push({ if: Le(t), h: n }), this;
  }
  /**
   * @template R
   * @param {(o:any,s:State)=>R} h
   */
  else(t) {
    return this.if(kn, t);
  }
  /**
   * @return {State extends undefined
   *   ? <In extends Unwrap<Patterns['if']>>(o:In,state?:undefined)=>PatternMatchResult<Patterns,In>
   *   : <In extends Unwrap<Patterns['if']>>(o:In,state:State)=>PatternMatchResult<Patterns,In>}
   */
  done() {
    return (
      /** @type {any} */
      (t, n) => {
        for (let i = 0; i < this.patterns.length; i++) {
          const s = this.patterns[i];
          if (s.if.check(t))
            return s.h(t, n);
        }
        throw Ot("Unhandled pattern");
      }
    );
  }
}
const Qh = (e) => new Zh(
  /** @type {any} */
  e
), bo = (
  /** @type {any} */
  Qh(
    /** @type {Schema<prng.PRNG>} */
    kn
  ).if(go, (e, t) => li(t, Us, bn)).if(mo, (e, t) => _h(t)).if(qh, (e, t) => js(t)).if(Yh, (e, t) => BigInt(li(t, Us, bn))).if(Cn, (e, t) => re(t, ci(t, e.shape))).if(Lh, (e, t) => {
    const n = {};
    for (const i in e.shape) {
      let s = e.shape[i];
      if (Mh.check(s)) {
        if (js(t))
          continue;
        s = s.shape;
      }
      n[i] = bo(s, t);
    }
    return n;
  }).if($h, (e, t) => {
    const n = [], i = no(t, 0, 42);
    for (let s = 0; s < i; s++)
      n.push(re(t, e.shape));
    return n;
  }).if(io, (e, t) => ci(t, e.shape)).if(Jh, (e, t) => null).if(Kh, (e, t) => {
    const n = re(t, e.res);
    return () => n;
  }).if(Gh, (e, t) => re(t, ci(t, [
    ce,
    me,
    Fn,
    wo,
    cs,
    Hn,
    ho(ce),
    lo(xe("a", "b", "c"), ce)
  ]))).if(Ph, (e, t) => {
    const n = {}, i = li(t, 0, 3);
    for (let s = 0; s < i; s++) {
      const r = re(t, e.shape.keys), o = re(t, e.shape.values);
      n[r] = o;
    }
    return n;
  }).done()
), re = (e, t) => (
  /** @type {any} */
  bo(Le(t), e)
), Qe = (
  /** @type {Document} */
  typeof document < "u" ? document : {}
);
J((e) => e.nodeType === sd);
typeof DOMParser < "u" && new DOMParser();
J((e) => e.nodeType === ed);
J((e) => e.nodeType === nd);
const td = (e) => xa(e, (t, n) => `${n}:${t};`).join(""), ed = Qe.ELEMENT_NODE, nd = Qe.TEXT_NODE, id = Qe.DOCUMENT_NODE, sd = Qe.DOCUMENT_FRAGMENT_NODE;
J((e) => e.nodeType === id);
const rd = (e) => class {
  /**
   * @param {number} timeoutId
   */
  constructor(n) {
    this._ = n;
  }
  destroy() {
    e(this._);
  }
}, od = rd(clearTimeout), xo = (e, t) => new od(setTimeout(t, e)), Pt = Symbol, yo = Pt(), vo = Pt(), ld = Pt(), cd = Pt(), ad = Pt(), So = Pt(), hd = Pt(), as = Pt(), dd = Pt(), ud = (e) => {
  var s;
  e.length === 1 && ((s = e[0]) == null ? void 0 : s.constructor) === Function && (e = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  e[0]());
  const t = [], n = [];
  let i = 0;
  for (; i < e.length; i++) {
    const r = e[i];
    if (r === void 0)
      break;
    if (r.constructor === String || r.constructor === Number)
      t.push(r);
    else if (r.constructor === Object)
      break;
  }
  for (i > 0 && n.push(t.join("")); i < e.length; i++) {
    const r = e[i];
    r instanceof Symbol || n.push(r);
  }
  return n;
}, fd = {
  [yo]: Rt("font-weight", "bold"),
  [vo]: Rt("font-weight", "normal"),
  [ld]: Rt("color", "blue"),
  [ad]: Rt("color", "green"),
  [cd]: Rt("color", "grey"),
  [So]: Rt("color", "red"),
  [hd]: Rt("color", "purple"),
  [as]: Rt("color", "orange"),
  // not well supported in chrome when debugging node with inspector - TODO: deprecate
  [dd]: Rt("color", "black")
}, pd = (e) => {
  var o;
  e.length === 1 && ((o = e[0]) == null ? void 0 : o.constructor) === Function && (e = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  e[0]());
  const t = [], n = [], i = St();
  let s = [], r = 0;
  for (; r < e.length; r++) {
    const l = e[r], c = fd[l];
    if (c !== void 0)
      i.set(c.left, c.right);
    else {
      if (l === void 0)
        break;
      if (l.constructor === String || l.constructor === Number) {
        const a = td(i);
        r > 0 || a.length > 0 ? (t.push("%c" + l), n.push(a)) : t.push(l);
      } else
        break;
    }
  }
  for (r > 0 && (s = n, s.unshift(t.join(""))); r < e.length; r++) {
    const l = e[r];
    l instanceof Symbol || s.push(l);
  }
  return s;
}, Co = xh ? pd : ud, gd = (...e) => {
  console.log(...Co(e)), Eo.forEach((t) => t.print(e));
}, ko = (...e) => {
  console.warn(...Co(e)), e.unshift(as), Eo.forEach((t) => t.print(e));
}, Eo = Jt(), _o = (e) => ({
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return this;
  },
  // @ts-ignore
  next: e
}), md = (e, t) => _o(() => {
  let n;
  do
    n = e.next();
  while (!n.done && !t(n.value));
  return n;
}), ai = (e, t) => _o(() => {
  const { done: n, value: i } = e.next();
  return { done: n, value: n ? void 0 : t(i) };
});
class Kn {
  /**
   * @param {number} clock
   * @param {number} len
   */
  constructor(t, n) {
    this.clock = t, this.len = n;
  }
}
class ye {
  constructor() {
    this.clients = /* @__PURE__ */ new Map();
  }
}
const Ft = (e, t, n) => t.clients.forEach((i, s) => {
  const r = (
    /** @type {Array<GC|Item>} */
    e.doc.store.clients.get(s)
  );
  if (r != null) {
    const o = r[r.length - 1], l = o.id.clock + o.length;
    for (let c = 0, a = i[c]; c < i.length && a.clock < l; a = i[++c])
      No(e, r, a.clock, a.len, n);
  }
}), wd = (e, t) => {
  let n = 0, i = e.length - 1;
  for (; n <= i; ) {
    const s = Ct((n + i) / 2), r = e[s], o = r.clock;
    if (o <= t) {
      if (t < o + r.len)
        return s;
      n = s + 1;
    } else
      i = s - 1;
  }
  return null;
}, ve = (e, t) => {
  const n = e.clients.get(t.client);
  return n !== void 0 && wd(n, t.clock) !== null;
}, hs = (e) => {
  e.clients.forEach((t) => {
    t.sort((s, r) => s.clock - r.clock);
    let n, i;
    for (n = 1, i = 1; n < t.length; n++) {
      const s = t[i - 1], r = t[n];
      s.clock + s.len >= r.clock ? t[i - 1] = new Kn(s.clock, Ht(s.len, r.clock + r.len - s.clock)) : (i < n && (t[i] = r), i++);
    }
    t.length = i;
  });
}, Ii = (e) => {
  const t = new ye();
  for (let n = 0; n < e.length; n++)
    e[n].clients.forEach((i, s) => {
      if (!t.clients.has(s)) {
        const r = i.slice();
        for (let o = n + 1; o < e.length; o++)
          va(r, e[o].clients.get(s) || []);
        t.clients.set(s, r);
      }
    });
  return hs(t), t;
}, Ue = (e, t, n, i) => {
  Ut(e.clients, t, () => (
    /** @type {Array<DeleteItem>} */
    []
  )).push(new Kn(n, i));
}, To = () => new ye(), bd = (e) => {
  const t = To();
  return e.clients.forEach((n, i) => {
    const s = [];
    for (let r = 0; r < n.length; r++) {
      const o = n[r];
      if (o.deleted) {
        const l = o.id.clock;
        let c = o.length;
        if (r + 1 < n.length)
          for (let a = n[r + 1]; r + 1 < n.length && a.deleted; a = n[++r + 1])
            c += a.length;
        s.push(new Kn(l, c));
      }
    }
    s.length > 0 && t.clients.set(i, s);
  }), t;
}, ds = (e, t) => {
  A(e.restEncoder, t.clients.size), Xt(t.clients.entries()).sort((n, i) => i[0] - n[0]).forEach(([n, i]) => {
    e.resetDsCurVal(), A(e.restEncoder, n);
    const s = i.length;
    A(e.restEncoder, s);
    for (let r = 0; r < s; r++) {
      const o = i[r];
      e.writeDsClock(o.clock), e.writeDsLen(o.len);
    }
  });
}, xd = (e) => {
  const t = new ye(), n = P(e.restDecoder);
  for (let i = 0; i < n; i++) {
    e.resetDsCurVal();
    const s = P(e.restDecoder), r = P(e.restDecoder);
    if (r > 0) {
      const o = Ut(t.clients, s, () => (
        /** @type {Array<DeleteItem>} */
        []
      ));
      for (let l = 0; l < r; l++)
        o.push(new Kn(e.readDsClock(), e.readDsLen()));
    }
  }
  return t;
}, Gs = (e, t, n) => {
  const i = new ye(), s = P(e.restDecoder);
  for (let r = 0; r < s; r++) {
    e.resetDsCurVal();
    const o = P(e.restDecoder), l = P(e.restDecoder), c = n.clients.get(o) || [], a = V(n, o);
    for (let d = 0; d < l; d++) {
      const h = e.readDsClock(), u = h + e.readDsLen();
      if (h < a) {
        a < u && Ue(i, o, a, u - a);
        let f = kt(c, h), g = c[f];
        for (!g.deleted && g.id.clock < h && (c.splice(f + 1, 0, On(t, g, h - g.id.clock)), f++); f < c.length && (g = c[f++], g.id.clock < u); )
          g.deleted || (u < g.id.clock + g.length && c.splice(f, 0, On(t, g, u - g.id.clock)), g.delete(t));
      } else
        Ue(i, o, h, u - h);
    }
  }
  if (i.clients.size > 0) {
    const r = new jn();
    return A(r.restEncoder, 0), ds(r, i), r.toUint8Array();
  }
  return null;
}, Do = qr;
class ie extends Kr {
  /**
   * @param {DocOpts} opts configuration
   */
  constructor({ guid: t = sh(), collectionid: n = null, gc: i = !0, gcFilter: s = () => !0, meta: r = null, autoLoad: o = !1, shouldLoad: l = !0 } = {}) {
    super(), this.gc = i, this.gcFilter = s, this.clientID = Do(), this.guid = t, this.collectionid = n, this.share = /* @__PURE__ */ new Map(), this.store = new Mo(), this._transaction = null, this._transactionCleanups = [], this.subdocs = /* @__PURE__ */ new Set(), this._item = null, this.shouldLoad = l, this.autoLoad = o, this.meta = r, this.isLoaded = !1, this.isSynced = !1, this.isDestroyed = !1, this.whenLoaded = Fs((a) => {
      this.on("load", () => {
        this.isLoaded = !0, a(this);
      });
    });
    const c = () => Fs((a) => {
      const d = (h) => {
        (h === void 0 || h === !0) && (this.off("sync", d), a());
      };
      this.on("sync", d);
    });
    this.on("sync", (a) => {
      a === !1 && this.isSynced && (this.whenSynced = c()), this.isSynced = a === void 0 || a === !0, this.isSynced && !this.isLoaded && this.emit("load", [this]);
    }), this.whenSynced = c();
  }
  /**
   * Notify the parent document that you request to load data into this subdocument (if it is a subdocument).
   *
   * `load()` might be used in the future to request any provider to load the most current data.
   *
   * It is safe to call `load()` multiple times.
   */
  load() {
    const t = this._item;
    t !== null && !this.shouldLoad && L(
      /** @type {any} */
      t.parent.doc,
      (n) => {
        n.subdocsLoaded.add(this);
      },
      null,
      !0
    ), this.shouldLoad = !0;
  }
  getSubdocs() {
    return this.subdocs;
  }
  getSubdocGuids() {
    return new Set(Xt(this.subdocs).map((t) => t.guid));
  }
  /**
   * Changes that happen inside of a transaction are bundled. This means that
   * the observer fires _after_ the transaction is finished and that all changes
   * that happened inside of the transaction are sent as one message to the
   * other peers.
   *
   * @template T
   * @param {function(Transaction):T} f The function that should be executed as a transaction
   * @param {any} [origin] Origin of who started the transaction. Will be stored on transaction.origin
   * @return T
   *
   * @public
   */
  transact(t, n = null) {
    return L(this, t, n);
  }
  /**
   * Define a shared data type.
   *
   * Multiple calls of `ydoc.get(name, TypeConstructor)` yield the same result
   * and do not overwrite each other. I.e.
   * `ydoc.get(name, Y.Array) === ydoc.get(name, Y.Array)`
   *
   * After this method is called, the type is also available on `ydoc.share.get(name)`.
   *
   * *Best Practices:*
   * Define all types right after the Y.Doc instance is created and store them in a separate object.
   * Also use the typed methods `getText(name)`, `getArray(name)`, ..
   *
   * @template {typeof AbstractType<any>} Type
   * @example
   *   const ydoc = new Y.Doc(..)
   *   const appState = {
   *     document: ydoc.getText('document')
   *     comments: ydoc.getArray('comments')
   *   }
   *
   * @param {string} name
   * @param {Type} TypeConstructor The constructor of the type definition. E.g. Y.Text, Y.Array, Y.Map, ...
   * @return {InstanceType<Type>} The created type. Constructed with TypeConstructor
   *
   * @public
   */
  get(t, n = (
    /** @type {any} */
    q
  )) {
    const i = Ut(this.share, t, () => {
      const r = new n();
      return r._integrate(this, null), r;
    }), s = i.constructor;
    if (n !== q && s !== n)
      if (s === q) {
        const r = new n();
        r._map = i._map, i._map.forEach(
          /** @param {Item?} n */
          (o) => {
            for (; o !== null; o = o.left)
              o.parent = r;
          }
        ), r._start = i._start;
        for (let o = r._start; o !== null; o = o.right)
          o.parent = r;
        return r._length = i._length, this.share.set(t, r), r._integrate(this, null), /** @type {InstanceType<Type>} */
        r;
      } else
        throw new Error(`Type with the name ${t} has already been defined with a different constructor`);
    return (
      /** @type {InstanceType<Type>} */
      i
    );
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YArray<T>}
   *
   * @public
   */
  getArray(t = "") {
    return (
      /** @type {YArray<T>} */
      this.get(t, de)
    );
  }
  /**
   * @param {string} [name]
   * @return {YText}
   *
   * @public
   */
  getText(t = "") {
    return this.get(t, Kt);
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YMap<T>}
   *
   * @public
   */
  getMap(t = "") {
    return (
      /** @type {YMap<T>} */
      this.get(t, we)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlElement}
   *
   * @public
   */
  getXmlElement(t = "") {
    return (
      /** @type {YXmlElement<{[key:string]:string}>} */
      this.get(t, rt)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlFragment}
   *
   * @public
   */
  getXmlFragment(t = "") {
    return this.get(t, Zt);
  }
  /**
   * Converts the entire document into a js object, recursively traversing each yjs type
   * Doesn't log types that have not been defined (using ydoc.getType(..)).
   *
   * @deprecated Do not use this method and rather call toJSON directly on the shared types.
   *
   * @return {Object<string, any>}
   */
  toJSON() {
    const t = {};
    return this.share.forEach((n, i) => {
      t[i] = n.toJSON();
    }), t;
  }
  /**
   * Emit `destroy` event and unregister all event handlers.
   */
  destroy() {
    this.isDestroyed = !0, Xt(this.subdocs).forEach((n) => n.destroy());
    const t = this._item;
    if (t !== null) {
      this._item = null;
      const n = (
        /** @type {ContentDoc} */
        t.content
      );
      n.doc = new ie({ guid: this.guid, ...n.opts, shouldLoad: !1 }), n.doc._item = t, L(
        /** @type {any} */
        t.parent.doc,
        (i) => {
          const s = n.doc;
          t.deleted || i.subdocsAdded.add(s), i.subdocsRemoved.add(this);
        },
        null,
        !0
      );
    }
    this.emit("destroyed", [!0]), this.emit("destroy", [this]), super.destroy();
  }
}
class yd {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(t) {
    this.dsCurrVal = 0, this.restDecoder = t;
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @return {number}
   */
  readDsClock() {
    return this.dsCurrVal += P(this.restDecoder), this.dsCurrVal;
  }
  /**
   * @return {number}
   */
  readDsLen() {
    const t = P(this.restDecoder) + 1;
    return this.dsCurrVal += t, t;
  }
}
class En extends yd {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(t) {
    super(t), this.keys = [], P(t), this.keyClockDecoder = new oi(mt(t)), this.clientDecoder = new un(mt(t)), this.leftClockDecoder = new oi(mt(t)), this.rightClockDecoder = new oi(mt(t)), this.infoDecoder = new Hs(mt(t), Re), this.stringDecoder = new Qa(mt(t)), this.parentInfoDecoder = new Hs(mt(t), Re), this.typeRefDecoder = new un(mt(t)), this.lenDecoder = new un(mt(t));
  }
  /**
   * @return {ID}
   */
  readLeftID() {
    return new ae(this.clientDecoder.read(), this.leftClockDecoder.read());
  }
  /**
   * @return {ID}
   */
  readRightID() {
    return new ae(this.clientDecoder.read(), this.rightClockDecoder.read());
  }
  /**
   * Read the next client id.
   * Use this in favor of readID whenever possible to reduce the number of objects created.
   */
  readClient() {
    return this.clientDecoder.read();
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readInfo() {
    return (
      /** @type {number} */
      this.infoDecoder.read()
    );
  }
  /**
   * @return {string}
   */
  readString() {
    return this.stringDecoder.read();
  }
  /**
   * @return {boolean}
   */
  readParentInfo() {
    return this.parentInfoDecoder.read() === 1;
  }
  /**
   * @return {number} An unsigned 8-bit integer
   */
  readTypeRef() {
    return this.typeRefDecoder.read();
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @return {number}
   */
  readLen() {
    return this.lenDecoder.read();
  }
  /**
   * @return {any}
   */
  readAny() {
    return xn(this.restDecoder);
  }
  /**
   * @return {Uint8Array}
   */
  readBuf() {
    return mt(this.restDecoder);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @return {any}
   */
  readJSON() {
    return xn(this.restDecoder);
  }
  /**
   * @return {string}
   */
  readKey() {
    const t = this.keyClockDecoder.read();
    if (t < this.keys.length)
      return this.keys[t];
    {
      const n = this.stringDecoder.read();
      return this.keys.push(n), n;
    }
  }
}
class vd {
  constructor() {
    this.restEncoder = Ze();
  }
  toUint8Array() {
    return vt(this.restEncoder);
  }
  resetDsCurVal() {
  }
  /**
   * @param {number} clock
   */
  writeDsClock(t) {
    A(this.restEncoder, t);
  }
  /**
   * @param {number} len
   */
  writeDsLen(t) {
    A(this.restEncoder, t);
  }
}
class Sd extends vd {
  /**
   * @param {ID} id
   */
  writeLeftID(t) {
    A(this.restEncoder, t.client), A(this.restEncoder, t.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(t) {
    A(this.restEncoder, t.client), A(this.restEncoder, t.clock);
  }
  /**
   * Use writeClient and writeClock instead of writeID if possible.
   * @param {number} client
   */
  writeClient(t) {
    A(this.restEncoder, t);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(t) {
    Ei(this.restEncoder, t);
  }
  /**
   * @param {string} s
   */
  writeString(t) {
    le(this.restEncoder, t);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(t) {
    A(this.restEncoder, t ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(t) {
    A(this.restEncoder, t);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(t) {
    A(this.restEncoder, t);
  }
  /**
   * @param {any} any
   */
  writeAny(t) {
    pe(this.restEncoder, t);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(t) {
    dt(this.restEncoder, t);
  }
  /**
   * @param {any} embed
   */
  writeJSON(t) {
    le(this.restEncoder, JSON.stringify(t));
  }
  /**
   * @param {string} key
   */
  writeKey(t) {
    le(this.restEncoder, t);
  }
}
class Cd {
  constructor() {
    this.restEncoder = Ze(), this.dsCurrVal = 0;
  }
  toUint8Array() {
    return vt(this.restEncoder);
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @param {number} clock
   */
  writeDsClock(t) {
    const n = t - this.dsCurrVal;
    this.dsCurrVal = t, A(this.restEncoder, n);
  }
  /**
   * @param {number} len
   */
  writeDsLen(t) {
    t === 0 && lt(), A(this.restEncoder, t - 1), this.dsCurrVal += t;
  }
}
class jn extends Cd {
  constructor() {
    super(), this.keyMap = /* @__PURE__ */ new Map(), this.keyClock = 0, this.keyClockEncoder = new ri(), this.clientEncoder = new dn(), this.leftClockEncoder = new ri(), this.rightClockEncoder = new ri(), this.infoEncoder = new $s(Ei), this.stringEncoder = new Ka(), this.parentInfoEncoder = new $s(Ei), this.typeRefEncoder = new dn(), this.lenEncoder = new dn();
  }
  toUint8Array() {
    const t = Ze();
    return A(t, 0), dt(t, this.keyClockEncoder.toUint8Array()), dt(t, this.clientEncoder.toUint8Array()), dt(t, this.leftClockEncoder.toUint8Array()), dt(t, this.rightClockEncoder.toUint8Array()), dt(t, vt(this.infoEncoder)), dt(t, this.stringEncoder.toUint8Array()), dt(t, vt(this.parentInfoEncoder)), dt(t, this.typeRefEncoder.toUint8Array()), dt(t, this.lenEncoder.toUint8Array()), Pn(t, vt(this.restEncoder)), vt(t);
  }
  /**
   * @param {ID} id
   */
  writeLeftID(t) {
    this.clientEncoder.write(t.client), this.leftClockEncoder.write(t.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(t) {
    this.clientEncoder.write(t.client), this.rightClockEncoder.write(t.clock);
  }
  /**
   * @param {number} client
   */
  writeClient(t) {
    this.clientEncoder.write(t);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(t) {
    this.infoEncoder.write(t);
  }
  /**
   * @param {string} s
   */
  writeString(t) {
    this.stringEncoder.write(t);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(t) {
    this.parentInfoEncoder.write(t ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(t) {
    this.typeRefEncoder.write(t);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(t) {
    this.lenEncoder.write(t);
  }
  /**
   * @param {any} any
   */
  writeAny(t) {
    pe(this.restEncoder, t);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(t) {
    dt(this.restEncoder, t);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @param {any} embed
   */
  writeJSON(t) {
    pe(this.restEncoder, t);
  }
  /**
   * Property keys are often reused. For example, in y-prosemirror the key `bold` might
   * occur very often. For a 3d application, the key `position` might occur very often.
   *
   * We cache these keys in a Map and refer to them via a unique number.
   *
   * @param {string} key
   */
  writeKey(t) {
    const n = this.keyMap.get(t);
    n === void 0 ? (this.keyClockEncoder.write(this.keyClock++), this.stringEncoder.write(t)) : this.keyClockEncoder.write(n);
  }
}
const kd = (e, t, n, i) => {
  i = Ht(i, t[0].id.clock);
  const s = kt(t, i);
  A(e.restEncoder, t.length - s), e.writeClient(n), A(e.restEncoder, i);
  const r = t[s];
  r.write(e, i - r.id.clock);
  for (let o = s + 1; o < t.length; o++)
    t[o].write(e, 0);
}, Ao = (e, t, n) => {
  const i = /* @__PURE__ */ new Map();
  n.forEach((s, r) => {
    V(t, r) > s && i.set(r, s);
  }), Wn(t).forEach((s, r) => {
    n.has(r) || i.set(r, 0);
  }), A(e.restEncoder, i.size), Xt(i.entries()).sort((s, r) => r[0] - s[0]).forEach(([s, r]) => {
    kd(
      e,
      /** @type {Array<GC|Item>} */
      t.clients.get(s),
      s,
      r
    );
  });
}, Ed = (e, t) => {
  const n = St(), i = P(e.restDecoder);
  for (let s = 0; s < i; s++) {
    const r = P(e.restDecoder), o = new Array(r), l = e.readClient();
    let c = P(e.restDecoder);
    n.set(l, { i: 0, refs: o });
    for (let a = 0; a < r; a++) {
      const d = e.readInfo();
      switch (Un & d) {
        case 0: {
          const h = e.readLen();
          o[a] = new wt(_(l, c), h), c += h;
          break;
        }
        case 10: {
          const h = P(e.restDecoder);
          o[a] = new yt(_(l, c), h), c += h;
          break;
        }
        default: {
          const h = (d & (Nt | ot)) === 0, u = new N(
            _(l, c),
            null,
            // left
            (d & ot) === ot ? e.readLeftID() : null,
            // origin
            null,
            // right
            (d & Nt) === Nt ? e.readRightID() : null,
            // right origin
            h ? e.readParentInfo() ? t.get(e.readString()) : e.readLeftID() : null,
            // parent
            h && (d & Oe) === Oe ? e.readString() : null,
            // parentSub
            nl(e, d)
            // item content
          );
          o[a] = u, c += u.length;
        }
      }
    }
  }
  return n;
}, _d = (e, t, n) => {
  const i = [];
  let s = Xt(n.keys()).sort((f, g) => f - g);
  if (s.length === 0)
    return null;
  const r = () => {
    if (s.length === 0)
      return null;
    let f = (
      /** @type {{i:number,refs:Array<GC|Item>}} */
      n.get(s[s.length - 1])
    );
    for (; f.refs.length === f.i; )
      if (s.pop(), s.length > 0)
        f = /** @type {{i:number,refs:Array<GC|Item>}} */
        n.get(s[s.length - 1]);
      else
        return null;
    return f;
  };
  let o = r();
  if (o === null)
    return null;
  const l = new Mo(), c = /* @__PURE__ */ new Map(), a = (f, g) => {
    const m = c.get(f);
    (m == null || m > g) && c.set(f, g);
  };
  let d = (
    /** @type {any} */
    o.refs[
      /** @type {any} */
      o.i++
    ]
  );
  const h = /* @__PURE__ */ new Map(), u = () => {
    for (const f of i) {
      const g = f.id.client, m = n.get(g);
      m ? (m.i--, l.clients.set(g, m.refs.slice(m.i)), n.delete(g), m.i = 0, m.refs = []) : l.clients.set(g, [f]), s = s.filter((w) => w !== g);
    }
    i.length = 0;
  };
  for (; ; ) {
    if (d.constructor !== yt) {
      const g = Ut(h, d.id.client, () => V(t, d.id.client)) - d.id.clock;
      if (g < 0)
        i.push(d), a(d.id.client, d.id.clock - 1), u();
      else {
        const m = d.getMissing(e, t);
        if (m !== null) {
          i.push(d);
          const w = n.get(
            /** @type {number} */
            m
          ) || { refs: [], i: 0 };
          if (w.refs.length === w.i)
            a(
              /** @type {number} */
              m,
              V(t, m)
            ), u();
          else {
            d = w.refs[w.i++];
            continue;
          }
        } else (g === 0 || g < d.length) && (d.integrate(e, g), h.set(d.id.client, d.id.clock + d.length));
      }
    }
    if (i.length > 0)
      d = /** @type {GC|Item} */
      i.pop();
    else if (o !== null && o.i < o.refs.length)
      d = /** @type {GC|Item} */
      o.refs[o.i++];
    else {
      if (o = r(), o === null)
        break;
      d = /** @type {GC|Item} */
      o.refs[o.i++];
    }
  }
  if (l.clients.size > 0) {
    const f = new jn();
    return Ao(f, l, /* @__PURE__ */ new Map()), A(f.restEncoder, 0), { missing: c, update: f.toUint8Array() };
  }
  return null;
}, Td = (e, t) => Ao(e, t.doc.store, t.beforeState), Dd = (e, t, n, i = new En(e)) => L(t, (s) => {
  s.local = !1;
  let r = !1;
  const o = s.doc, l = o.store, c = Ed(i, o), a = _d(s, l, c), d = l.pendingStructs;
  if (d) {
    for (const [u, f] of d.missing)
      if (f < V(l, u)) {
        r = !0;
        break;
      }
    if (a) {
      for (const [u, f] of a.missing) {
        const g = d.missing.get(u);
        (g == null || g > f) && d.missing.set(u, f);
      }
      d.update = nr([d.update, a.update]);
    }
  } else
    l.pendingStructs = a;
  const h = Gs(i, s, l);
  if (l.pendingDs) {
    const u = new En(ts(l.pendingDs));
    P(u.restDecoder);
    const f = Gs(u, s, l);
    h && f ? l.pendingDs = nr([h, f]) : l.pendingDs = h || f;
  } else
    l.pendingDs = h;
  if (r) {
    const u = (
      /** @type {{update: Uint8Array}} */
      l.pendingStructs.update
    );
    l.pendingStructs = null, Oi(s.doc, u);
  }
}, n, !1), Oi = (e, t, n, i = En) => {
  const s = ts(t);
  Dd(s, e, n, new i(s));
};
class Ad {
  constructor() {
    this.l = [];
  }
}
const Ys = () => new Ad(), qs = (e, t) => e.l.push(t), Js = (e, t) => {
  const n = e.l, i = n.length;
  e.l = n.filter((s) => t !== s), i === e.l.length && console.error("[yjs] Tried to remove event handler that doesn't exist.");
}, Io = (e, t, n) => ss(e.l, [t, n]);
class ae {
  /**
   * @param {number} client client id
   * @param {number} clock unique per client id, continuous number
   */
  constructor(t, n) {
    this.client = t, this.clock = n;
  }
}
const rn = (e, t) => e === t || e !== null && t !== null && e.client === t.client && e.clock === t.clock, _ = (e, t) => new ae(e, t), Pe = (e) => {
  for (const [t, n] of e.doc.share.entries())
    if (n === e)
      return t;
  throw lt();
}, Be = (e, t) => {
  for (; t !== null; ) {
    if (t.parent === e)
      return !0;
    t = /** @type {AbstractType<any>} */
    t.parent._item;
  }
  return !1;
};
class _n {
  /**
   * @param {ID|null} type
   * @param {string|null} tname
   * @param {ID|null} item
   * @param {number} assoc
   */
  constructor(t, n, i, s = 0) {
    this.type = t, this.tname = n, this.item = i, this.assoc = s;
  }
}
class Id {
  /**
   * @param {AbstractType<any>} type
   * @param {number} index
   * @param {number} [assoc]
   */
  constructor(t, n, i = 0) {
    this.type = t, this.index = n, this.assoc = i;
  }
}
const Od = (e, t, n = 0) => new Id(e, t, n), on = (e, t, n) => {
  let i = null, s = null;
  return e._item === null ? s = Pe(e) : i = _(e._item.id.client, e._item.id.clock), new _n(i, s, t, n);
}, hi = (e, t, n = 0) => {
  let i = e._start;
  if (n < 0) {
    if (t === 0)
      return on(e, null, n);
    t--;
  }
  for (; i !== null; ) {
    if (!i.deleted && i.countable) {
      if (i.length > t)
        return on(e, _(i.id.client, i.id.clock + t), n);
      t -= i.length;
    }
    if (i.right === null && n < 0)
      return on(e, i.lastId, n);
    i = i.right;
  }
  return on(e, null, n);
}, Md = (e, t) => {
  const n = he(e, t), i = t.clock - n.id.clock;
  return {
    item: n,
    diff: i
  };
}, Rd = (e, t, n = !0) => {
  const i = t.store, s = e.item, r = e.type, o = e.tname, l = e.assoc;
  let c = null, a = 0;
  if (s !== null) {
    if (V(i, s.client) <= s.clock)
      return null;
    const d = n ? Li(i, s) : Md(i, s), h = d.item;
    if (!(h instanceof N))
      return null;
    if (c = /** @type {AbstractType<any>} */
    h.parent, c._item === null || !c._item.deleted) {
      a = h.deleted || !h.countable ? 0 : d.diff + (l >= 0 ? 0 : 1);
      let u = h.left;
      for (; u !== null; )
        !u.deleted && u.countable && (a += u.length), u = u.left;
    }
  } else {
    if (o !== null)
      c = t.get(o);
    else if (r !== null) {
      if (V(i, r.client) <= r.clock)
        return null;
      const { item: d } = n ? Li(i, r) : { item: he(i, r) };
      if (d instanceof N && d.content instanceof _t)
        c = d.content.type;
      else
        return null;
    } else
      throw lt();
    l >= 0 ? a = c._length : a = 0;
  }
  return Od(c, a, e.assoc);
};
class us {
  /**
   * @param {DeleteSet} ds
   * @param {Map<number,number>} sv state map
   */
  constructor(t, n) {
    this.ds = t, this.sv = n;
  }
}
const Oo = (e, t) => new us(e, t), di = (e) => Oo(bd(e.store), Wn(e.store)), Gt = (e, t) => t === void 0 ? !e.deleted : t.sv.has(e.id.client) && (t.sv.get(e.id.client) || 0) > e.id.clock && !ve(t.ds, e.id), Mi = (e, t) => {
  const n = Ut(e.meta, Mi, Jt), i = e.doc.store;
  n.has(t) || (t.sv.forEach((s, r) => {
    s < V(i, r) && ht(e, _(r, s));
  }), Ft(e, t.ds, (s) => {
  }), n.add(t));
};
class Mo {
  constructor() {
    this.clients = /* @__PURE__ */ new Map(), this.pendingStructs = null, this.pendingDs = null;
  }
}
const Wn = (e) => {
  const t = /* @__PURE__ */ new Map();
  return e.clients.forEach((n, i) => {
    const s = n[n.length - 1];
    t.set(i, s.id.clock + s.length);
  }), t;
}, V = (e, t) => {
  const n = e.clients.get(t);
  if (n === void 0)
    return 0;
  const i = n[n.length - 1];
  return i.id.clock + i.length;
}, Ro = (e, t) => {
  let n = e.clients.get(t.id.client);
  if (n === void 0)
    n = [], e.clients.set(t.id.client, n);
  else {
    const i = n[n.length - 1];
    if (i.id.clock + i.length !== t.id.clock)
      throw lt();
  }
  n.push(t);
}, kt = (e, t) => {
  let n = 0, i = e.length - 1, s = e[i], r = s.id.clock;
  if (r === t)
    return i;
  let o = Ct(t / (r + s.length - 1) * i);
  for (; n <= i; ) {
    if (s = e[o], r = s.id.clock, r <= t) {
      if (t < r + s.length)
        return o;
      n = o + 1;
    } else
      i = o - 1;
    o = Ct((n + i) / 2);
  }
  throw lt();
}, Nd = (e, t) => {
  const n = e.clients.get(t.client);
  return n[kt(n, t.clock)];
}, he = (
  /** @type {function(StructStore,ID):Item} */
  Nd
), Ri = (e, t, n) => {
  const i = kt(t, n), s = t[i];
  return s.id.clock < n && s instanceof N ? (t.splice(i + 1, 0, On(e, s, n - s.id.clock)), i + 1) : i;
}, ht = (e, t) => {
  const n = (
    /** @type {Array<Item>} */
    e.doc.store.clients.get(t.client)
  );
  return n[Ri(e, n, t.clock)];
}, Xs = (e, t, n) => {
  const i = t.clients.get(n.client), s = kt(i, n.clock), r = i[s];
  return n.clock !== r.id.clock + r.length - 1 && r.constructor !== wt && i.splice(s + 1, 0, On(e, r, n.clock - r.id.clock + 1)), r;
}, Ld = (e, t, n) => {
  const i = (
    /** @type {Array<GC|Item>} */
    e.clients.get(t.id.client)
  );
  i[kt(i, t.id.clock)] = n;
}, No = (e, t, n, i, s) => {
  if (i === 0)
    return;
  const r = n + i;
  let o = Ri(e, t, n), l;
  do
    l = t[o++], r < l.id.clock + l.length && Ri(e, t, r), s(l);
  while (o < t.length && t[o].id.clock < r);
};
class Ud {
  /**
   * @param {Doc} doc
   * @param {any} origin
   * @param {boolean} local
   */
  constructor(t, n, i) {
    this.doc = t, this.deleteSet = new ye(), this.beforeState = Wn(t.store), this.afterState = /* @__PURE__ */ new Map(), this.changed = /* @__PURE__ */ new Map(), this.changedParentTypes = /* @__PURE__ */ new Map(), this._mergeStructs = [], this.origin = n, this.meta = /* @__PURE__ */ new Map(), this.local = i, this.subdocsAdded = /* @__PURE__ */ new Set(), this.subdocsRemoved = /* @__PURE__ */ new Set(), this.subdocsLoaded = /* @__PURE__ */ new Set(), this._needFormattingCleanup = !1;
  }
}
const Zs = (e, t) => t.deleteSet.clients.size === 0 && !ya(t.afterState, (n, i) => t.beforeState.get(i) !== n) ? !1 : (hs(t.deleteSet), Td(e, t), ds(e, t.deleteSet), !0), Qs = (e, t, n) => {
  const i = t._item;
  (i === null || i.id.clock < (e.beforeState.get(i.id.client) || 0) && !i.deleted) && Ut(e.changed, t, Jt).add(n);
}, pn = (e, t) => {
  let n = e[t], i = e[t - 1], s = t;
  for (; s > 0; n = i, i = e[--s - 1]) {
    if (i.deleted === n.deleted && i.constructor === n.constructor && i.mergeWith(n)) {
      n instanceof N && n.parentSub !== null && /** @type {AbstractType<any>} */
      n.parent._map.get(n.parentSub) === n && n.parent._map.set(
        n.parentSub,
        /** @type {Item} */
        i
      );
      continue;
    }
    break;
  }
  const r = t - s;
  return r && e.splice(t + 1 - r, r), r;
}, Pd = (e, t, n) => {
  for (const [i, s] of e.clients.entries()) {
    const r = (
      /** @type {Array<GC|Item>} */
      t.clients.get(i)
    );
    for (let o = s.length - 1; o >= 0; o--) {
      const l = s[o], c = l.clock + l.len;
      for (let a = kt(r, l.clock), d = r[a]; a < r.length && d.id.clock < c; d = r[++a]) {
        const h = r[a];
        if (l.clock + l.len <= h.id.clock)
          break;
        h instanceof N && h.deleted && !h.keep && n(h) && h.gc(t, !1);
      }
    }
  }
}, Bd = (e, t) => {
  e.clients.forEach((n, i) => {
    const s = (
      /** @type {Array<GC|Item>} */
      t.clients.get(i)
    );
    for (let r = n.length - 1; r >= 0; r--) {
      const o = n[r], l = fe(s.length - 1, 1 + kt(s, o.clock + o.len - 1));
      for (let c = l, a = s[c]; c > 0 && a.id.clock >= o.clock; a = s[c])
        c -= 1 + pn(s, c);
    }
  });
}, Lo = (e, t) => {
  if (t < e.length) {
    const n = e[t], i = n.doc, s = i.store, r = n.deleteSet, o = n._mergeStructs;
    try {
      hs(r), n.afterState = Wn(n.doc.store), i.emit("beforeObserverCalls", [n, i]);
      const l = [];
      n.changed.forEach(
        (c, a) => l.push(() => {
          (a._item === null || !a._item.deleted) && a._callObserver(n, c);
        })
      ), l.push(() => {
        n.changedParentTypes.forEach((c, a) => {
          a._dEH.l.length > 0 && (a._item === null || !a._item.deleted) && (c = c.filter(
            (d) => d.target._item === null || !d.target._item.deleted
          ), c.forEach((d) => {
            d.currentTarget = a, d._path = null;
          }), c.sort((d, h) => d.path.length - h.path.length), l.push(() => {
            Io(a._dEH, c, n);
          }));
        }), l.push(() => i.emit("afterTransaction", [n, i])), l.push(() => {
          n._needFormattingCleanup && su(n);
        });
      }), ss(l, []);
    } finally {
      i.gc && Pd(r, s, i.gcFilter), Bd(r, s), n.afterState.forEach((d, h) => {
        const u = n.beforeState.get(h) || 0;
        if (u !== d) {
          const f = (
            /** @type {Array<GC|Item>} */
            s.clients.get(h)
          ), g = Ht(kt(f, u), 1);
          for (let m = f.length - 1; m >= g; )
            m -= 1 + pn(f, m);
        }
      });
      for (let d = o.length - 1; d >= 0; d--) {
        const { client: h, clock: u } = o[d].id, f = (
          /** @type {Array<GC|Item>} */
          s.clients.get(h)
        ), g = kt(f, u);
        g + 1 < f.length && pn(f, g + 1) > 1 || g > 0 && pn(f, g);
      }
      if (!n.local && n.afterState.get(i.clientID) !== n.beforeState.get(i.clientID) && (gd(as, yo, "[yjs] ", vo, So, "Changed the client-id because another client seems to be using it."), i.clientID = Do()), i.emit("afterTransactionCleanup", [n, i]), i._observers.has("update")) {
        const d = new Sd();
        Zs(d, n) && i.emit("update", [d.toUint8Array(), n.origin, i, n]);
      }
      if (i._observers.has("updateV2")) {
        const d = new jn();
        Zs(d, n) && i.emit("updateV2", [d.toUint8Array(), n.origin, i, n]);
      }
      const { subdocsAdded: l, subdocsLoaded: c, subdocsRemoved: a } = n;
      (l.size > 0 || a.size > 0 || c.size > 0) && (l.forEach((d) => {
        d.clientID = i.clientID, d.collectionid == null && (d.collectionid = i.collectionid), i.subdocs.add(d);
      }), a.forEach((d) => i.subdocs.delete(d)), i.emit("subdocs", [{ loaded: c, added: l, removed: a }, i, n]), a.forEach((d) => d.destroy())), e.length <= t + 1 ? (i._transactionCleanups = [], i.emit("afterAllTransactions", [i, e])) : Lo(e, t + 1);
    }
  }
}, L = (e, t, n = null, i = !0) => {
  const s = e._transactionCleanups;
  let r = !1, o = null;
  e._transaction === null && (r = !0, e._transaction = new Ud(e, n, i), s.push(e._transaction), s.length === 1 && e.emit("beforeAllTransactions", [e]), e.emit("beforeTransaction", [e._transaction, e]));
  try {
    o = t(e._transaction);
  } finally {
    if (r) {
      const l = e._transaction === s[0];
      e._transaction = null, l && Lo(s, 0);
    }
  }
  return o;
};
class $d {
  /**
   * @param {DeleteSet} deletions
   * @param {DeleteSet} insertions
   */
  constructor(t, n) {
    this.insertions = n, this.deletions = t, this.meta = /* @__PURE__ */ new Map();
  }
}
const tr = (e, t, n) => {
  Ft(e, n.deletions, (i) => {
    i instanceof N && t.scope.some((s) => s === e.doc || Be(
      /** @type {AbstractType<any>} */
      s,
      i
    )) && ws(i, !1);
  });
}, er = (e, t, n) => {
  let i = null;
  const s = e.doc, r = e.scope;
  L(s, (l) => {
    for (; t.length > 0 && e.currStackItem === null; ) {
      const c = s.store, a = (
        /** @type {StackItem} */
        t.pop()
      ), d = /* @__PURE__ */ new Set(), h = [];
      let u = !1;
      Ft(l, a.insertions, (f) => {
        if (f instanceof N) {
          if (f.redone !== null) {
            let { item: g, diff: m } = Li(c, f.id);
            m > 0 && (g = ht(l, _(g.id.client, g.id.clock + m))), f = g;
          }
          !f.deleted && r.some((g) => g === l.doc || Be(
            /** @type {AbstractType<any>} */
            g,
            /** @type {Item} */
            f
          )) && h.push(f);
        }
      }), Ft(l, a.deletions, (f) => {
        f instanceof N && r.some((g) => g === l.doc || Be(
          /** @type {AbstractType<any>} */
          g,
          f
        )) && // Never redo structs in stackItem.insertions because they were created and deleted in the same capture interval.
        !ve(a.insertions, f.id) && d.add(f);
      }), d.forEach((f) => {
        u = el(l, f, d, a.insertions, e.ignoreRemoteMapChanges, e) !== null || u;
      });
      for (let f = h.length - 1; f >= 0; f--) {
        const g = h[f];
        e.deleteFilter(g) && (g.delete(l), u = !0);
      }
      e.currStackItem = u ? a : null;
    }
    l.changed.forEach((c, a) => {
      c.has(null) && a._searchMarker && (a._searchMarker.length = 0);
    }), i = l;
  }, e);
  const o = e.currStackItem;
  if (o != null) {
    const l = i.changedParentTypes;
    e.emit("stack-item-popped", [{ stackItem: o, type: n, changedParentTypes: l, origin: e }, e]), e.currStackItem = null;
  }
  return o;
};
class Uo extends Kr {
  /**
   * @param {Doc|AbstractType<any>|Array<AbstractType<any>>} typeScope Limits the scope of the UndoManager. If this is set to a ydoc instance, all changes on that ydoc will be undone. If set to a specific type, only changes on that type or its children will be undone. Also accepts an array of types.
   * @param {UndoManagerOptions} options
   */
  constructor(t, {
    captureTimeout: n = 500,
    captureTransaction: i = (c) => !0,
    deleteFilter: s = () => !0,
    trackedOrigins: r = /* @__PURE__ */ new Set([null]),
    ignoreRemoteMapChanges: o = !1,
    doc: l = (
      /** @type {Doc} */
      ue(t) ? t[0].doc : t instanceof ie ? t : t.doc
    )
  } = {}) {
    super(), this.scope = [], this.doc = l, this.addToScope(t), this.deleteFilter = s, r.add(this), this.trackedOrigins = r, this.captureTransaction = i, this.undoStack = [], this.redoStack = [], this.undoing = !1, this.redoing = !1, this.currStackItem = null, this.lastChange = 0, this.ignoreRemoteMapChanges = o, this.captureTimeout = n, this.afterTransactionHandler = (c) => {
      if (!this.captureTransaction(c) || !this.scope.some((w) => c.changedParentTypes.has(
        /** @type {AbstractType<any>} */
        w
      ) || w === this.doc) || !this.trackedOrigins.has(c.origin) && (!c.origin || !this.trackedOrigins.has(c.origin.constructor)))
        return;
      const a = this.undoing, d = this.redoing, h = a ? this.redoStack : this.undoStack;
      a ? this.stopCapturing() : d || this.clear(!1, !0);
      const u = new ye();
      c.afterState.forEach((w, b) => {
        const C = c.beforeState.get(b) || 0, y = w - C;
        y > 0 && Ue(u, b, C, y);
      });
      const f = rh();
      let g = !1;
      if (this.lastChange > 0 && f - this.lastChange < this.captureTimeout && h.length > 0 && !a && !d) {
        const w = h[h.length - 1];
        w.deletions = Ii([w.deletions, c.deleteSet]), w.insertions = Ii([w.insertions, u]);
      } else
        h.push(new $d(c.deleteSet, u)), g = !0;
      !a && !d && (this.lastChange = f), Ft(
        c,
        c.deleteSet,
        /** @param {Item|GC} item */
        (w) => {
          w instanceof N && this.scope.some((b) => b === c.doc || Be(
            /** @type {AbstractType<any>} */
            b,
            w
          )) && ws(w, !0);
        }
      );
      const m = [{ stackItem: h[h.length - 1], origin: c.origin, type: a ? "redo" : "undo", changedParentTypes: c.changedParentTypes }, this];
      g ? this.emit("stack-item-added", m) : this.emit("stack-item-updated", m);
    }, this.doc.on("afterTransaction", this.afterTransactionHandler), this.doc.on("destroy", () => {
      this.destroy();
    });
  }
  /**
   * Extend the scope.
   *
   * @param {Array<AbstractType<any> | Doc> | AbstractType<any> | Doc} ytypes
   */
  addToScope(t) {
    const n = new Set(this.scope);
    t = ue(t) ? t : [t], t.forEach((i) => {
      n.has(i) || (n.add(i), (i instanceof q ? i.doc !== this.doc : i !== this.doc) && ko("[yjs#509] Not same Y.Doc"), this.scope.push(i));
    });
  }
  /**
   * @param {any} origin
   */
  addTrackedOrigin(t) {
    this.trackedOrigins.add(t);
  }
  /**
   * @param {any} origin
   */
  removeTrackedOrigin(t) {
    this.trackedOrigins.delete(t);
  }
  clear(t = !0, n = !0) {
    (t && this.canUndo() || n && this.canRedo()) && this.doc.transact((i) => {
      t && (this.undoStack.forEach((s) => tr(i, this, s)), this.undoStack = []), n && (this.redoStack.forEach((s) => tr(i, this, s)), this.redoStack = []), this.emit("stack-cleared", [{ undoStackCleared: t, redoStackCleared: n }]);
    });
  }
  /**
   * UndoManager merges Undo-StackItem if they are created within time-gap
   * smaller than `options.captureTimeout`. Call `um.stopCapturing()` so that the next
   * StackItem won't be merged.
   *
   *
   * @example
   *     // without stopCapturing
   *     ytext.insert(0, 'a')
   *     ytext.insert(1, 'b')
   *     um.undo()
   *     ytext.toString() // => '' (note that 'ab' was removed)
   *     // with stopCapturing
   *     ytext.insert(0, 'a')
   *     um.stopCapturing()
   *     ytext.insert(0, 'b')
   *     um.undo()
   *     ytext.toString() // => 'a' (note that only 'b' was removed)
   *
   */
  stopCapturing() {
    this.lastChange = 0;
  }
  /**
   * Undo last changes on type.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  undo() {
    this.undoing = !0;
    let t;
    try {
      t = er(this, this.undoStack, "undo");
    } finally {
      this.undoing = !1;
    }
    return t;
  }
  /**
   * Redo last undo operation.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  redo() {
    this.redoing = !0;
    let t;
    try {
      t = er(this, this.redoStack, "redo");
    } finally {
      this.redoing = !1;
    }
    return t;
  }
  /**
   * Are undo steps available?
   *
   * @return {boolean} `true` if undo is possible
   */
  canUndo() {
    return this.undoStack.length > 0;
  }
  /**
   * Are redo steps available?
   *
   * @return {boolean} `true` if redo is possible
   */
  canRedo() {
    return this.redoStack.length > 0;
  }
  destroy() {
    this.trackedOrigins.delete(this), this.doc.off("afterTransaction", this.afterTransactionHandler), super.destroy();
  }
}
function* zd(e) {
  const t = P(e.restDecoder);
  for (let n = 0; n < t; n++) {
    const i = P(e.restDecoder), s = e.readClient();
    let r = P(e.restDecoder);
    for (let o = 0; o < i; o++) {
      const l = e.readInfo();
      if (l === 10) {
        const c = P(e.restDecoder);
        yield new yt(_(s, r), c), r += c;
      } else if ((Un & l) !== 0) {
        const c = (l & (Nt | ot)) === 0, a = new N(
          _(s, r),
          null,
          // left
          (l & ot) === ot ? e.readLeftID() : null,
          // origin
          null,
          // right
          (l & Nt) === Nt ? e.readRightID() : null,
          // right origin
          // @ts-ignore Force writing a string here.
          c ? e.readParentInfo() ? e.readString() : e.readLeftID() : null,
          // parent
          c && (l & Oe) === Oe ? e.readString() : null,
          // parentSub
          nl(e, l)
          // item content
        );
        yield a, r += a.length;
      } else {
        const c = e.readLen();
        yield new wt(_(s, r), c), r += c;
      }
    }
  }
}
class Vd {
  /**
   * @param {UpdateDecoderV1 | UpdateDecoderV2} decoder
   * @param {boolean} filterSkips
   */
  constructor(t, n) {
    this.gen = zd(t), this.curr = null, this.done = !1, this.filterSkips = n, this.next();
  }
  /**
   * @return {Item | GC | Skip |null}
   */
  next() {
    do
      this.curr = this.gen.next().value || null;
    while (this.filterSkips && this.curr !== null && this.curr.constructor === yt);
    return this.curr;
  }
}
class Hd {
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  constructor(t) {
    this.currClient = 0, this.startClock = 0, this.written = 0, this.encoder = t, this.clientStructs = [];
  }
}
const Fd = (e, t) => {
  if (e.constructor === wt) {
    const { client: n, clock: i } = e.id;
    return new wt(_(n, i + t), e.length - t);
  } else if (e.constructor === yt) {
    const { client: n, clock: i } = e.id;
    return new yt(_(n, i + t), e.length - t);
  } else {
    const n = (
      /** @type {Item} */
      e
    ), { client: i, clock: s } = n.id;
    return new N(
      _(i, s + t),
      null,
      _(i, s + t - 1),
      null,
      n.rightOrigin,
      n.parent,
      n.parentSub,
      n.content.splice(t)
    );
  }
}, nr = (e, t = En, n = jn) => {
  if (e.length === 1)
    return e[0];
  const i = e.map((d) => new t(ts(d)));
  let s = i.map((d) => new Vd(d, !0)), r = null;
  const o = new n(), l = new Hd(o);
  for (; s = s.filter((u) => u.curr !== null), s.sort(
    /** @type {function(any,any):number} */
    (u, f) => {
      if (u.curr.id.client === f.curr.id.client) {
        const g = u.curr.id.clock - f.curr.id.clock;
        return g === 0 ? u.curr.constructor === f.curr.constructor ? 0 : u.curr.constructor === yt ? 1 : -1 : g;
      } else
        return f.curr.id.client - u.curr.id.client;
    }
  ), s.length !== 0; ) {
    const d = s[0], h = (
      /** @type {Item | GC} */
      d.curr.id.client
    );
    if (r !== null) {
      let u = (
        /** @type {Item | GC | null} */
        d.curr
      ), f = !1;
      for (; u !== null && u.id.clock + u.length <= r.struct.id.clock + r.struct.length && u.id.client >= r.struct.id.client; )
        u = d.next(), f = !0;
      if (u === null || // current decoder is empty
      u.id.client !== h || // check whether there is another decoder that has has updates from `firstClient`
      f && u.id.clock > r.struct.id.clock + r.struct.length)
        continue;
      if (h !== r.struct.id.client)
        Te(l, r.struct, r.offset), r = { struct: u, offset: 0 }, d.next();
      else if (r.struct.id.clock + r.struct.length < u.id.clock)
        if (r.struct.constructor === yt)
          r.struct.length = u.id.clock + u.length - r.struct.id.clock;
        else {
          Te(l, r.struct, r.offset);
          const g = u.id.clock - r.struct.id.clock - r.struct.length;
          r = { struct: new yt(_(h, r.struct.id.clock + r.struct.length), g), offset: 0 };
        }
      else {
        const g = r.struct.id.clock + r.struct.length - u.id.clock;
        g > 0 && (r.struct.constructor === yt ? r.struct.length -= g : u = Fd(u, g)), r.struct.mergeWith(
          /** @type {any} */
          u
        ) || (Te(l, r.struct, r.offset), r = { struct: u, offset: 0 }, d.next());
      }
    } else
      r = { struct: (
        /** @type {Item | GC} */
        d.curr
      ), offset: 0 }, d.next();
    for (let u = d.curr; u !== null && u.id.client === h && u.id.clock === r.struct.id.clock + r.struct.length && u.constructor !== yt; u = d.next())
      Te(l, r.struct, r.offset), r = { struct: u, offset: 0 };
  }
  r !== null && (Te(l, r.struct, r.offset), r = null), Kd(l);
  const c = i.map((d) => xd(d)), a = Ii(c);
  return ds(o, a), o.toUint8Array();
}, Po = (e) => {
  e.written > 0 && (e.clientStructs.push({ written: e.written, restEncoder: vt(e.encoder.restEncoder) }), e.encoder.restEncoder = Ze(), e.written = 0);
}, Te = (e, t, n) => {
  e.written > 0 && e.currClient !== t.id.client && Po(e), e.written === 0 && (e.currClient = t.id.client, e.encoder.writeClient(t.id.client), A(e.encoder.restEncoder, t.id.clock + n)), t.write(e.encoder, n), e.written++;
}, Kd = (e) => {
  Po(e);
  const t = e.encoder.restEncoder;
  A(t, e.clientStructs.length);
  for (let n = 0; n < e.clientStructs.length; n++) {
    const i = e.clientStructs[n];
    A(t, i.written), Pn(t, i.restEncoder);
  }
}, ir = "You must not compute changes after the event-handler fired.";
class Gn {
  /**
   * @param {T} target The changed type.
   * @param {Transaction} transaction
   */
  constructor(t, n) {
    this.target = t, this.currentTarget = t, this.transaction = n, this._changes = null, this._keys = null, this._delta = null, this._path = null;
  }
  /**
   * Computes the path from `y` to the changed type.
   *
   * @todo v14 should standardize on path: Array<{parent, index}> because that is easier to work with.
   *
   * The following property holds:
   * @example
   *   let type = y
   *   event.path.forEach(dir => {
   *     type = type.get(dir)
   *   })
   *   type === event.target // => true
   */
  get path() {
    return this._path || (this._path = jd(this.currentTarget, this.target));
  }
  /**
   * Check if a struct is deleted by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  deletes(t) {
    return ve(this.transaction.deleteSet, t.id);
  }
  /**
   * @type {Map<string, { action: 'add' | 'update' | 'delete', oldValue: any }>}
   */
  get keys() {
    if (this._keys === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw Ot(ir);
      const t = /* @__PURE__ */ new Map(), n = this.target;
      /** @type Set<string|null> */
      this.transaction.changed.get(n).forEach((s) => {
        if (s !== null) {
          const r = (
            /** @type {Item} */
            n._map.get(s)
          );
          let o, l;
          if (this.adds(r)) {
            let c = r.left;
            for (; c !== null && this.adds(c); )
              c = c.left;
            if (this.deletes(r))
              if (c !== null && this.deletes(c))
                o = "delete", l = ni(c.content.getContent());
              else
                return;
            else
              c !== null && this.deletes(c) ? (o = "update", l = ni(c.content.getContent())) : (o = "add", l = void 0);
          } else if (this.deletes(r))
            o = "delete", l = ni(
              /** @type {Item} */
              r.content.getContent()
            );
          else
            return;
          t.set(s, { action: o, oldValue: l });
        }
      }), this._keys = t;
    }
    return this._keys;
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {Array<{insert?: string | Array<any> | object | AbstractType<any>, retain?: number, delete?: number, attributes?: Object<string, any>}>}
   */
  get delta() {
    return this.changes.delta;
  }
  /**
   * Check if a struct is added by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  adds(t) {
    return t.id.clock >= (this.transaction.beforeState.get(t.id.client) || 0);
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    let t = this._changes;
    if (t === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw Ot(ir);
      const n = this.target, i = Jt(), s = Jt(), r = [];
      if (t = {
        added: i,
        deleted: s,
        delta: r,
        keys: this.keys
      }, /** @type Set<string|null> */
      this.transaction.changed.get(n).has(null)) {
        let l = null;
        const c = () => {
          l && r.push(l);
        };
        for (let a = n._start; a !== null; a = a.right)
          a.deleted ? this.deletes(a) && !this.adds(a) && ((l === null || l.delete === void 0) && (c(), l = { delete: 0 }), l.delete += a.length, s.add(a)) : this.adds(a) ? ((l === null || l.insert === void 0) && (c(), l = { insert: [] }), l.insert = l.insert.concat(a.content.getContent()), i.add(a)) : ((l === null || l.retain === void 0) && (c(), l = { retain: 0 }), l.retain += a.length);
        l !== null && l.retain === void 0 && c();
      }
      this._changes = t;
    }
    return (
      /** @type {any} */
      t
    );
  }
}
const jd = (e, t) => {
  const n = [];
  for (; t._item !== null && t !== e; ) {
    if (t._item.parentSub !== null)
      n.unshift(t._item.parentSub);
    else {
      let i = 0, s = (
        /** @type {AbstractType<any>} */
        t._item.parent._start
      );
      for (; s !== t._item && s !== null; )
        !s.deleted && s.countable && (i += s.length), s = s.right;
      n.unshift(i);
    }
    t = /** @type {AbstractType<any>} */
    t._item.parent;
  }
  return n;
}, tt = () => {
  ko("Invalid access: Add Yjs type to a document before reading data.");
}, Bo = 80;
let fs = 0;
class Wd {
  /**
   * @param {Item} p
   * @param {number} index
   */
  constructor(t, n) {
    t.marker = !0, this.p = t, this.index = n, this.timestamp = fs++;
  }
}
const Gd = (e) => {
  e.timestamp = fs++;
}, $o = (e, t, n) => {
  e.p.marker = !1, e.p = t, t.marker = !0, e.index = n, e.timestamp = fs++;
}, Yd = (e, t, n) => {
  if (e.length >= Bo) {
    const i = e.reduce((s, r) => s.timestamp < r.timestamp ? s : r);
    return $o(i, t, n), i;
  } else {
    const i = new Wd(t, n);
    return e.push(i), i;
  }
}, Yn = (e, t) => {
  if (e._start === null || t === 0 || e._searchMarker === null)
    return null;
  const n = e._searchMarker.length === 0 ? null : e._searchMarker.reduce((r, o) => hn(t - r.index) < hn(t - o.index) ? r : o);
  let i = e._start, s = 0;
  for (n !== null && (i = n.p, s = n.index, Gd(n)); i.right !== null && s < t; ) {
    if (!i.deleted && i.countable) {
      if (t < s + i.length)
        break;
      s += i.length;
    }
    i = i.right;
  }
  for (; i.left !== null && s > t; )
    i = i.left, !i.deleted && i.countable && (s -= i.length);
  for (; i.left !== null && i.left.id.client === i.id.client && i.left.id.clock + i.left.length === i.id.clock; )
    i = i.left, !i.deleted && i.countable && (s -= i.length);
  return n !== null && hn(n.index - s) < /** @type {YText|YArray<any>} */
  i.parent.length / Bo ? ($o(n, i, s), n) : Yd(e._searchMarker, i, s);
}, $e = (e, t, n) => {
  for (let i = e.length - 1; i >= 0; i--) {
    const s = e[i];
    if (n > 0) {
      let r = s.p;
      for (r.marker = !1; r && (r.deleted || !r.countable); )
        r = r.left, r && !r.deleted && r.countable && (s.index -= r.length);
      if (r === null || r.marker === !0) {
        e.splice(i, 1);
        continue;
      }
      s.p = r, r.marker = !0;
    }
    (t < s.index || n > 0 && t === s.index) && (s.index = Ht(t, s.index + n));
  }
}, qn = (e, t, n) => {
  const i = e, s = t.changedParentTypes;
  for (; Ut(s, e, () => []).push(n), e._item !== null; )
    e = /** @type {AbstractType<any>} */
    e._item.parent;
  Io(i._eH, n, t);
};
class q {
  constructor() {
    this._item = null, this._map = /* @__PURE__ */ new Map(), this._start = null, this.doc = null, this._length = 0, this._eH = Ys(), this._dEH = Ys(), this._searchMarker = null;
  }
  /**
   * @return {AbstractType<any>|null}
   */
  get parent() {
    return this._item ? (
      /** @type {AbstractType<any>} */
      this._item.parent
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item|null} item
   */
  _integrate(t, n) {
    this.doc = t, this._item = n;
  }
  /**
   * @return {AbstractType<EventType>}
   */
  _copy() {
    throw xt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {AbstractType<EventType>}
   */
  clone() {
    throw xt();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} _encoder
   */
  _write(t) {
  }
  /**
   * The first non-deleted item
   */
  get _first() {
    let t = this._start;
    for (; t !== null && t.deleted; )
      t = t.right;
    return t;
  }
  /**
   * Creates YEvent and calls all type observers.
   * Must be implemented by each type.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} _parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, n) {
    !t.local && this._searchMarker && (this._searchMarker.length = 0);
  }
  /**
   * Observe all events that are created on this type.
   *
   * @param {function(EventType, Transaction):void} f Observer function
   */
  observe(t) {
    qs(this._eH, t);
  }
  /**
   * Observe all events that are created by this type and its children.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  observeDeep(t) {
    qs(this._dEH, t);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(EventType,Transaction):void} f Observer function
   */
  unobserve(t) {
    Js(this._eH, t);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  unobserveDeep(t) {
    Js(this._dEH, t);
  }
  /**
   * @abstract
   * @return {any}
   */
  toJSON() {
  }
}
const zo = (e, t, n) => {
  e.doc ?? tt(), t < 0 && (t = e._length + t), n < 0 && (n = e._length + n);
  let i = n - t;
  const s = [];
  let r = e._start;
  for (; r !== null && i > 0; ) {
    if (r.countable && !r.deleted) {
      const o = r.content.getContent();
      if (o.length <= t)
        t -= o.length;
      else {
        for (let l = t; l < o.length && i > 0; l++)
          s.push(o[l]), i--;
        t = 0;
      }
    }
    r = r.right;
  }
  return s;
}, Vo = (e) => {
  e.doc ?? tt();
  const t = [];
  let n = e._start;
  for (; n !== null; ) {
    if (n.countable && !n.deleted) {
      const i = n.content.getContent();
      for (let s = 0; s < i.length; s++)
        t.push(i[s]);
    }
    n = n.right;
  }
  return t;
}, Ho = (e, t) => {
  const n = [];
  let i = e._start;
  for (; i !== null; ) {
    if (i.countable && Gt(i, t)) {
      const s = i.content.getContent();
      for (let r = 0; r < s.length; r++)
        n.push(s[r]);
    }
    i = i.right;
  }
  return n;
}, ze = (e, t) => {
  let n = 0, i = e._start;
  for (e.doc ?? tt(); i !== null; ) {
    if (i.countable && !i.deleted) {
      const s = i.content.getContent();
      for (let r = 0; r < s.length; r++)
        t(s[r], n++, e);
    }
    i = i.right;
  }
}, Fo = (e, t) => {
  const n = [];
  return ze(e, (i, s) => {
    n.push(t(i, s, e));
  }), n;
}, qd = (e) => {
  let t = e._start, n = null, i = 0;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next: () => {
      if (n === null) {
        for (; t !== null && t.deleted; )
          t = t.right;
        if (t === null)
          return {
            done: !0,
            value: void 0
          };
        n = t.content.getContent(), i = 0, t = t.right;
      }
      const s = n[i++];
      return n.length <= i && (n = null), {
        done: !1,
        value: s
      };
    }
  };
}, Ko = (e, t) => {
  e.doc ?? tt();
  const n = Yn(e, t);
  let i = e._start;
  for (n !== null && (i = n.p, t -= n.index); i !== null; i = i.right)
    if (!i.deleted && i.countable) {
      if (t < i.length)
        return i.content.getContent()[t];
      t -= i.length;
    }
}, Tn = (e, t, n, i) => {
  let s = n;
  const r = e.doc, o = r.clientID, l = r.store, c = n === null ? t._start : n.right;
  let a = [];
  const d = () => {
    a.length > 0 && (s = new N(_(o, V(l, o)), s, s && s.lastId, c, c && c.id, t, null, new Qt(a)), s.integrate(e, 0), a = []);
  };
  i.forEach((h) => {
    if (h === null)
      a.push(h);
    else
      switch (h.constructor) {
        case Number:
        case Object:
        case Boolean:
        case Array:
        case String:
          a.push(h);
          break;
        default:
          switch (d(), h.constructor) {
            case Uint8Array:
            case ArrayBuffer:
              s = new N(_(o, V(l, o)), s, s && s.lastId, c, c && c.id, t, null, new tn(new Uint8Array(
                /** @type {Uint8Array} */
                h
              ))), s.integrate(e, 0);
              break;
            case ie:
              s = new N(_(o, V(l, o)), s, s && s.lastId, c, c && c.id, t, null, new en(
                /** @type {Doc} */
                h
              )), s.integrate(e, 0);
              break;
            default:
              if (h instanceof q)
                s = new N(_(o, V(l, o)), s, s && s.lastId, c, c && c.id, t, null, new _t(h)), s.integrate(e, 0);
              else
                throw new Error("Unexpected content type in insert operation");
          }
      }
  }), d();
}, jo = () => Ot("Length exceeded!"), Wo = (e, t, n, i) => {
  if (n > t._length)
    throw jo();
  if (n === 0)
    return t._searchMarker && $e(t._searchMarker, n, i.length), Tn(e, t, null, i);
  const s = n, r = Yn(t, n);
  let o = t._start;
  for (r !== null && (o = r.p, n -= r.index, n === 0 && (o = o.prev, n += o && o.countable && !o.deleted ? o.length : 0)); o !== null; o = o.right)
    if (!o.deleted && o.countable) {
      if (n <= o.length) {
        n < o.length && ht(e, _(o.id.client, o.id.clock + n));
        break;
      }
      n -= o.length;
    }
  return t._searchMarker && $e(t._searchMarker, s, i.length), Tn(e, t, o, i);
}, Jd = (e, t, n) => {
  let s = (t._searchMarker || []).reduce((r, o) => o.index > r.index ? o : r, { index: 0, p: t._start }).p;
  if (s)
    for (; s.right; )
      s = s.right;
  return Tn(e, t, s, n);
}, Go = (e, t, n, i) => {
  if (i === 0)
    return;
  const s = n, r = i, o = Yn(t, n);
  let l = t._start;
  for (o !== null && (l = o.p, n -= o.index); l !== null && n > 0; l = l.right)
    !l.deleted && l.countable && (n < l.length && ht(e, _(l.id.client, l.id.clock + n)), n -= l.length);
  for (; i > 0 && l !== null; )
    l.deleted || (i < l.length && ht(e, _(l.id.client, l.id.clock + i)), l.delete(e), i -= l.length), l = l.right;
  if (i > 0)
    throw jo();
  t._searchMarker && $e(
    t._searchMarker,
    s,
    -r + i
    /* in case we remove the above exception */
  );
}, Dn = (e, t, n) => {
  const i = t._map.get(n);
  i !== void 0 && i.delete(e);
}, ps = (e, t, n, i) => {
  const s = t._map.get(n) || null, r = e.doc, o = r.clientID;
  let l;
  if (i == null)
    l = new Qt([i]);
  else
    switch (i.constructor) {
      case Number:
      case Object:
      case Boolean:
      case Array:
      case String:
      case Date:
      case BigInt:
        l = new Qt([i]);
        break;
      case Uint8Array:
        l = new tn(
          /** @type {Uint8Array} */
          i
        );
        break;
      case ie:
        l = new en(
          /** @type {Doc} */
          i
        );
        break;
      default:
        if (i instanceof q)
          l = new _t(i);
        else
          throw new Error("Unexpected content type");
    }
  new N(_(o, V(r.store, o)), s, s && s.lastId, null, null, t, n, l).integrate(e, 0);
}, gs = (e, t) => {
  e.doc ?? tt();
  const n = e._map.get(t);
  return n !== void 0 && !n.deleted ? n.content.getContent()[n.length - 1] : void 0;
}, Yo = (e) => {
  const t = {};
  return e.doc ?? tt(), e._map.forEach((n, i) => {
    n.deleted || (t[i] = n.content.getContent()[n.length - 1]);
  }), t;
}, qo = (e, t) => {
  e.doc ?? tt();
  const n = e._map.get(t);
  return n !== void 0 && !n.deleted;
}, Xd = (e, t) => {
  const n = {};
  return e._map.forEach((i, s) => {
    let r = i;
    for (; r !== null && (!t.sv.has(r.id.client) || r.id.clock >= (t.sv.get(r.id.client) || 0)); )
      r = r.left;
    r !== null && Gt(r, t) && (n[s] = r.content.getContent()[r.length - 1]);
  }), n;
}, ln = (e) => (e.doc ?? tt(), md(
  e._map.entries(),
  /** @param {any} entry */
  (t) => !t[1].deleted
));
class Zd extends Gn {
}
class de extends q {
  constructor() {
    super(), this._prelimContent = [], this._searchMarker = [];
  }
  /**
   * Construct a new YArray containing the specified items.
   * @template {Object<string,any>|Array<any>|number|null|string|Uint8Array} T
   * @param {Array<T>} items
   * @return {YArray<T>}
   */
  static from(t) {
    const n = new de();
    return n.push(t), n;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, n) {
    super._integrate(t, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  /**
   * @return {YArray<T>}
   */
  _copy() {
    return new de();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YArray<T>}
   */
  clone() {
    const t = new de();
    return t.insert(0, this.toArray().map(
      (n) => n instanceof q ? (
        /** @type {typeof el} */
        n.clone()
      ) : n
    )), t;
  }
  get length() {
    return this.doc ?? tt(), this._length;
  }
  /**
   * Creates YArrayEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, n) {
    super._callObserver(t, n), qn(this, t, new Zd(this, t));
  }
  /**
   * Inserts new content at an index.
   *
   * Important: This function expects an array of content. Not just a content
   * object. The reason for this "weirdness" is that inserting several elements
   * is very efficient when it is done as a single operation.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  yarray.insert(0, ['a'])
   *  // Insert numbers 1, 2 at position 1
   *  yarray.insert(1, [1, 2])
   *
   * @param {number} index The index to insert content at.
   * @param {Array<T>} content The array of content
   */
  insert(t, n) {
    this.doc !== null ? L(this.doc, (i) => {
      Wo(
        i,
        this,
        t,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.splice(t, 0, ...n);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<T>} content Array of content to append.
   *
   * @todo Use the following implementation in all types.
   */
  push(t) {
    this.doc !== null ? L(this.doc, (n) => {
      Jd(
        n,
        this,
        /** @type {any} */
        t
      );
    }) : this._prelimContent.push(...t);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<T>} content Array of content to prepend.
   */
  unshift(t) {
    this.insert(0, t);
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} length The number of elements to remove. Defaults to 1.
   */
  delete(t, n = 1) {
    this.doc !== null ? L(this.doc, (i) => {
      Go(i, this, t, n);
    }) : this._prelimContent.splice(t, n);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {T}
   */
  get(t) {
    return Ko(this, t);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<T>}
   */
  toArray() {
    return Vo(this);
  }
  /**
   * Returns a portion of this YArray into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<T>}
   */
  slice(t = 0, n = this.length) {
    return zo(this, t, n);
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Array<any>}
   */
  toJSON() {
    return this.map((t) => t instanceof q ? t.toJSON() : t);
  }
  /**
   * Returns an Array with the result of calling a provided function on every
   * element of this YArray.
   *
   * @template M
   * @param {function(T,number,YArray<T>):M} f Function that produces an element of the new Array
   * @return {Array<M>} A new array with each element being the result of the
   *                 callback function
   */
  map(t) {
    return Fo(
      this,
      /** @type {any} */
      t
    );
  }
  /**
   * Executes a provided function once on every element of this YArray.
   *
   * @param {function(T,number,YArray<T>):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    ze(this, t);
  }
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return qd(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(Cu);
  }
}
const Qd = (e) => new de();
class tu extends Gn {
  /**
   * @param {YMap<T>} ymap The YArray that changed.
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed.
   */
  constructor(t, n, i) {
    super(t, n), this.keysChanged = i;
  }
}
class we extends q {
  /**
   *
   * @param {Iterable<readonly [string, any]>=} entries - an optional iterable to initialize the YMap
   */
  constructor(t) {
    super(), this._prelimContent = null, t === void 0 ? this._prelimContent = /* @__PURE__ */ new Map() : this._prelimContent = new Map(t);
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, n) {
    super._integrate(t, n), this._prelimContent.forEach((i, s) => {
      this.set(s, i);
    }), this._prelimContent = null;
  }
  /**
   * @return {YMap<MapType>}
   */
  _copy() {
    return new we();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YMap<MapType>}
   */
  clone() {
    const t = new we();
    return this.forEach((n, i) => {
      t.set(i, n instanceof q ? (
        /** @type {typeof value} */
        n.clone()
      ) : n);
    }), t;
  }
  /**
   * Creates YMapEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, n) {
    qn(this, t, new tu(this, t, n));
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Object<string,any>}
   */
  toJSON() {
    this.doc ?? tt();
    const t = {};
    return this._map.forEach((n, i) => {
      if (!n.deleted) {
        const s = n.content.getContent()[n.length - 1];
        t[i] = s instanceof q ? s.toJSON() : s;
      }
    }), t;
  }
  /**
   * Returns the size of the YMap (count of key/value pairs)
   *
   * @return {number}
   */
  get size() {
    return [...ln(this)].length;
  }
  /**
   * Returns the keys for each element in the YMap Type.
   *
   * @return {IterableIterator<string>}
   */
  keys() {
    return ai(
      ln(this),
      /** @param {any} v */
      (t) => t[0]
    );
  }
  /**
   * Returns the values for each element in the YMap Type.
   *
   * @return {IterableIterator<MapType>}
   */
  values() {
    return ai(
      ln(this),
      /** @param {any} v */
      (t) => t[1].content.getContent()[t[1].length - 1]
    );
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  entries() {
    return ai(
      ln(this),
      /** @param {any} v */
      (t) => (
        /** @type {any} */
        [t[0], t[1].content.getContent()[t[1].length - 1]]
      )
    );
  }
  /**
   * Executes a provided function on once on every key-value pair.
   *
   * @param {function(MapType,string,YMap<MapType>):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    this.doc ?? tt(), this._map.forEach((n, i) => {
      n.deleted || t(n.content.getContent()[n.length - 1], i, this);
    });
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * Remove a specified element from this YMap.
   *
   * @param {string} key The key of the element to remove.
   */
  delete(t) {
    this.doc !== null ? L(this.doc, (n) => {
      Dn(n, this, t);
    }) : this._prelimContent.delete(t);
  }
  /**
   * Adds or updates an element with a specified key and value.
   * @template {MapType} VAL
   *
   * @param {string} key The key of the element to add to this YMap
   * @param {VAL} value The value of the element to add
   * @return {VAL}
   */
  set(t, n) {
    return this.doc !== null ? L(this.doc, (i) => {
      ps(
        i,
        this,
        t,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.set(t, n), n;
  }
  /**
   * Returns a specified element from this YMap.
   *
   * @param {string} key
   * @return {MapType|undefined}
   */
  get(t) {
    return (
      /** @type {any} */
      gs(this, t)
    );
  }
  /**
   * Returns a boolean indicating whether the specified key exists or not.
   *
   * @param {string} key The key to test.
   * @return {boolean}
   */
  has(t) {
    return qo(this, t);
  }
  /**
   * Removes all elements from this YMap.
   */
  clear() {
    this.doc !== null ? L(this.doc, (t) => {
      this.forEach(function(n, i, s) {
        Dn(t, s, i);
      });
    }) : this._prelimContent.clear();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(ku);
  }
}
const eu = (e) => new we(), $t = (e, t) => e === t || typeof e == "object" && typeof t == "object" && e && t && fh(e, t);
class Ni {
  /**
   * @param {Item|null} left
   * @param {Item|null} right
   * @param {number} index
   * @param {Map<string,any>} currentAttributes
   */
  constructor(t, n, i, s) {
    this.left = t, this.right = n, this.index = i, this.currentAttributes = s;
  }
  /**
   * Only call this if you know that this.right is defined
   */
  forward() {
    switch (this.right === null && lt(), this.right.content.constructor) {
      case F:
        this.right.deleted || Se(
          this.currentAttributes,
          /** @type {ContentFormat} */
          this.right.content
        );
        break;
      default:
        this.right.deleted || (this.index += this.right.length);
        break;
    }
    this.left = this.right, this.right = this.right.right;
  }
}
const sr = (e, t, n) => {
  for (; t.right !== null && n > 0; ) {
    switch (t.right.content.constructor) {
      case F:
        t.right.deleted || Se(
          t.currentAttributes,
          /** @type {ContentFormat} */
          t.right.content
        );
        break;
      default:
        t.right.deleted || (n < t.right.length && ht(e, _(t.right.id.client, t.right.id.clock + n)), t.index += t.right.length, n -= t.right.length);
        break;
    }
    t.left = t.right, t.right = t.right.right;
  }
  return t;
}, cn = (e, t, n, i) => {
  const s = /* @__PURE__ */ new Map(), r = i ? Yn(t, n) : null;
  if (r) {
    const o = new Ni(r.p.left, r.p, r.index, s);
    return sr(e, o, n - r.index);
  } else {
    const o = new Ni(null, t._start, 0, s);
    return sr(e, o, n);
  }
}, Jo = (e, t, n, i) => {
  for (; n.right !== null && (n.right.deleted === !0 || n.right.content.constructor === F && $t(
    i.get(
      /** @type {ContentFormat} */
      n.right.content.key
    ),
    /** @type {ContentFormat} */
    n.right.content.value
  )); )
    n.right.deleted || i.delete(
      /** @type {ContentFormat} */
      n.right.content.key
    ), n.forward();
  const s = e.doc, r = s.clientID;
  i.forEach((o, l) => {
    const c = n.left, a = n.right, d = new N(_(r, V(s.store, r)), c, c && c.lastId, a, a && a.id, t, null, new F(l, o));
    d.integrate(e, 0), n.right = d, n.forward();
  });
}, Se = (e, t) => {
  const { key: n, value: i } = t;
  i === null ? e.delete(n) : e.set(n, i);
}, Xo = (e, t) => {
  for (; e.right !== null; ) {
    if (!(e.right.deleted || e.right.content.constructor === F && $t(
      t[
        /** @type {ContentFormat} */
        e.right.content.key
      ] ?? null,
      /** @type {ContentFormat} */
      e.right.content.value
    ))) break;
    e.forward();
  }
}, Zo = (e, t, n, i) => {
  const s = e.doc, r = s.clientID, o = /* @__PURE__ */ new Map();
  for (const l in i) {
    const c = i[l], a = n.currentAttributes.get(l) ?? null;
    if (!$t(a, c)) {
      o.set(l, a);
      const { left: d, right: h } = n;
      n.right = new N(_(r, V(s.store, r)), d, d && d.lastId, h, h && h.id, t, null, new F(l, c)), n.right.integrate(e, 0), n.forward();
    }
  }
  return o;
}, ui = (e, t, n, i, s) => {
  n.currentAttributes.forEach((u, f) => {
    s[f] === void 0 && (s[f] = null);
  });
  const r = e.doc, o = r.clientID;
  Xo(n, s);
  const l = Zo(e, t, n, s), c = i.constructor === String ? new Et(
    /** @type {string} */
    i
  ) : i instanceof q ? new _t(i) : new se(i);
  let { left: a, right: d, index: h } = n;
  t._searchMarker && $e(t._searchMarker, n.index, c.getLength()), d = new N(_(o, V(r.store, o)), a, a && a.lastId, d, d && d.id, t, null, c), d.integrate(e, 0), n.right = d, n.index = h, n.forward(), Jo(e, t, n, l);
}, rr = (e, t, n, i, s) => {
  const r = e.doc, o = r.clientID;
  Xo(n, s);
  const l = Zo(e, t, n, s);
  t: for (; n.right !== null && (i > 0 || l.size > 0 && (n.right.deleted || n.right.content.constructor === F)); ) {
    if (!n.right.deleted)
      switch (n.right.content.constructor) {
        case F: {
          const { key: c, value: a } = (
            /** @type {ContentFormat} */
            n.right.content
          ), d = s[c];
          if (d !== void 0) {
            if ($t(d, a))
              l.delete(c);
            else {
              if (i === 0)
                break t;
              l.set(c, a);
            }
            n.right.delete(e);
          } else
            n.currentAttributes.set(c, a);
          break;
        }
        default:
          i < n.right.length && ht(e, _(n.right.id.client, n.right.id.clock + i)), i -= n.right.length;
          break;
      }
    n.forward();
  }
  if (i > 0) {
    let c = "";
    for (; i > 0; i--)
      c += `
`;
    n.right = new N(_(o, V(r.store, o)), n.left, n.left && n.left.lastId, n.right, n.right && n.right.id, t, null, new Et(c)), n.right.integrate(e, 0), n.forward();
  }
  Jo(e, t, n, l);
}, Qo = (e, t, n, i, s) => {
  let r = t;
  const o = St();
  for (; r && (!r.countable || r.deleted); ) {
    if (!r.deleted && r.content.constructor === F) {
      const a = (
        /** @type {ContentFormat} */
        r.content
      );
      o.set(a.key, a);
    }
    r = r.right;
  }
  let l = 0, c = !1;
  for (; t !== r; ) {
    if (n === t && (c = !0), !t.deleted) {
      const a = t.content;
      switch (a.constructor) {
        case F: {
          const { key: d, value: h } = (
            /** @type {ContentFormat} */
            a
          ), u = i.get(d) ?? null;
          (o.get(d) !== a || u === h) && (t.delete(e), l++, !c && (s.get(d) ?? null) === h && u !== h && (u === null ? s.delete(d) : s.set(d, u))), !c && !t.deleted && Se(
            s,
            /** @type {ContentFormat} */
            a
          );
          break;
        }
      }
    }
    t = /** @type {Item} */
    t.right;
  }
  return l;
}, nu = (e, t) => {
  for (; t && t.right && (t.right.deleted || !t.right.countable); )
    t = t.right;
  const n = /* @__PURE__ */ new Set();
  for (; t && (t.deleted || !t.countable); ) {
    if (!t.deleted && t.content.constructor === F) {
      const i = (
        /** @type {ContentFormat} */
        t.content.key
      );
      n.has(i) ? t.delete(e) : n.add(i);
    }
    t = t.left;
  }
}, iu = (e) => {
  let t = 0;
  return L(
    /** @type {Doc} */
    e.doc,
    (n) => {
      let i = (
        /** @type {Item} */
        e._start
      ), s = e._start, r = St();
      const o = Ci(r);
      for (; s; ) {
        if (s.deleted === !1)
          switch (s.content.constructor) {
            case F:
              Se(
                o,
                /** @type {ContentFormat} */
                s.content
              );
              break;
            default:
              t += Qo(n, i, s, r, o), r = Ci(o), i = s;
              break;
          }
        s = s.right;
      }
    }
  ), t;
}, su = (e) => {
  const t = /* @__PURE__ */ new Set(), n = e.doc;
  for (const [i, s] of e.afterState.entries()) {
    const r = e.beforeState.get(i) || 0;
    s !== r && No(
      e,
      /** @type {Array<Item|GC>} */
      n.store.clients.get(i),
      r,
      s,
      (o) => {
        !o.deleted && /** @type {Item} */
        o.content.constructor === F && o.constructor !== wt && t.add(
          /** @type {any} */
          o.parent
        );
      }
    );
  }
  L(n, (i) => {
    Ft(e, e.deleteSet, (s) => {
      if (s instanceof wt || !/** @type {YText} */
      s.parent._hasFormatting || t.has(
        /** @type {YText} */
        s.parent
      ))
        return;
      const r = (
        /** @type {YText} */
        s.parent
      );
      s.content.constructor === F ? t.add(r) : nu(i, s);
    });
    for (const s of t)
      iu(s);
  });
}, or = (e, t, n) => {
  const i = n, s = Ci(t.currentAttributes), r = t.right;
  for (; n > 0 && t.right !== null; ) {
    if (t.right.deleted === !1)
      switch (t.right.content.constructor) {
        case _t:
        case se:
        case Et:
          n < t.right.length && ht(e, _(t.right.id.client, t.right.id.clock + n)), n -= t.right.length, t.right.delete(e);
          break;
      }
    t.forward();
  }
  r && Qo(e, r, t.right, s, t.currentAttributes);
  const o = (
    /** @type {AbstractType<any>} */
    /** @type {Item} */
    (t.left || t.right).parent
  );
  return o._searchMarker && $e(o._searchMarker, t.index, -i + n), t;
};
class ru extends Gn {
  /**
   * @param {YText} ytext
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed
   */
  constructor(t, n, i) {
    super(t, n), this.childListChanged = !1, this.keysChanged = /* @__PURE__ */ new Set(), i.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.keysChanged.add(s);
    });
  }
  /**
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    if (this._changes === null) {
      const t = {
        keys: this.keys,
        delta: this.delta,
        added: /* @__PURE__ */ new Set(),
        deleted: /* @__PURE__ */ new Set()
      };
      this._changes = t;
    }
    return (
      /** @type {any} */
      this._changes
    );
  }
  /**
   * Compute the changes in the delta format.
   * A {@link https://quilljs.com/docs/delta/|Quill Delta}) that represents the changes on the document.
   *
   * @type {Array<{insert?:string|object|AbstractType<any>, delete?:number, retain?:number, attributes?: Object<string,any>}>}
   *
   * @public
   */
  get delta() {
    if (this._delta === null) {
      const t = (
        /** @type {Doc} */
        this.target.doc
      ), n = [];
      L(t, (i) => {
        const s = /* @__PURE__ */ new Map(), r = /* @__PURE__ */ new Map();
        let o = this.target._start, l = null;
        const c = {};
        let a = "", d = 0, h = 0;
        const u = () => {
          if (l !== null) {
            let f = null;
            switch (l) {
              case "delete":
                h > 0 && (f = { delete: h }), h = 0;
                break;
              case "insert":
                (typeof a == "object" || a.length > 0) && (f = { insert: a }, s.size > 0 && (f.attributes = {}, s.forEach((g, m) => {
                  g !== null && (f.attributes[m] = g);
                }))), a = "";
                break;
              case "retain":
                d > 0 && (f = { retain: d }, uh(c) || (f.attributes = hh({}, c))), d = 0;
                break;
            }
            f && n.push(f), l = null;
          }
        };
        for (; o !== null; ) {
          switch (o.content.constructor) {
            case _t:
            case se:
              this.adds(o) ? this.deletes(o) || (u(), l = "insert", a = o.content.getContent()[0], u()) : this.deletes(o) ? (l !== "delete" && (u(), l = "delete"), h += 1) : o.deleted || (l !== "retain" && (u(), l = "retain"), d += 1);
              break;
            case Et:
              this.adds(o) ? this.deletes(o) || (l !== "insert" && (u(), l = "insert"), a += /** @type {ContentString} */
              o.content.str) : this.deletes(o) ? (l !== "delete" && (u(), l = "delete"), h += o.length) : o.deleted || (l !== "retain" && (u(), l = "retain"), d += o.length);
              break;
            case F: {
              const { key: f, value: g } = (
                /** @type {ContentFormat} */
                o.content
              );
              if (this.adds(o)) {
                if (!this.deletes(o)) {
                  const m = s.get(f) ?? null;
                  $t(m, g) ? g !== null && o.delete(i) : (l === "retain" && u(), $t(g, r.get(f) ?? null) ? delete c[f] : c[f] = g);
                }
              } else if (this.deletes(o)) {
                r.set(f, g);
                const m = s.get(f) ?? null;
                $t(m, g) || (l === "retain" && u(), c[f] = m);
              } else if (!o.deleted) {
                r.set(f, g);
                const m = c[f];
                m !== void 0 && ($t(m, g) ? m !== null && o.delete(i) : (l === "retain" && u(), g === null ? delete c[f] : c[f] = g));
              }
              o.deleted || (l === "insert" && u(), Se(
                s,
                /** @type {ContentFormat} */
                o.content
              ));
              break;
            }
          }
          o = o.right;
        }
        for (u(); n.length > 0; ) {
          const f = n[n.length - 1];
          if (f.retain !== void 0 && f.attributes === void 0)
            n.pop();
          else
            break;
        }
      }), this._delta = n;
    }
    return (
      /** @type {any} */
      this._delta
    );
  }
}
class Kt extends q {
  /**
   * @param {String} [string] The initial value of the YText.
   */
  constructor(t) {
    super(), this._pending = t !== void 0 ? [() => this.insert(0, t)] : [], this._searchMarker = [], this._hasFormatting = !1;
  }
  /**
   * Number of characters of this text type.
   *
   * @type {number}
   */
  get length() {
    return this.doc ?? tt(), this._length;
  }
  /**
   * @param {Doc} y
   * @param {Item} item
   */
  _integrate(t, n) {
    super._integrate(t, n);
    try {
      this._pending.forEach((i) => i());
    } catch (i) {
      console.error(i);
    }
    this._pending = null;
  }
  _copy() {
    return new Kt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YText}
   */
  clone() {
    const t = new Kt();
    return t.applyDelta(this.toDelta()), t;
  }
  /**
   * Creates YTextEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, n) {
    super._callObserver(t, n);
    const i = new ru(this, t, n);
    qn(this, t, i), !t.local && this._hasFormatting && (t._needFormattingCleanup = !0);
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @public
   */
  toString() {
    this.doc ?? tt();
    let t = "", n = this._start;
    for (; n !== null; )
      !n.deleted && n.countable && n.content.constructor === Et && (t += /** @type {ContentString} */
      n.content.str), n = n.right;
    return t;
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @return {string}
   * @public
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Apply a {@link Delta} on this shared YText type.
   *
   * @param {Array<any>} delta The changes to apply on this element.
   * @param {object}  opts
   * @param {boolean} [opts.sanitize] Sanitize input delta. Removes ending newlines if set to true.
   *
   *
   * @public
   */
  applyDelta(t, { sanitize: n = !0 } = {}) {
    this.doc !== null ? L(this.doc, (i) => {
      const s = new Ni(null, this._start, 0, /* @__PURE__ */ new Map());
      for (let r = 0; r < t.length; r++) {
        const o = t[r];
        if (o.insert !== void 0) {
          const l = !n && typeof o.insert == "string" && r === t.length - 1 && s.right === null && o.insert.slice(-1) === `
` ? o.insert.slice(0, -1) : o.insert;
          (typeof l != "string" || l.length > 0) && ui(i, this, s, l, o.attributes || {});
        } else o.retain !== void 0 ? rr(i, this, s, o.retain, o.attributes || {}) : o.delete !== void 0 && or(i, s, o.delete);
      }
    }) : this._pending.push(() => this.applyDelta(t));
  }
  /**
   * Returns the Delta representation of this YText type.
   *
   * @param {Snapshot} [snapshot]
   * @param {Snapshot} [prevSnapshot]
   * @param {function('removed' | 'added', ID):any} [computeYChange]
   * @return {any} The Delta representation of this type.
   *
   * @public
   */
  toDelta(t, n, i) {
    this.doc ?? tt();
    const s = [], r = /* @__PURE__ */ new Map(), o = (
      /** @type {Doc} */
      this.doc
    );
    let l = "", c = this._start;
    function a() {
      if (l.length > 0) {
        const h = {};
        let u = !1;
        r.forEach((g, m) => {
          u = !0, h[m] = g;
        });
        const f = { insert: l };
        u && (f.attributes = h), s.push(f), l = "";
      }
    }
    const d = () => {
      for (; c !== null; ) {
        if (Gt(c, t) || n !== void 0 && Gt(c, n))
          switch (c.content.constructor) {
            case Et: {
              const h = r.get("ychange");
              t !== void 0 && !Gt(c, t) ? (h === void 0 || h.user !== c.id.client || h.type !== "removed") && (a(), r.set("ychange", i ? i("removed", c.id) : { type: "removed" })) : n !== void 0 && !Gt(c, n) ? (h === void 0 || h.user !== c.id.client || h.type !== "added") && (a(), r.set("ychange", i ? i("added", c.id) : { type: "added" })) : h !== void 0 && (a(), r.delete("ychange")), l += /** @type {ContentString} */
              c.content.str;
              break;
            }
            case _t:
            case se: {
              a();
              const h = {
                insert: c.content.getContent()[0]
              };
              if (r.size > 0) {
                const u = (
                  /** @type {Object<string,any>} */
                  {}
                );
                h.attributes = u, r.forEach((f, g) => {
                  u[g] = f;
                });
              }
              s.push(h);
              break;
            }
            case F:
              Gt(c, t) && (a(), Se(
                r,
                /** @type {ContentFormat} */
                c.content
              ));
              break;
          }
        c = c.right;
      }
      a();
    };
    return t || n ? L(o, (h) => {
      t && Mi(h, t), n && Mi(h, n), d();
    }, "cleanup") : d(), s;
  }
  /**
   * Insert text at a given index.
   *
   * @param {number} index The index at which to start inserting.
   * @param {String} text The text to insert at the specified position.
   * @param {TextAttributes} [attributes] Optionally define some formatting
   *                                    information to apply on the inserted
   *                                    Text.
   * @public
   */
  insert(t, n, i) {
    if (n.length <= 0)
      return;
    const s = this.doc;
    s !== null ? L(s, (r) => {
      const o = cn(r, this, t, !i);
      i || (i = {}, o.currentAttributes.forEach((l, c) => {
        i[c] = l;
      })), ui(r, this, o, n, i);
    }) : this._pending.push(() => this.insert(t, n, i));
  }
  /**
   * Inserts an embed at a index.
   *
   * @param {number} index The index to insert the embed at.
   * @param {Object | AbstractType<any>} embed The Object that represents the embed.
   * @param {TextAttributes} [attributes] Attribute information to apply on the
   *                                    embed
   *
   * @public
   */
  insertEmbed(t, n, i) {
    const s = this.doc;
    s !== null ? L(s, (r) => {
      const o = cn(r, this, t, !i);
      ui(r, this, o, n, i || {});
    }) : this._pending.push(() => this.insertEmbed(t, n, i || {}));
  }
  /**
   * Deletes text starting from an index.
   *
   * @param {number} index Index at which to start deleting.
   * @param {number} length The number of characters to remove. Defaults to 1.
   *
   * @public
   */
  delete(t, n) {
    if (n === 0)
      return;
    const i = this.doc;
    i !== null ? L(i, (s) => {
      or(s, cn(s, this, t, !0), n);
    }) : this._pending.push(() => this.delete(t, n));
  }
  /**
   * Assigns properties to a range of text.
   *
   * @param {number} index The position where to start formatting.
   * @param {number} length The amount of characters to assign properties to.
   * @param {TextAttributes} attributes Attribute information to apply on the
   *                                    text.
   *
   * @public
   */
  format(t, n, i) {
    if (n === 0)
      return;
    const s = this.doc;
    s !== null ? L(s, (r) => {
      const o = cn(r, this, t, !1);
      o.right !== null && rr(r, this, o, n, i);
    }) : this._pending.push(() => this.format(t, n, i));
  }
  /**
   * Removes an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(t) {
    this.doc !== null ? L(this.doc, (n) => {
      Dn(n, this, t);
    }) : this._pending.push(() => this.removeAttribute(t));
  }
  /**
   * Sets or updates an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be set.
   * @param {any} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(t, n) {
    this.doc !== null ? L(this.doc, (i) => {
      ps(i, this, t, n);
    }) : this._pending.push(() => this.setAttribute(t, n));
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {any} The queried attribute value.
   *
   * @public
   */
  getAttribute(t) {
    return (
      /** @type {any} */
      gs(this, t)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @return {Object<string, any>} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes() {
    return Yo(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(Eu);
  }
}
const ou = (e) => new Kt();
class fi {
  /**
   * @param {YXmlFragment | YXmlElement} root
   * @param {function(AbstractType<any>):boolean} [f]
   */
  constructor(t, n = () => !0) {
    this._filter = n, this._root = t, this._currentNode = /** @type {Item} */
    t._start, this._firstCall = !0, t.doc ?? tt();
  }
  [Symbol.iterator]() {
    return this;
  }
  /**
   * Get the next node.
   *
   * @return {IteratorResult<YXmlElement|YXmlText|YXmlHook>} The next node.
   *
   * @public
   */
  next() {
    let t = this._currentNode, n = t && t.content && /** @type {any} */
    t.content.type;
    if (t !== null && (!this._firstCall || t.deleted || !this._filter(n)))
      do
        if (n = /** @type {any} */
        t.content.type, !t.deleted && (n.constructor === rt || n.constructor === Zt) && n._start !== null)
          t = n._start;
        else
          for (; t !== null; ) {
            const i = t.next;
            if (i !== null) {
              t = i;
              break;
            } else t.parent === this._root ? t = null : t = /** @type {AbstractType<any>} */
            t.parent._item;
          }
      while (t !== null && (t.deleted || !this._filter(
        /** @type {ContentType} */
        t.content.type
      )));
    return this._firstCall = !1, t === null ? { value: void 0, done: !0 } : (this._currentNode = t, { value: (
      /** @type {any} */
      t.content.type
    ), done: !1 });
  }
}
class Zt extends q {
  constructor() {
    super(), this._prelimContent = [];
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get firstChild() {
    const t = this._first;
    return t ? t.content.getContent()[0] : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, n) {
    super._integrate(t, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  _copy() {
    return new Zt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlFragment}
   */
  clone() {
    const t = new Zt();
    return t.insert(0, this.toArray().map((n) => n instanceof q ? n.clone() : n)), t;
  }
  get length() {
    return this.doc ?? tt(), this._prelimContent === null ? this._length : this._prelimContent.length;
  }
  /**
   * Create a subtree of childNodes.
   *
   * @example
   * const walker = elem.createTreeWalker(dom => dom.nodeName === 'div')
   * for (let node in walker) {
   *   // `node` is a div node
   *   nop(node)
   * }
   *
   * @param {function(AbstractType<any>):boolean} filter Function that is called on each child element and
   *                          returns a Boolean indicating whether the child
   *                          is to be included in the subtree.
   * @return {YXmlTreeWalker} A subtree and a position within it.
   *
   * @public
   */
  createTreeWalker(t) {
    return new fi(this, t);
  }
  /**
   * Returns the first YXmlElement that matches the query.
   * Similar to DOM's {@link querySelector}.
   *
   * Query support:
   *   - tagname
   * TODO:
   *   - id
   *   - attribute
   *
   * @param {CSS_Selector} query The query on the children.
   * @return {YXmlElement|YXmlText|YXmlHook|null} The first element that matches the query or null.
   *
   * @public
   */
  querySelector(t) {
    t = t.toUpperCase();
    const i = new fi(this, (s) => s.nodeName && s.nodeName.toUpperCase() === t).next();
    return i.done ? null : i.value;
  }
  /**
   * Returns all YXmlElements that match the query.
   * Similar to Dom's {@link querySelectorAll}.
   *
   * @todo Does not yet support all queries. Currently only query by tagName.
   *
   * @param {CSS_Selector} query The query on the children
   * @return {Array<YXmlElement|YXmlText|YXmlHook|null>} The elements that match this query.
   *
   * @public
   */
  querySelectorAll(t) {
    return t = t.toUpperCase(), Xt(new fi(this, (n) => n.nodeName && n.nodeName.toUpperCase() === t));
  }
  /**
   * Creates YXmlEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, n) {
    qn(this, t, new au(this, n, t));
  }
  /**
   * Get the string representation of all the children of this YXmlFragment.
   *
   * @return {string} The string representation of all children.
   */
  toString() {
    return Fo(this, (t) => t.toString()).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, n = {}, i) {
    const s = t.createDocumentFragment();
    return i !== void 0 && i._createAssociation(s, this), ze(this, (r) => {
      s.insertBefore(r.toDOM(t, n, i), null);
    }), s;
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {number} index The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insert(t, n) {
    this.doc !== null ? L(this.doc, (i) => {
      Wo(i, this, t, n);
    }) : this._prelimContent.splice(t, 0, ...n);
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {null|Item|YXmlElement|YXmlText} ref The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insertAfter(t, n) {
    if (this.doc !== null)
      L(this.doc, (i) => {
        const s = t && t instanceof q ? t._item : t;
        Tn(i, this, s, n);
      });
    else {
      const i = (
        /** @type {Array<any>} */
        this._prelimContent
      ), s = t === null ? 0 : i.findIndex((r) => r === t) + 1;
      if (s === 0 && t !== null)
        throw Ot("Reference item not found");
      i.splice(s, 0, ...n);
    }
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} [length=1] The number of elements to remove. Defaults to 1.
   */
  delete(t, n = 1) {
    this.doc !== null ? L(this.doc, (i) => {
      Go(i, this, t, n);
    }) : this._prelimContent.splice(t, n);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<YXmlElement|YXmlText|YXmlHook>}
   */
  toArray() {
    return Vo(this);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to append.
   */
  push(t) {
    this.insert(this.length, t);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to prepend.
   */
  unshift(t) {
    this.insert(0, t);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {YXmlElement|YXmlText}
   */
  get(t) {
    return Ko(this, t);
  }
  /**
   * Returns a portion of this YXmlFragment into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<YXmlElement|YXmlText>}
   */
  slice(t = 0, n = this.length) {
    return zo(this, t, n);
  }
  /**
   * Executes a provided function on once on every child element.
   *
   * @param {function(YXmlElement|YXmlText,number, typeof self):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    ze(this, t);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(Tu);
  }
}
const lu = (e) => new Zt();
class rt extends Zt {
  constructor(t = "UNDEFINED") {
    super(), this.nodeName = t, this._prelimAttrs = /* @__PURE__ */ new Map();
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const t = this._item ? this._item.next : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const t = this._item ? this._item.prev : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, n) {
    super._integrate(t, n), /** @type {Map<string, any>} */
    this._prelimAttrs.forEach((i, s) => {
      this.setAttribute(s, i);
    }), this._prelimAttrs = null;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   *
   * @return {YXmlElement}
   */
  _copy() {
    return new rt(this.nodeName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlElement<KV>}
   */
  clone() {
    const t = new rt(this.nodeName), n = this.getAttributes();
    return dh(n, (i, s) => {
      t.setAttribute(
        s,
        /** @type {any} */
        i
      );
    }), t.insert(0, this.toArray().map((i) => i instanceof q ? i.clone() : i)), t;
  }
  /**
   * Returns the XML serialization of this YXmlElement.
   * The attributes are ordered by attribute-name, so you can easily use this
   * method to compare YXmlElements
   *
   * @return {string} The string representation of this type.
   *
   * @public
   */
  toString() {
    const t = this.getAttributes(), n = [], i = [];
    for (const l in t)
      i.push(l);
    i.sort();
    const s = i.length;
    for (let l = 0; l < s; l++) {
      const c = i[l];
      n.push(c + '="' + t[c] + '"');
    }
    const r = this.nodeName.toLocaleLowerCase(), o = n.length > 0 ? " " + n.join(" ") : "";
    return `<${r}${o}>${super.toString()}</${r}>`;
  }
  /**
   * Removes an attribute from this YXmlElement.
   *
   * @param {string} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(t) {
    this.doc !== null ? L(this.doc, (n) => {
      Dn(n, this, t);
    }) : this._prelimAttrs.delete(t);
  }
  /**
   * Sets or updates an attribute.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that is to be set.
   * @param {KV[KEY]} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(t, n) {
    this.doc !== null ? L(this.doc, (i) => {
      ps(i, this, t, n);
    }) : this._prelimAttrs.set(t, n);
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {KV[KEY]|undefined} The queried attribute value.
   *
   * @public
   */
  getAttribute(t) {
    return (
      /** @type {any} */
      gs(this, t)
    );
  }
  /**
   * Returns whether an attribute exists
   *
   * @param {string} attributeName The attribute name to check for existence.
   * @return {boolean} whether the attribute exists.
   *
   * @public
   */
  hasAttribute(t) {
    return (
      /** @type {any} */
      qo(this, t)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @param {Snapshot} [snapshot]
   * @return {{ [Key in Extract<keyof KV,string>]?: KV[Key]}} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes(t) {
    return (
      /** @type {any} */
      t ? Xd(this, t) : Yo(this)
    );
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, n = {}, i) {
    const s = t.createElement(this.nodeName), r = this.getAttributes();
    for (const o in r) {
      const l = r[o];
      typeof l == "string" && s.setAttribute(o, l);
    }
    return ze(this, (o) => {
      s.appendChild(o.toDOM(t, n, i));
    }), i !== void 0 && i._createAssociation(s, this), s;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(_u), t.writeKey(this.nodeName);
  }
}
const cu = (e) => new rt(e.readKey());
class au extends Gn {
  /**
   * @param {YXmlElement|YXmlText|YXmlFragment} target The target on which the event is created.
   * @param {Set<string|null>} subs The set of changed attributes. `null` is included if the
   *                   child list changed.
   * @param {Transaction} transaction The transaction instance with which the
   *                                  change was created.
   */
  constructor(t, n, i) {
    super(t, i), this.childListChanged = !1, this.attributesChanged = /* @__PURE__ */ new Set(), n.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.attributesChanged.add(s);
    });
  }
}
class An extends we {
  /**
   * @param {string} hookName nodeName of the Dom Node.
   */
  constructor(t) {
    super(), this.hookName = t;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   */
  _copy() {
    return new An(this.hookName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlHook}
   */
  clone() {
    const t = new An(this.hookName);
    return this.forEach((n, i) => {
      t.set(i, n);
    }), t;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object.<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type
   * @return {Element} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, n = {}, i) {
    const s = n[this.hookName];
    let r;
    return s !== void 0 ? r = s.createDom(this) : r = document.createElement(this.hookName), r.setAttribute("data-yjs-hook", this.hookName), i !== void 0 && i._createAssociation(r, this), r;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(Du), t.writeKey(this.hookName);
  }
}
const hu = (e) => new An(e.readKey());
class ft extends Kt {
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const t = this._item ? this._item.next : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const t = this._item ? this._item.prev : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  _copy() {
    return new ft();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlText}
   */
  clone() {
    const t = new ft();
    return t.applyDelta(this.toDelta()), t;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlText.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Text} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, n, i) {
    const s = t.createTextNode(this.toString());
    return i !== void 0 && i._createAssociation(s, this), s;
  }
  toString() {
    return this.toDelta().map((t) => {
      const n = [];
      for (const s in t.attributes) {
        const r = [];
        for (const o in t.attributes[s])
          r.push({ key: o, value: t.attributes[s][o] });
        r.sort((o, l) => o.key < l.key ? -1 : 1), n.push({ nodeName: s, attrs: r });
      }
      n.sort((s, r) => s.nodeName < r.nodeName ? -1 : 1);
      let i = "";
      for (let s = 0; s < n.length; s++) {
        const r = n[s];
        i += `<${r.nodeName}`;
        for (let o = 0; o < r.attrs.length; o++) {
          const l = r.attrs[o];
          i += ` ${l.key}="${l.value}"`;
        }
        i += ">";
      }
      i += t.insert;
      for (let s = n.length - 1; s >= 0; s--)
        i += `</${n[s].nodeName}>`;
      return i;
    }).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(Au);
  }
}
const du = (e) => new ft();
class ms {
  /**
   * @param {ID} id
   * @param {number} length
   */
  constructor(t, n) {
    this.id = t, this.length = n;
  }
  /**
   * @type {boolean}
   */
  get deleted() {
    throw xt();
  }
  /**
   * Merge this struct with the item to the right.
   * This method is already assuming that `this.id.clock + this.length === this.id.clock`.
   * Also this method does *not* remove right from StructStore!
   * @param {AbstractStruct} right
   * @return {boolean} whether this merged with right
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   * @param {number} encodingRef
   */
  write(t, n, i) {
    throw xt();
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, n) {
    throw xt();
  }
}
const uu = 0;
class wt extends ms {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {GC} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.constructor !== t.constructor ? !1 : (this.length += t.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, n) {
    n > 0 && (this.id.clock += n, this.length -= n), Ro(t.doc.store, this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeInfo(uu), t.writeLen(this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, n) {
    return null;
  }
}
class tn {
  /**
   * @param {Uint8Array} content
   */
  constructor(t) {
    this.content = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.content];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentBinary}
   */
  copy() {
    return new tn(this.content);
  }
  /**
   * @param {number} offset
   * @return {ContentBinary}
   */
  splice(t) {
    throw xt();
  }
  /**
   * @param {ContentBinary} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeBuf(this.content);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 3;
  }
}
const fu = (e) => new tn(e.readBuf());
class Ve {
  /**
   * @param {number} len
   */
  constructor(t) {
    this.len = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.len;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentDeleted}
   */
  copy() {
    return new Ve(this.len);
  }
  /**
   * @param {number} offset
   * @return {ContentDeleted}
   */
  splice(t) {
    const n = new Ve(this.len - t);
    return this.len = t, n;
  }
  /**
   * @param {ContentDeleted} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.len += t.len, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
    Ue(t.deleteSet, n.id.client, n.id.clock, this.len), n.markDeleted();
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeLen(this.len - n);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 1;
  }
}
const pu = (e) => new Ve(e.readLen()), tl = (e, t) => new ie({ guid: e, ...t, shouldLoad: t.shouldLoad || t.autoLoad || !1 });
class en {
  /**
   * @param {Doc} doc
   */
  constructor(t) {
    t._item && console.error("This document was already integrated as a sub-document. You should create a second instance instead with the same guid."), this.doc = t;
    const n = {};
    this.opts = n, t.gc || (n.gc = !1), t.autoLoad && (n.autoLoad = !0), t.meta !== null && (n.meta = t.meta);
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.doc];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentDoc}
   */
  copy() {
    return new en(tl(this.doc.guid, this.opts));
  }
  /**
   * @param {number} offset
   * @return {ContentDoc}
   */
  splice(t) {
    throw xt();
  }
  /**
   * @param {ContentDoc} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
    this.doc._item = n, t.subdocsAdded.add(this.doc), this.doc.shouldLoad && t.subdocsLoaded.add(this.doc);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
    t.subdocsAdded.has(this.doc) ? t.subdocsAdded.delete(this.doc) : t.subdocsRemoved.add(this.doc);
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeString(this.doc.guid), t.writeAny(this.opts);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 9;
  }
}
const gu = (e) => new en(tl(e.readString(), e.readAny()));
class se {
  /**
   * @param {Object} embed
   */
  constructor(t) {
    this.embed = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.embed];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentEmbed}
   */
  copy() {
    return new se(this.embed);
  }
  /**
   * @param {number} offset
   * @return {ContentEmbed}
   */
  splice(t) {
    throw xt();
  }
  /**
   * @param {ContentEmbed} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeJSON(this.embed);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 5;
  }
}
const mu = (e) => new se(e.readJSON());
class F {
  /**
   * @param {string} key
   * @param {Object} value
   */
  constructor(t, n) {
    this.key = t, this.value = n;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentFormat}
   */
  copy() {
    return new F(this.key, this.value);
  }
  /**
   * @param {number} _offset
   * @return {ContentFormat}
   */
  splice(t) {
    throw xt();
  }
  /**
   * @param {ContentFormat} _right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} _transaction
   * @param {Item} item
   */
  integrate(t, n) {
    const i = (
      /** @type {YText} */
      n.parent
    );
    i._searchMarker = null, i._hasFormatting = !0;
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeKey(this.key), t.writeJSON(this.value);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 6;
  }
}
const wu = (e) => new F(e.readKey(), e.readJSON());
class In {
  /**
   * @param {Array<any>} arr
   */
  constructor(t) {
    this.arr = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentJSON}
   */
  copy() {
    return new In(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentJSON}
   */
  splice(t) {
    const n = new In(this.arr.slice(t));
    return this.arr = this.arr.slice(0, t), n;
  }
  /**
   * @param {ContentJSON} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.arr = this.arr.concat(t.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    const i = this.arr.length;
    t.writeLen(i - n);
    for (let s = n; s < i; s++) {
      const r = this.arr[s];
      t.writeString(r === void 0 ? "undefined" : JSON.stringify(r));
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 2;
  }
}
const bu = (e) => {
  const t = e.readLen(), n = [];
  for (let i = 0; i < t; i++) {
    const s = e.readString();
    s === "undefined" ? n.push(void 0) : n.push(JSON.parse(s));
  }
  return new In(n);
}, xu = vn("node_env") === "development";
class Qt {
  /**
   * @param {Array<any>} arr
   */
  constructor(t) {
    this.arr = t, xu && Qr(t);
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentAny}
   */
  copy() {
    return new Qt(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentAny}
   */
  splice(t) {
    const n = new Qt(this.arr.slice(t));
    return this.arr = this.arr.slice(0, t), n;
  }
  /**
   * @param {ContentAny} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.arr = this.arr.concat(t.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    const i = this.arr.length;
    t.writeLen(i - n);
    for (let s = n; s < i; s++) {
      const r = this.arr[s];
      t.writeAny(r);
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 8;
  }
}
const yu = (e) => {
  const t = e.readLen(), n = [];
  for (let i = 0; i < t; i++)
    n.push(e.readAny());
  return new Qt(n);
};
class Et {
  /**
   * @param {string} str
   */
  constructor(t) {
    this.str = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.str.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.str.split("");
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentString}
   */
  copy() {
    return new Et(this.str);
  }
  /**
   * @param {number} offset
   * @return {ContentString}
   */
  splice(t) {
    const n = new Et(this.str.slice(t));
    this.str = this.str.slice(0, t);
    const i = this.str.charCodeAt(t - 1);
    return i >= 55296 && i <= 56319 && (this.str = this.str.slice(0, t - 1) + "�", n.str = "�" + n.str.slice(1)), n;
  }
  /**
   * @param {ContentString} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.str += t.str, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeString(n === 0 ? this.str : this.str.slice(n));
  }
  /**
   * @return {number}
   */
  getRef() {
    return 4;
  }
}
const vu = (e) => new Et(e.readString()), Su = [
  Qd,
  eu,
  ou,
  cu,
  lu,
  hu,
  du
], Cu = 0, ku = 1, Eu = 2, _u = 3, Tu = 4, Du = 5, Au = 6;
class _t {
  /**
   * @param {AbstractType<any>} type
   */
  constructor(t) {
    this.type = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.type];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentType}
   */
  copy() {
    return new _t(this.type._copy());
  }
  /**
   * @param {number} offset
   * @return {ContentType}
   */
  splice(t) {
    throw xt();
  }
  /**
   * @param {ContentType} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, n) {
    this.type._integrate(t.doc, n);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
    let n = this.type._start;
    for (; n !== null; )
      n.deleted ? n.id.clock < (t.beforeState.get(n.id.client) || 0) && t._mergeStructs.push(n) : n.delete(t), n = n.right;
    this.type._map.forEach((i) => {
      i.deleted ? i.id.clock < (t.beforeState.get(i.id.client) || 0) && t._mergeStructs.push(i) : i.delete(t);
    }), t.changed.delete(this.type);
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
    let n = this.type._start;
    for (; n !== null; )
      n.gc(t, !0), n = n.right;
    this.type._start = null, this.type._map.forEach(
      /** @param {Item | null} item */
      (i) => {
        for (; i !== null; )
          i.gc(t, !0), i = i.left;
      }
    ), this.type._map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    this.type._write(t);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 7;
  }
}
const Iu = (e) => new _t(Su[e.readTypeRef()](e)), Li = (e, t) => {
  let n = t, i = 0, s;
  do
    i > 0 && (n = _(n.client, n.clock + i)), s = he(e, n), i = n.clock - s.id.clock, n = s.redone;
  while (n !== null && s instanceof N);
  return {
    item: s,
    diff: i
  };
}, ws = (e, t) => {
  for (; e !== null && e.keep !== t; )
    e.keep = t, e = /** @type {AbstractType<any>} */
    e.parent._item;
}, On = (e, t, n) => {
  const { client: i, clock: s } = t.id, r = new N(
    _(i, s + n),
    t,
    _(i, s + n - 1),
    t.right,
    t.rightOrigin,
    t.parent,
    t.parentSub,
    t.content.splice(n)
  );
  return t.deleted && r.markDeleted(), t.keep && (r.keep = !0), t.redone !== null && (r.redone = _(t.redone.client, t.redone.clock + n)), t.right = r, r.right !== null && (r.right.left = r), e._mergeStructs.push(r), r.parentSub !== null && r.right === null && r.parent._map.set(r.parentSub, r), t.length = n, r;
}, lr = (e, t) => Xi(
  e,
  /** @param {StackItem} s */
  (n) => ve(n.deletions, t)
), el = (e, t, n, i, s, r) => {
  const o = e.doc, l = o.store, c = o.clientID, a = t.redone;
  if (a !== null)
    return ht(e, a);
  let d = (
    /** @type {AbstractType<any>} */
    t.parent._item
  ), h = null, u;
  if (d !== null && d.deleted === !0) {
    if (d.redone === null && (!n.has(d) || el(e, d, n, i, s, r) === null))
      return null;
    for (; d.redone !== null; )
      d = ht(e, d.redone);
  }
  const f = d === null ? (
    /** @type {AbstractType<any>} */
    t.parent
  ) : (
    /** @type {ContentType} */
    d.content.type
  );
  if (t.parentSub === null) {
    for (h = t.left, u = t; h !== null; ) {
      let b = h;
      for (; b !== null && /** @type {AbstractType<any>} */
      b.parent._item !== d; )
        b = b.redone === null ? null : ht(e, b.redone);
      if (b !== null && /** @type {AbstractType<any>} */
      b.parent._item === d) {
        h = b;
        break;
      }
      h = h.left;
    }
    for (; u !== null; ) {
      let b = u;
      for (; b !== null && /** @type {AbstractType<any>} */
      b.parent._item !== d; )
        b = b.redone === null ? null : ht(e, b.redone);
      if (b !== null && /** @type {AbstractType<any>} */
      b.parent._item === d) {
        u = b;
        break;
      }
      u = u.right;
    }
  } else {
    if (u = null, t.right && !s) {
      for (h = t; h !== null && h.right !== null && (h.right.redone || ve(i, h.right.id) || lr(r.undoStack, h.right.id) || lr(r.redoStack, h.right.id)); )
        for (h = h.right; h.redone; ) h = ht(e, h.redone);
      if (h && h.right !== null)
        return null;
    } else
      h = f._map.get(t.parentSub) || null;
    h !== null && /** @type {AbstractType<any>} */
    h.parent._item !== d && (h = f._map.get(t.parentSub) || null);
  }
  const g = V(l, c), m = _(c, g), w = new N(
    m,
    h,
    h && h.lastId,
    u,
    u && u.id,
    f,
    t.parentSub,
    t.content.copy()
  );
  return t.redone = m, ws(w, !0), w.integrate(e, 0), w;
};
class N extends ms {
  /**
   * @param {ID} id
   * @param {Item | null} left
   * @param {ID | null} origin
   * @param {Item | null} right
   * @param {ID | null} rightOrigin
   * @param {AbstractType<any>|ID|null} parent Is a type if integrated, is null if it is possible to copy parent from left or right, is ID before integration to search for it.
   * @param {string | null} parentSub
   * @param {AbstractContent} content
   */
  constructor(t, n, i, s, r, o, l, c) {
    super(t, c.getLength()), this.origin = i, this.left = n, this.right = s, this.rightOrigin = r, this.parent = o, this.parentSub = l, this.redone = null, this.content = c, this.info = this.content.isCountable() ? Ls : 0;
  }
  /**
   * This is used to mark the item as an indexed fast-search marker
   *
   * @type {boolean}
   */
  set marker(t) {
    (this.info & si) > 0 !== t && (this.info ^= si);
  }
  get marker() {
    return (this.info & si) > 0;
  }
  /**
   * If true, do not garbage collect this Item.
   */
  get keep() {
    return (this.info & Ns) > 0;
  }
  set keep(t) {
    this.keep !== t && (this.info ^= Ns);
  }
  get countable() {
    return (this.info & Ls) > 0;
  }
  /**
   * Whether this item was deleted or not.
   * @type {Boolean}
   */
  get deleted() {
    return (this.info & ii) > 0;
  }
  set deleted(t) {
    this.deleted !== t && (this.info ^= ii);
  }
  markDeleted() {
    this.info |= ii;
  }
  /**
   * Return the creator clientID of the missing op or define missing items and return null.
   *
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, n) {
    if (this.origin && this.origin.client !== this.id.client && this.origin.clock >= V(n, this.origin.client))
      return this.origin.client;
    if (this.rightOrigin && this.rightOrigin.client !== this.id.client && this.rightOrigin.clock >= V(n, this.rightOrigin.client))
      return this.rightOrigin.client;
    if (this.parent && this.parent.constructor === ae && this.id.client !== this.parent.client && this.parent.clock >= V(n, this.parent.client))
      return this.parent.client;
    if (this.origin && (this.left = Xs(t, n, this.origin), this.origin = this.left.lastId), this.rightOrigin && (this.right = ht(t, this.rightOrigin), this.rightOrigin = this.right.id), this.left && this.left.constructor === wt || this.right && this.right.constructor === wt)
      this.parent = null;
    else if (!this.parent)
      this.left && this.left.constructor === N ? (this.parent = this.left.parent, this.parentSub = this.left.parentSub) : this.right && this.right.constructor === N && (this.parent = this.right.parent, this.parentSub = this.right.parentSub);
    else if (this.parent.constructor === ae) {
      const i = he(n, this.parent);
      i.constructor === wt ? this.parent = null : this.parent = /** @type {ContentType} */
      i.content.type;
    }
    return null;
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, n) {
    if (n > 0 && (this.id.clock += n, this.left = Xs(t, t.doc.store, _(this.id.client, this.id.clock - 1)), this.origin = this.left.lastId, this.content = this.content.splice(n), this.length -= n), this.parent) {
      if (!this.left && (!this.right || this.right.left !== null) || this.left && this.left.right !== this.right) {
        let i = this.left, s;
        if (i !== null)
          s = i.right;
        else if (this.parentSub !== null)
          for (s = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; s !== null && s.left !== null; )
            s = s.left;
        else
          s = /** @type {AbstractType<any>} */
          this.parent._start;
        const r = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Set();
        for (; s !== null && s !== this.right; ) {
          if (o.add(s), r.add(s), rn(this.origin, s.origin)) {
            if (s.id.client < this.id.client)
              i = s, r.clear();
            else if (rn(this.rightOrigin, s.rightOrigin))
              break;
          } else if (s.origin !== null && o.has(he(t.doc.store, s.origin)))
            r.has(he(t.doc.store, s.origin)) || (i = s, r.clear());
          else
            break;
          s = s.right;
        }
        this.left = i;
      }
      if (this.left !== null) {
        const i = this.left.right;
        this.right = i, this.left.right = this;
      } else {
        let i;
        if (this.parentSub !== null)
          for (i = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; i !== null && i.left !== null; )
            i = i.left;
        else
          i = /** @type {AbstractType<any>} */
          this.parent._start, this.parent._start = this;
        this.right = i;
      }
      this.right !== null ? this.right.left = this : this.parentSub !== null && (this.parent._map.set(this.parentSub, this), this.left !== null && this.left.delete(t)), this.parentSub === null && this.countable && !this.deleted && (this.parent._length += this.length), Ro(t.doc.store, this), this.content.integrate(t, this), Qs(
        t,
        /** @type {AbstractType<any>} */
        this.parent,
        this.parentSub
      ), /** @type {AbstractType<any>} */
      (this.parent._item !== null && /** @type {AbstractType<any>} */
      this.parent._item.deleted || this.parentSub !== null && this.right !== null) && this.delete(t);
    } else
      new wt(this.id, this.length).integrate(t, 0);
  }
  /**
   * Returns the next non-deleted item
   */
  get next() {
    let t = this.right;
    for (; t !== null && t.deleted; )
      t = t.right;
    return t;
  }
  /**
   * Returns the previous non-deleted item
   */
  get prev() {
    let t = this.left;
    for (; t !== null && t.deleted; )
      t = t.left;
    return t;
  }
  /**
   * Computes the last content address of this Item.
   */
  get lastId() {
    return this.length === 1 ? this.id : _(this.id.client, this.id.clock + this.length - 1);
  }
  /**
   * Try to merge two items
   *
   * @param {Item} right
   * @return {boolean}
   */
  mergeWith(t) {
    if (this.constructor === t.constructor && rn(t.origin, this.lastId) && this.right === t && rn(this.rightOrigin, t.rightOrigin) && this.id.client === t.id.client && this.id.clock + this.length === t.id.clock && this.deleted === t.deleted && this.redone === null && t.redone === null && this.content.constructor === t.content.constructor && this.content.mergeWith(t.content)) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent._searchMarker
      );
      return n && n.forEach((i) => {
        i.p === t && (i.p = this, !this.deleted && this.countable && (i.index -= this.length));
      }), t.keep && (this.keep = !0), this.right = t.right, this.right !== null && (this.right.left = this), this.length += t.length, !0;
    }
    return !1;
  }
  /**
   * Mark this Item as deleted.
   *
   * @param {Transaction} transaction
   */
  delete(t) {
    if (!this.deleted) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      this.countable && this.parentSub === null && (n._length -= this.length), this.markDeleted(), Ue(t.deleteSet, this.id.client, this.id.clock, this.length), Qs(t, n, this.parentSub), this.content.delete(t);
    }
  }
  /**
   * @param {StructStore} store
   * @param {boolean} parentGCd
   */
  gc(t, n) {
    if (!this.deleted)
      throw lt();
    this.content.gc(t), n ? Ld(t, this, new wt(this.id, this.length)) : this.content = new Ve(this.length);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   */
  write(t, n) {
    const i = n > 0 ? _(this.id.client, this.id.clock + n - 1) : this.origin, s = this.rightOrigin, r = this.parentSub, o = this.content.getRef() & Un | (i === null ? 0 : ot) | // origin is defined
    (s === null ? 0 : Nt) | // right origin is defined
    (r === null ? 0 : Oe);
    if (t.writeInfo(o), i !== null && t.writeLeftID(i), s !== null && t.writeRightID(s), i === null && s === null) {
      const l = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      if (l._item !== void 0) {
        const c = l._item;
        if (c === null) {
          const a = Pe(l);
          t.writeParentInfo(!0), t.writeString(a);
        } else
          t.writeParentInfo(!1), t.writeLeftID(c.id);
      } else l.constructor === String ? (t.writeParentInfo(!0), t.writeString(l)) : l.constructor === ae ? (t.writeParentInfo(!1), t.writeLeftID(l)) : lt();
      r !== null && t.writeString(r);
    }
    this.content.write(t, n);
  }
}
const nl = (e, t) => Ou[t & Un](e), Ou = [
  () => {
    lt();
  },
  // GC is not ItemContent
  pu,
  // 1
  bu,
  // 2
  fu,
  // 3
  vu,
  // 4
  mu,
  // 5
  wu,
  // 6
  Iu,
  // 7
  yu,
  // 8
  gu,
  // 9
  () => {
    lt();
  }
  // 10 - Skip is not ItemContent
], Mu = 10;
class yt extends ms {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {Skip} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.constructor !== t.constructor ? !1 : (this.length += t.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, n) {
    lt();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, n) {
    t.writeInfo(Mu), A(t.restEncoder, this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, n) {
    return null;
  }
}
const il = (
  /** @type {any} */
  typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : {}
), sl = "__ $YJS$ __";
il[sl] === !0 && console.error("Yjs was already imported. This breaks constructor checks and will lead to issues! - https://github.com/yjs/yjs/issues/438");
il[sl] = !0;
const Ru = () => {
  let e = !0;
  return (t, n) => {
    if (e) {
      e = !1;
      try {
        t();
      } finally {
        e = !0;
      }
    } else n !== void 0 && n();
  };
};
function rl(e, t, n) {
  for (let i = 0; ; i++) {
    if (i == e.childCount || i == t.childCount)
      return e.childCount == t.childCount ? null : n;
    let s = e.child(i), r = t.child(i);
    if (s == r) {
      n += s.nodeSize;
      continue;
    }
    if (!s.sameMarkup(r))
      return n;
    if (s.isText && s.text != r.text) {
      for (let o = 0; s.text[o] == r.text[o]; o++)
        n++;
      return n;
    }
    if (s.content.size || r.content.size) {
      let o = rl(s.content, r.content, n + 1);
      if (o != null)
        return o;
    }
    n += s.nodeSize;
  }
}
function ol(e, t, n, i) {
  for (let s = e.childCount, r = t.childCount; ; ) {
    if (s == 0 || r == 0)
      return s == r ? null : { a: n, b: i };
    let o = e.child(--s), l = t.child(--r), c = o.nodeSize;
    if (o == l) {
      n -= c, i -= c;
      continue;
    }
    if (!o.sameMarkup(l))
      return { a: n, b: i };
    if (o.isText && o.text != l.text) {
      let a = 0, d = Math.min(o.text.length, l.text.length);
      for (; a < d && o.text[o.text.length - a - 1] == l.text[l.text.length - a - 1]; )
        a++, n--, i--;
      return { a: n, b: i };
    }
    if (o.content.size || l.content.size) {
      let a = ol(o.content, l.content, n - 1, i - 1);
      if (a)
        return a;
    }
    n -= c, i -= c;
  }
}
class $ {
  /**
  @internal
  */
  constructor(t, n) {
    if (this.content = t, this.size = n || 0, n == null)
      for (let i = 0; i < t.length; i++)
        this.size += t[i].nodeSize;
  }
  /**
  Invoke a callback for all descendant nodes between the given two
  positions (relative to start of this fragment). Doesn't descend
  into a node when the callback returns `false`.
  */
  nodesBetween(t, n, i, s = 0, r) {
    for (let o = 0, l = 0; l < n; o++) {
      let c = this.content[o], a = l + c.nodeSize;
      if (a > t && i(c, s + l, r || null, o) !== !1 && c.content.size) {
        let d = l + 1;
        c.nodesBetween(Math.max(0, t - d), Math.min(c.content.size, n - d), i, s + d);
      }
      l = a;
    }
  }
  /**
  Call the given callback for every descendant node. `pos` will be
  relative to the start of the fragment. The callback may return
  `false` to prevent traversal of a given node's children.
  */
  descendants(t) {
    this.nodesBetween(0, this.size, t);
  }
  /**
  Extract the text between `from` and `to`. See the same method on
  [`Node`](https://prosemirror.net/docs/ref/#model.Node.textBetween).
  */
  textBetween(t, n, i, s) {
    let r = "", o = !0;
    return this.nodesBetween(t, n, (l, c) => {
      let a = l.isText ? l.text.slice(Math.max(t, c) - c, n - c) : l.isLeaf ? s ? typeof s == "function" ? s(l) : s : l.type.spec.leafText ? l.type.spec.leafText(l) : "" : "";
      l.isBlock && (l.isLeaf && a || l.isTextblock) && i && (o ? o = !1 : r += i), r += a;
    }, 0), r;
  }
  /**
  Create a new fragment containing the combined content of this
  fragment and the other.
  */
  append(t) {
    if (!t.size)
      return this;
    if (!this.size)
      return t;
    let n = this.lastChild, i = t.firstChild, s = this.content.slice(), r = 0;
    for (n.isText && n.sameMarkup(i) && (s[s.length - 1] = n.withText(n.text + i.text), r = 1); r < t.content.length; r++)
      s.push(t.content[r]);
    return new $(s, this.size + t.size);
  }
  /**
  Cut out the sub-fragment between the two given positions.
  */
  cut(t, n = this.size) {
    if (t == 0 && n == this.size)
      return this;
    let i = [], s = 0;
    if (n > t)
      for (let r = 0, o = 0; o < n; r++) {
        let l = this.content[r], c = o + l.nodeSize;
        c > t && ((o < t || c > n) && (l.isText ? l = l.cut(Math.max(0, t - o), Math.min(l.text.length, n - o)) : l = l.cut(Math.max(0, t - o - 1), Math.min(l.content.size, n - o - 1))), i.push(l), s += l.nodeSize), o = c;
      }
    return new $(i, s);
  }
  /**
  @internal
  */
  cutByIndex(t, n) {
    return t == n ? $.empty : t == 0 && n == this.content.length ? this : new $(this.content.slice(t, n));
  }
  /**
  Create a new fragment in which the node at the given index is
  replaced by the given node.
  */
  replaceChild(t, n) {
    let i = this.content[t];
    if (i == n)
      return this;
    let s = this.content.slice(), r = this.size + n.nodeSize - i.nodeSize;
    return s[t] = n, new $(s, r);
  }
  /**
  Create a new fragment by prepending the given node to this
  fragment.
  */
  addToStart(t) {
    return new $([t].concat(this.content), this.size + t.nodeSize);
  }
  /**
  Create a new fragment by appending the given node to this
  fragment.
  */
  addToEnd(t) {
    return new $(this.content.concat(t), this.size + t.nodeSize);
  }
  /**
  Compare this fragment to another one.
  */
  eq(t) {
    if (this.content.length != t.content.length)
      return !1;
    for (let n = 0; n < this.content.length; n++)
      if (!this.content[n].eq(t.content[n]))
        return !1;
    return !0;
  }
  /**
  The first child of the fragment, or `null` if it is empty.
  */
  get firstChild() {
    return this.content.length ? this.content[0] : null;
  }
  /**
  The last child of the fragment, or `null` if it is empty.
  */
  get lastChild() {
    return this.content.length ? this.content[this.content.length - 1] : null;
  }
  /**
  The number of child nodes in this fragment.
  */
  get childCount() {
    return this.content.length;
  }
  /**
  Get the child node at the given index. Raise an error when the
  index is out of range.
  */
  child(t) {
    let n = this.content[t];
    if (!n)
      throw new RangeError("Index " + t + " out of range for " + this);
    return n;
  }
  /**
  Get the child node at the given index, if it exists.
  */
  maybeChild(t) {
    return this.content[t] || null;
  }
  /**
  Call `f` for every child node, passing the node, its offset
  into this parent node, and its index.
  */
  forEach(t) {
    for (let n = 0, i = 0; n < this.content.length; n++) {
      let s = this.content[n];
      t(s, i, n), i += s.nodeSize;
    }
  }
  /**
  Find the first position at which this fragment and another
  fragment differ, or `null` if they are the same.
  */
  findDiffStart(t, n = 0) {
    return rl(this, t, n);
  }
  /**
  Find the first position, searching from the end, at which this
  fragment and the given fragment differ, or `null` if they are
  the same. Since this position will not be the same in both
  nodes, an object with two separate positions is returned.
  */
  findDiffEnd(t, n = this.size, i = t.size) {
    return ol(this, t, n, i);
  }
  /**
  Find the index and inner offset corresponding to a given relative
  position in this fragment. The result object will be reused
  (overwritten) the next time the function is called. @internal
  */
  findIndex(t) {
    if (t == 0)
      return an(0, t);
    if (t == this.size)
      return an(this.content.length, t);
    if (t > this.size || t < 0)
      throw new RangeError(`Position ${t} outside of fragment (${this})`);
    for (let n = 0, i = 0; ; n++) {
      let s = this.child(n), r = i + s.nodeSize;
      if (r >= t)
        return r == t ? an(n + 1, r) : an(n, i);
      i = r;
    }
  }
  /**
  Return a debugging string that describes this fragment.
  */
  toString() {
    return "<" + this.toStringInner() + ">";
  }
  /**
  @internal
  */
  toStringInner() {
    return this.content.join(", ");
  }
  /**
  Create a JSON-serializeable representation of this fragment.
  */
  toJSON() {
    return this.content.length ? this.content.map((t) => t.toJSON()) : null;
  }
  /**
  Deserialize a fragment from its JSON representation.
  */
  static fromJSON(t, n) {
    if (!n)
      return $.empty;
    if (!Array.isArray(n))
      throw new RangeError("Invalid input for Fragment.fromJSON");
    return new $(n.map(t.nodeFromJSON));
  }
  /**
  Build a fragment from an array of nodes. Ensures that adjacent
  text nodes with the same marks are joined together.
  */
  static fromArray(t) {
    if (!t.length)
      return $.empty;
    let n, i = 0;
    for (let s = 0; s < t.length; s++) {
      let r = t[s];
      i += r.nodeSize, s && r.isText && t[s - 1].sameMarkup(r) ? (n || (n = t.slice(0, s)), n[n.length - 1] = r.withText(n[n.length - 1].text + r.text)) : n && n.push(r);
    }
    return new $(n || t, i);
  }
  /**
  Create a fragment from something that can be interpreted as a
  set of nodes. For `null`, it returns the empty fragment. For a
  fragment, the fragment itself. For a node or array of nodes, a
  fragment containing those nodes.
  */
  static from(t) {
    if (!t)
      return $.empty;
    if (t instanceof $)
      return t;
    if (Array.isArray(t))
      return this.fromArray(t);
    if (t.attrs)
      return new $([t], t.nodeSize);
    throw new RangeError("Can not convert " + t + " to a Fragment" + (t.nodesBetween ? " (looks like multiple versions of prosemirror-model were loaded)" : ""));
  }
}
$.empty = new $([], 0);
const pi = { index: 0, offset: 0 };
function an(e, t) {
  return pi.index = e, pi.offset = t, pi;
}
class bt {
  /**
  Create a slice. When specifying a non-zero open depth, you must
  make sure that there are nodes of at least that depth at the
  appropriate side of the fragment—i.e. if the fragment is an
  empty paragraph node, `openStart` and `openEnd` can't be greater
  than 1.
  
  It is not necessary for the content of open nodes to conform to
  the schema's content constraints, though it should be a valid
  start/end/middle for such a node, depending on which sides are
  open.
  */
  constructor(t, n, i) {
    this.content = t, this.openStart = n, this.openEnd = i;
  }
  /**
  The size this slice would add when inserted into a document.
  */
  get size() {
    return this.content.size - this.openStart - this.openEnd;
  }
  /**
  @internal
  */
  insertAt(t, n) {
    let i = cl(this.content, t + this.openStart, n);
    return i && new bt(i, this.openStart, this.openEnd);
  }
  /**
  @internal
  */
  removeBetween(t, n) {
    return new bt(ll(this.content, t + this.openStart, n + this.openStart), this.openStart, this.openEnd);
  }
  /**
  Tests whether this slice is equal to another slice.
  */
  eq(t) {
    return this.content.eq(t.content) && this.openStart == t.openStart && this.openEnd == t.openEnd;
  }
  /**
  @internal
  */
  toString() {
    return this.content + "(" + this.openStart + "," + this.openEnd + ")";
  }
  /**
  Convert a slice to a JSON-serializable representation.
  */
  toJSON() {
    if (!this.content.size)
      return null;
    let t = { content: this.content.toJSON() };
    return this.openStart > 0 && (t.openStart = this.openStart), this.openEnd > 0 && (t.openEnd = this.openEnd), t;
  }
  /**
  Deserialize a slice from its JSON representation.
  */
  static fromJSON(t, n) {
    if (!n)
      return bt.empty;
    let i = n.openStart || 0, s = n.openEnd || 0;
    if (typeof i != "number" || typeof s != "number")
      throw new RangeError("Invalid input for Slice.fromJSON");
    return new bt($.fromJSON(t, n.content), i, s);
  }
  /**
  Create a slice from a fragment by taking the maximum possible
  open value on both side of the fragment.
  */
  static maxOpen(t, n = !0) {
    let i = 0, s = 0;
    for (let r = t.firstChild; r && !r.isLeaf && (n || !r.type.spec.isolating); r = r.firstChild)
      i++;
    for (let r = t.lastChild; r && !r.isLeaf && (n || !r.type.spec.isolating); r = r.lastChild)
      s++;
    return new bt(t, i, s);
  }
}
bt.empty = new bt($.empty, 0, 0);
function ll(e, t, n) {
  let { index: i, offset: s } = e.findIndex(t), r = e.maybeChild(i), { index: o, offset: l } = e.findIndex(n);
  if (s == t || r.isText) {
    if (l != n && !e.child(o).isText)
      throw new RangeError("Removing non-flat range");
    return e.cut(0, t).append(e.cut(n));
  }
  if (i != o)
    throw new RangeError("Removing non-flat range");
  return e.replaceChild(i, r.copy(ll(r.content, t - s - 1, n - s - 1)));
}
function cl(e, t, n, i) {
  let { index: s, offset: r } = e.findIndex(t), o = e.maybeChild(s);
  if (r == t || o.isText)
    return i && !i.canReplace(s, s, n) ? null : e.cut(0, t).append(n).append(e.cut(t));
  let l = cl(o.content, t - r - 1, n, o);
  return l && e.replaceChild(s, o.copy(l));
}
const Nu = /[\uD800-\uDBFF]/, Lu = /[\uDC00-\uDFFF]/, Uu = (e, t) => {
  let n = 0, i = 0;
  for (; n < e.length && n < t.length && e[n] === t[n]; )
    n++;
  for (n > 0 && Nu.test(e[n - 1]) && n--; i + n < e.length && i + n < t.length && e[e.length - i - 1] === t[t.length - i - 1]; )
    i++;
  return i > 0 && Lu.test(e[e.length - i]) && i--, {
    index: n,
    remove: e.length - n - i,
    insert: t.slice(n, t.length - i)
  };
}, Pu = Uu, It = (e, t) => e >>> t | e << 32 - t, Bu = (e) => It(e, 2) ^ It(e, 13) ^ It(e, 22), $u = (e) => It(e, 6) ^ It(e, 11) ^ It(e, 25), zu = (e) => It(e, 7) ^ It(e, 18) ^ e >>> 3, Vu = (e) => It(e, 17) ^ It(e, 19) ^ e >>> 10, Hu = new Uint32Array([
  1116352408,
  1899447441,
  3049323471,
  3921009573,
  961987163,
  1508970993,
  2453635748,
  2870763221,
  3624381080,
  310598401,
  607225278,
  1426881987,
  1925078388,
  2162078206,
  2614888103,
  3248222580,
  3835390401,
  4022224774,
  264347078,
  604807628,
  770255983,
  1249150122,
  1555081692,
  1996064986,
  2554220882,
  2821834349,
  2952996808,
  3210313671,
  3336571891,
  3584528711,
  113926993,
  338241895,
  666307205,
  773529912,
  1294757372,
  1396182291,
  1695183700,
  1986661051,
  2177026350,
  2456956037,
  2730485921,
  2820302411,
  3259730800,
  3345764771,
  3516065817,
  3600352804,
  4094571909,
  275423344,
  430227734,
  506948616,
  659060556,
  883997877,
  958139571,
  1322822218,
  1537002063,
  1747873779,
  1955562222,
  2024104815,
  2227730452,
  2361852424,
  2428436474,
  2756734187,
  3204031479,
  3329325298
]), Fu = new Uint32Array([
  1779033703,
  3144134277,
  1013904242,
  2773480762,
  1359893119,
  2600822924,
  528734635,
  1541459225
]);
class Ku {
  constructor() {
    const t = new ArrayBuffer(320);
    this._H = new Uint32Array(t, 0, 8), this._H.set(Fu), this._W = new Uint32Array(t, 64, 64);
  }
  _updateHash() {
    const t = this._H, n = this._W;
    for (let h = 16; h < 64; h++)
      n[h] = Vu(n[h - 2]) + n[h - 7] + zu(n[h - 15]) + n[h - 16];
    let i = t[0], s = t[1], r = t[2], o = t[3], l = t[4], c = t[5], a = t[6], d = t[7];
    for (let h = 0, u, f; h < 64; h++)
      u = d + $u(l) + (l & c ^ ~l & a) + Hu[h] + n[h] >>> 0, f = Bu(i) + (i & s ^ i & r ^ s & r) >>> 0, d = a, a = c, c = l, l = o + u >>> 0, o = r, r = s, s = i, i = u + f >>> 0;
    t[0] += i, t[1] += s, t[2] += r, t[3] += o, t[4] += l, t[5] += c, t[6] += a, t[7] += d;
  }
  /**
   * Returns a 32-byte hash.
   *
   * @param {Uint8Array} data
   */
  digest(t) {
    let n = 0;
    for (; n + 56 <= t.length; ) {
      let o = 0;
      for (; o < 16 && n + 3 < t.length; o++)
        this._W[o] = t[n++] << 24 | t[n++] << 16 | t[n++] << 8 | t[n++];
      if (n % 64 !== 0) {
        for (this._W.fill(0, o, 16); n < t.length; )
          this._W[o] |= t[n] << (3 - n % 4) * 8, n++;
        this._W[o] |= ot << (3 - n % 4) * 8;
      }
      this._updateHash();
    }
    const i = n % 64 !== 0;
    this._W.fill(0, 0, 16);
    let s = 0;
    for (; n < t.length; s++)
      for (let o = 3; o >= 0 && n < t.length; o--)
        this._W[s] |= t[n++] << o * 8;
    i || (this._W[s - (n % 4 === 0 ? 0 : 1)] |= ot << (3 - n % 4) * 8), this._W[14] = t.byteLength / Ca, this._W[15] = t.byteLength * 8, this._updateHash();
    const r = new Uint8Array(32);
    for (let o = 0; o < this._H.length; o++)
      for (let l = 0; l < 4; l++)
        r[o * 4 + l] = this._H[o] >>> (3 - l) * 8;
    return r;
  }
}
const ju = (e) => new Ku().digest(e), B = new Vi("y-sync"), Lt = new Vi("y-undo");
new Vi("yjs-cursor");
const Wu = (e) => {
  for (let n = 6; n < e.length; n++)
    e[n % 6] = e[n % 6] ^ e[n];
  return e.slice(0, 6);
}, Gu = (e) => Sh(Wu(ju(Ch(e)))), Mn = (e, t) => t === void 0 ? !e.deleted : t.sv.has(e.id.client) && /** @type {number} */
t.sv.get(e.id.client) > e.id.clock && !ve(t.ds, e.id), Yu = [{ light: "#ecd44433", dark: "#ecd444" }], qu = (e, t, n) => {
  if (!e.has(n)) {
    if (e.size < t.length) {
      const i = Jt();
      e.forEach((s) => i.add(s)), t = t.filter((s) => !i.has(s));
    }
    e.set(n, nh(t));
  }
  return (
    /** @type {ColorDef} */
    e.get(n)
  );
}, Ju = (e, {
  colors: t = Yu,
  colorMapping: n = /* @__PURE__ */ new Map(),
  permanentUserData: i = null,
  onFirstRender: s = () => {
  },
  mapping: r
} = {}) => {
  let o = !1;
  const l = new tf(e, r), c = new vr({
    props: {
      editable: (a) => {
        const d = B.getState(a);
        return d.snapshot == null && d.prevSnapshot == null;
      }
    },
    key: B,
    state: {
      /**
       * @returns {any}
       */
      init: (a, d) => ({
        type: e,
        doc: e.doc,
        binding: l,
        snapshot: null,
        prevSnapshot: null,
        isChangeOrigin: !1,
        isUndoRedoOperation: !1,
        addToHistory: !0,
        colors: t,
        colorMapping: n,
        permanentUserData: i
      }),
      apply: (a, d) => {
        const h = a.getMeta(B);
        if (h !== void 0) {
          d = Object.assign({}, d);
          for (const u in h)
            d[u] = h[u];
        }
        return d.addToHistory = a.getMeta("addToHistory") !== !1, d.isChangeOrigin = h !== void 0 && !!h.isChangeOrigin, d.isUndoRedoOperation = h !== void 0 && !!h.isChangeOrigin && !!h.isUndoRedoOperation, l.prosemirrorView !== null && h !== void 0 && (h.snapshot != null || h.prevSnapshot != null) && xo(0, () => {
          l.prosemirrorView != null && (h.restore == null ? l._renderSnapshot(
            h.snapshot,
            h.prevSnapshot,
            d
          ) : (l._renderSnapshot(
            h.snapshot,
            h.snapshot,
            d
          ), delete d.restore, delete d.snapshot, delete d.prevSnapshot, l.mux(() => {
            l._prosemirrorChanged(
              l.prosemirrorView.state.doc
            );
          })));
        }), d;
      }
    },
    view: (a) => (l.initView(a), r == null && l._forceRerender(), s(), {
      update: () => {
        const d = c.getState(a.state);
        if (d.snapshot == null && d.prevSnapshot == null && // If the content doesn't change initially, we don't render anything to Yjs
        // If the content was cleared by a user action, we want to catch the change and
        // represent it in Yjs
        (o || a.state.doc.content.findDiffStart(
          a.state.doc.type.createAndFill().content
        ) !== null)) {
          if (o = !0, d.addToHistory === !1 && !d.isChangeOrigin) {
            const h = Lt.getState(a.state), u = h && h.undoManager;
            u && u.stopCapturing();
          }
          l.mux(() => {
            d.doc.transact((h) => {
              h.meta.set("addToHistory", d.addToHistory), l._prosemirrorChanged(a.state.doc);
            }, B);
          });
        }
      },
      destroy: () => {
        l.destroy();
      }
    })
  });
  return c;
}, Xu = (e, t, n) => {
  if (t !== null && t.anchor !== null && t.head !== null)
    if (t.type === "all")
      e.setSelection(new Dl(e.doc));
    else if (t.type === "node") {
      const i = Yt(
        n.doc,
        n.type,
        t.anchor,
        n.mapping
      );
      i !== null && e.setSelection(Zu(e, i));
    } else if (t.type === "nodeRange") {
      const i = Yt(
        n.doc,
        n.type,
        t.anchor,
        n.mapping
      ), s = Yt(
        n.doc,
        n.type,
        t.head,
        n.mapping
      ), r = Qu(e, i, s, t.depth);
      r !== null && e.setSelection(r);
    } else {
      const i = Yt(
        n.doc,
        n.type,
        t.anchor,
        n.mapping
      ), s = Yt(
        n.doc,
        n.type,
        t.head,
        n.mapping
      );
      i !== null && s !== null && e.setSelection(Ie.between(e.doc.resolve(i), e.doc.resolve(s)));
    }
}, Zu = (e, t) => {
  const n = e.doc.resolve(t);
  return n.nodeAfter ? Al.create(e.doc, t) : Ie.near(n);
}, Qu = (e, t, n, i) => {
  if (t === null || n === null)
    return null;
  const s = Math.min(Math.max(t, 0), e.doc.content.size), r = Math.min(Math.max(n, 0), e.doc.content.size);
  try {
    const o = Il.fromJSON(e.doc, {
      type: "nodeRange",
      anchor: s,
      head: r,
      depth: i
    });
    return o.ranges.length ? o : Ie.near(e.doc.resolve(s));
  } catch {
    return Ie.near(e.doc.resolve(s));
  }
}, Ui = (e, t) => {
  const n = (
    /** @type {any} */
    t.selection.jsonID
  );
  return {
    type: n,
    // `depth` is only meaningful for NodeRangeSelection; undefined for every other type.
    depth: n === "nodeRange" ? (
      /** @type {any} */
      t.selection.depth
    ) : void 0,
    anchor: Nn(
      t.selection.anchor,
      e.type,
      e.mapping
    ),
    head: Nn(
      t.selection.head,
      e.type,
      e.mapping
    )
  };
};
class tf {
  /**
   * @param {Y.XmlFragment} yXmlFragment The bind source
   * @param {ProsemirrorMapping} mapping
   */
  constructor(t, n = /* @__PURE__ */ new Map()) {
    this.type = t, this.prosemirrorView = null, this.mux = Ru(), this.mapping = n, this.isOMark = /* @__PURE__ */ new Map(), this._observeFunction = this._typeChanged.bind(this), this.doc = t.doc, this.beforeTransactionSelection = null, this.beforeAllTransactions = () => {
      this.beforeTransactionSelection === null && this.prosemirrorView != null && (this.beforeTransactionSelection = Ui(
        this,
        this.prosemirrorView.state
      ));
    }, this.afterAllTransactions = () => {
      this.beforeTransactionSelection = null;
    }, this._domSelectionInView = null;
  }
  /**
   * Create a transaction for changing the prosemirror state.
   *
   * @returns
   */
  get _tr() {
    return this.prosemirrorView.state.tr.setMeta("addToHistory", !1);
  }
  _isLocalCursorInView() {
    return this.prosemirrorView.hasFocus() ? (to && this._domSelectionInView === null && (xo(0, () => {
      this._domSelectionInView = null;
    }), this._domSelectionInView = this._isDomSelectionInView()), this._domSelectionInView) : !1;
  }
  _isDomSelectionInView() {
    const t = this.prosemirrorView._root.getSelection();
    if (t == null || t.anchorNode == null) return !1;
    const n = this.prosemirrorView._root.createRange();
    n.setStart(t.anchorNode, t.anchorOffset), n.setEnd(t.focusNode, t.focusOffset), n.getClientRects().length === 0 && n.startContainer && n.collapsed && n.selectNodeContents(n.startContainer);
    const s = n.getBoundingClientRect(), r = Qe.documentElement;
    return s.bottom >= 0 && s.right >= 0 && s.left <= (window.innerWidth || r.clientWidth || 0) && s.top <= (window.innerHeight || r.clientHeight || 0);
  }
  /**
   * @param {Y.Snapshot} snapshot
   * @param {Y.Snapshot} prevSnapshot
   */
  renderSnapshot(t, n) {
    n || (n = Oo(To(), /* @__PURE__ */ new Map())), this.prosemirrorView.dispatch(
      this._tr.setMeta(B, { snapshot: t, prevSnapshot: n })
    );
  }
  unrenderSnapshot() {
    this.mapping.clear(), this.mux(() => {
      const t = this.type.toArray().map(
        (i) => gn(
          /** @type {Y.XmlElement} */
          i,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((i) => i !== null), n = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new bt($.from(t), 0, 0)
      );
      n.setMeta(B, { snapshot: null, prevSnapshot: null }), this.prosemirrorView.dispatch(n);
    });
  }
  _forceRerender() {
    this.mapping.clear(), this.mux(() => {
      const t = this.beforeTransactionSelection !== null ? null : this.prosemirrorView.state.selection, n = this.type.toArray().map(
        (s) => gn(
          /** @type {Y.XmlElement} */
          s,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((s) => s !== null), i = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new bt($.from(n), 0, 0)
      );
      if (t) {
        const s = fe(Ht(t.anchor, 0), i.doc.content.size), r = fe(Ht(t.head, 0), i.doc.content.size);
        i.setSelection(Ie.create(i.doc, s, r));
      }
      this.prosemirrorView.dispatch(
        i.setMeta(B, { isChangeOrigin: !0, binding: this })
      );
    });
  }
  /**
   * @param {Y.Snapshot|Uint8Array} snapshot
   * @param {Y.Snapshot|Uint8Array} prevSnapshot
   * @param {Object} pluginState
   */
  _renderSnapshot(t, n, i) {
    let s = this.doc, r = this.type;
    if (t || (t = di(this.doc)), t instanceof Uint8Array || n instanceof Uint8Array)
      if ((!(t instanceof Uint8Array) || !(n instanceof Uint8Array)) && lt(), s = new ie({ gc: !1 }), Oi(s, n), n = di(s), Oi(s, t), t = di(s), r._item === null) {
        const o = Array.from(this.doc.share.keys()).find(
          (l) => this.doc.share.get(l) === this.type
        );
        r = s.getXmlFragment(o);
      } else {
        const o = s.store.clients.get(r._item.id.client) ?? [], l = kt(
          o,
          r._item.id.clock
        );
        r = /** @type {Y.XmlFragment} */
        /** @type {Y.ContentType} */
        /** @type {Y.Item} */
        o[l].content.type;
      }
    this.mapping.clear(), this.mux(() => {
      s.transact((o) => {
        const l = i.permanentUserData;
        l && l.dss.forEach((h) => {
          Ft(o, h, (u) => {
          });
        });
        const c = (h, u) => {
          const f = h === "added" ? l.getUserByClientId(u.client) : l.getUserByDeletedId(u);
          return {
            user: f,
            type: h,
            color: qu(
              i.colorMapping,
              i.colors,
              f
            )
          };
        }, a = Ho(
          r,
          new us(n.ds, t.sv)
        ).map((h) => !h._item.deleted || Mn(h._item, t) || Mn(h._item, n) ? gn(
          h,
          this.prosemirrorView.state.schema,
          { mapping: /* @__PURE__ */ new Map(), isOMark: /* @__PURE__ */ new Map() },
          t,
          n,
          c
        ) : null).filter((h) => h !== null), d = this._tr.replace(
          0,
          this.prosemirrorView.state.doc.content.size,
          new bt($.from(a), 0, 0)
        );
        this.prosemirrorView.dispatch(
          d.setMeta(B, { isChangeOrigin: !0 })
        );
      }, B);
    });
  }
  /**
   * @param {Array<Y.YEvent<any>>} events
   * @param {Y.Transaction} transaction
   */
  _typeChanged(t, n) {
    if (this.prosemirrorView == null) return;
    const i = B.getState(this.prosemirrorView.state);
    if (t.length === 0 || i.snapshot != null || i.prevSnapshot != null) {
      this.renderSnapshot(i.snapshot, i.prevSnapshot);
      return;
    }
    this.mux(() => {
      const s = (l, c) => this.mapping.delete(c);
      Ft(
        n,
        n.deleteSet,
        (l) => {
          if (l.constructor === N) {
            const c = (
              /** @type {Y.ContentType} */
              /** @type {Y.Item} */
              l.content.type
            );
            c && this.mapping.delete(c);
          }
        }
      ), n.changed.forEach(s), n.changedParentTypes.forEach(s);
      const r = this.type.toArray().map(
        (l) => al(
          /** @type {Y.XmlElement | Y.XmlHook} */
          l,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((l) => l !== null);
      let o = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new bt($.from(r), 0, 0)
      );
      Xu(o, this.beforeTransactionSelection, this), o = o.setMeta(B, { isChangeOrigin: !0, isUndoRedoOperation: n.origin instanceof Uo }), this.beforeTransactionSelection !== null && this._isLocalCursorInView() && o.scrollIntoView(), this.prosemirrorView.dispatch(o);
    });
  }
  /**
   * @param {import('prosemirror-model').Node} doc
   */
  _prosemirrorChanged(t) {
    this.doc.transact(() => {
      Bi(this.doc, this.type, t, this), this.beforeTransactionSelection = Ui(
        this,
        this.prosemirrorView.state
      );
    }, B);
  }
  /**
   * View is ready to listen to changes. Register observers.
   * @param {any} prosemirrorView
   */
  initView(t) {
    this.prosemirrorView != null && this.destroy(), this.prosemirrorView = t, this.doc.on("beforeAllTransactions", this.beforeAllTransactions), this.doc.on("afterAllTransactions", this.afterAllTransactions), this.type.observeDeep(this._observeFunction);
  }
  destroy() {
    this.prosemirrorView != null && (this.prosemirrorView = null, this.type.unobserveDeep(this._observeFunction), this.doc.off("beforeAllTransactions", this.beforeAllTransactions), this.doc.off("afterAllTransactions", this.afterAllTransactions));
  }
}
const al = (e, t, n, i, s, r) => {
  const o = (
    /** @type {PModel.Node} */
    n.mapping.get(e)
  );
  if (o === void 0) {
    if (e instanceof rt)
      return gn(
        e,
        t,
        n,
        i,
        s,
        r
      );
    throw xt();
  }
  return o;
}, gn = (e, t, n, i, s, r) => {
  const o = [], l = (c) => {
    var a, d;
    if (c instanceof rt) {
      const h = al(
        c,
        t,
        n,
        i,
        s,
        r
      );
      h !== null && o.push(h);
    } else {
      const h = (
        /** @type {Y.ContentType} */
        (d = (a = c._item.right) == null ? void 0 : a.content) == null ? void 0 : d.type
      );
      h instanceof Kt && !h._item.deleted && h._item.id.client === h.doc.clientID && (c.applyDelta([
        { retain: c.length },
        ...h.toDelta()
      ]), h.doc.transact((f) => {
        h._item.delete(f);
      }));
      const u = ef(
        c,
        t,
        n,
        i,
        s,
        r
      );
      u !== null && u.forEach((f) => {
        f !== null && o.push(f);
      });
    }
  };
  i === void 0 || s === void 0 ? e.toArray().forEach(l) : Ho(e, new us(s.ds, i.sv)).forEach(l);
  try {
    const c = e.getAttributes(i);
    i !== void 0 && (Mn(
      /** @type {Y.Item} */
      e._item,
      i
    ) ? Mn(
      /** @type {Y.Item} */
      e._item,
      s
    ) || (c.ychange = r ? r(
      "added",
      /** @type {Y.Item} */
      e._item.id
    ) : { type: "added" }) : c.ychange = r ? r(
      "removed",
      /** @type {Y.Item} */
      e._item.id
    ) : { type: "removed" });
    const a = t.node(e.nodeName, c, o);
    return n.mapping.set(e, a), a;
  } catch {
    return e.doc.transact((a) => {
      e._item.delete(a);
    }, B), n.mapping.delete(e), null;
  }
}, ef = (e, t, n, i, s, r) => {
  const o = [], l = e.toDelta(i, s, r);
  try {
    for (let c = 0; c < l.length; c++) {
      const a = l[c];
      o.push(t.text(a.insert, cf(a.attributes, t)));
    }
  } catch {
    return e.doc.transact((a) => {
      e._item.delete(a);
    }, B), null;
  }
  return o;
}, nf = (e, t) => {
  const n = new ft(), i = e.map((s) => ({
    // @ts-ignore
    insert: s.text,
    attributes: ul(s.marks, t)
  }));
  return n.applyDelta(i), t.mapping.set(n, e), n;
}, sf = (e, t) => {
  const n = new rt(e.type.name);
  for (const i in e.attrs) {
    const s = e.attrs[i];
    s !== null && i !== "ychange" && n.setAttribute(i, s);
  }
  return n.insert(
    0,
    Jn(e).map(
      (i) => Pi(i, t)
    )
  ), t.mapping.set(n, e), n;
}, Pi = (e, t) => e instanceof Array ? nf(e, t) : sf(e, t), cr = (e) => typeof e == "object" && e !== null, bs = (e, t) => {
  const n = Object.keys(e).filter((s) => e[s] !== null);
  let i = n.length === Object.keys(t).filter((s) => t[s] !== null).length;
  for (let s = 0; s < n.length && i; s++) {
    const r = n[s], o = e[r], l = t[r];
    i = r === "ychange" || o === l || cr(o) && cr(l) && bs(o, l);
  }
  return i;
}, Jn = (e) => {
  const t = e.content.content, n = [];
  for (let i = 0; i < t.length; i++) {
    const s = t[i];
    if (s.isText) {
      const r = [];
      for (let o = t[i]; i < t.length && o.isText; o = t[++i])
        r.push(o);
      i--, n.push(r);
    } else
      n.push(s);
  }
  return n;
}, hl = (e, t) => {
  const n = e.toDelta();
  return n.length === t.length && n.every(
    /** @type {(d:any,i:number) => boolean} */
    (i, s) => i.insert === /** @type {any} */
    t[s].text && Zr(i.attributes || {}).length === t[s].marks.length && be(i.attributes, (r, o) => {
      var d;
      const l = dl(o), c = t[s].marks;
      return c.find(
        /** @param {any} mark */
        (h) => h.type.name === l
      ) ? bs(r, (d = c.find(
        /** @param {any} mark */
        (h) => h.type.name === l
      )) == null ? void 0 : d.attrs) : !1;
    })
  );
}, He = (e, t) => {
  if (e instanceof rt && !(t instanceof Array) && $i(e, t)) {
    const n = Jn(t);
    return e._length === n.length && bs(e.getAttributes(), t.attrs) && e.toArray().every(
      (i, s) => He(i, n[s])
    );
  }
  return e instanceof ft && t instanceof Array && hl(e, t);
}, Rn = (e, t) => e === t || e instanceof Array && t instanceof Array && e.length === t.length && e.every(
  (n, i) => t[i] === n
), ar = (e, t, n) => {
  const i = e.toArray(), s = Jn(t), r = s.length, o = i.length, l = fe(o, r);
  let c = 0, a = 0, d = !1;
  for (; c < l; c++) {
    const h = i[c], u = s[c];
    if (Rn(n.mapping.get(h), u))
      d = !0;
    else if (!He(h, u))
      break;
  }
  for (; c + a < l; a++) {
    const h = i[o - a - 1], u = s[r - a - 1];
    if (Rn(n.mapping.get(h), u))
      d = !0;
    else if (!He(h, u))
      break;
  }
  return {
    equalityFactor: c + a,
    foundMappedChild: d
  };
}, rf = (e) => {
  let t = "", n = e._start;
  const i = {};
  for (; n !== null; )
    n.deleted || (n.countable && n.content instanceof Et ? t += n.content.str : n.content instanceof F && (i[n.content.key] = null)), n = n.right;
  return {
    str: t,
    nAttrs: i
  };
}, of = (e, t, n) => {
  n.mapping.set(e, t);
  const { nAttrs: i, str: s } = rf(e), r = t.map((a) => ({
    insert: (
      /** @type {any} */
      a.text
    ),
    attributes: Object.assign({}, i, ul(a.marks, n))
  })), { insert: o, remove: l, index: c } = Pu(
    s,
    r.map((a) => a.insert).join("")
  );
  e.delete(c, l), e.insert(c, o), e.applyDelta(
    r.map((a) => ({ retain: a.insert.length, attributes: a.attributes }))
  );
}, lf = /(.*)(--[a-zA-Z0-9+/=]{8})$/, dl = (e) => {
  var t;
  return ((t = lf.exec(e)) == null ? void 0 : t[1]) ?? e;
}, cf = (e, t) => {
  const n = [];
  for (const i in e)
    n.push(t.mark(dl(i), e[i]));
  return n;
}, ul = (e, t) => {
  const n = {};
  return e.forEach((i) => {
    if (i.type.name !== "ychange") {
      const s = Ut(t.isOMark, i.type, () => !i.type.excludes(i.type));
      n[s ? `${i.type.name}--${Gu(i.toJSON())}` : i.type.name] = i.attrs;
    }
  }), n;
}, Bi = (e, t, n, i) => {
  if (t instanceof rt && t.nodeName !== n.type.name)
    throw new Error("node name mismatch!");
  if (i.mapping.set(t, n), t instanceof rt) {
    const h = t.getAttributes(), u = n.attrs;
    for (const f in u)
      u[f] !== null ? h[f] !== u[f] && f !== "ychange" && t.setAttribute(f, u[f]) : t.removeAttribute(f);
    for (const f in h)
      u[f] === void 0 && t.removeAttribute(f);
  }
  const s = Jn(n), r = s.length, o = t.toArray(), l = o.length, c = fe(r, l);
  let a = 0, d = 0;
  for (; a < c; a++) {
    const h = o[a], u = s[a];
    if (!Rn(i.mapping.get(h), u))
      if (He(h, u))
        i.mapping.set(h, u);
      else
        break;
  }
  for (; d + a + 1 < c; d++) {
    const h = o[l - d - 1], u = s[r - d - 1];
    if (!Rn(i.mapping.get(h), u))
      if (He(h, u))
        i.mapping.set(h, u);
      else
        break;
  }
  e.transact(() => {
    for (; l - a - d > 0 && r - a - d > 0; ) {
      const u = o[a], f = s[a], g = o[l - d - 1], m = s[r - d - 1];
      if (u instanceof ft && f instanceof Array)
        hl(u, f) || of(u, f, i), a += 1;
      else {
        let w = u instanceof rt && $i(u, f), b = g instanceof rt && $i(g, m);
        if (w && b) {
          const C = ar(
            /** @type {Y.XmlElement} */
            u,
            /** @type {PModel.Node} */
            f,
            i
          ), y = ar(
            /** @type {Y.XmlElement} */
            g,
            /** @type {PModel.Node} */
            m,
            i
          );
          C.foundMappedChild && !y.foundMappedChild ? b = !1 : !C.foundMappedChild && y.foundMappedChild || C.equalityFactor < y.equalityFactor ? w = !1 : b = !1;
        }
        w ? (Bi(
          e,
          /** @type {Y.XmlFragment} */
          u,
          /** @type {PModel.Node} */
          f,
          i
        ), a += 1) : b ? (Bi(
          e,
          /** @type {Y.XmlFragment} */
          g,
          /** @type {PModel.Node} */
          m,
          i
        ), d += 1) : (i.mapping.delete(t.get(a)), t.delete(a, 1), t.insert(a, [
          Pi(f, i)
        ]), a += 1);
      }
    }
    const h = l - a - d;
    if (l === 1 && r === 0 && o[0] instanceof ft ? (i.mapping.delete(o[0]), o[0].delete(0, o[0].length)) : h > 0 && (t.slice(a, a + h).forEach((u) => i.mapping.delete(u)), t.delete(a, h)), a + d < r) {
      const u = [];
      for (let f = a; f < r - d; f++)
        u.push(Pi(s[f], i));
      t.insert(a, u);
    }
  }, B);
}, $i = (e, t) => !(t instanceof Array) && e.nodeName === t.type.name, Nn = (e, t, n) => {
  if (e === 0)
    return hi(t, 0, -1);
  let i = t._first === null ? null : (
    /** @type {Y.ContentType} */
    t._first.content.type
  );
  for (; i !== null && t !== i; ) {
    if (i instanceof ft) {
      if (i._length >= e)
        return hi(i, e, -1);
      if (e -= i._length, i._item !== null && i._item.next !== null)
        i = /** @type {Y.ContentType} */
        i._item.next.content.type;
      else {
        do
          i = i._item === null ? null : i._item.parent, e--;
        while (i !== t && i !== null && i._item !== null && i._item.next === null);
        i !== null && i !== t && (i = i._item === null ? null : (
          /** @type {Y.ContentType} */
          /** @type Y.Item */
          i._item.next.content.type
        ));
      }
    } else {
      const s = (
        /** @type {any} */
        (n.get(i) || { nodeSize: 0 }).nodeSize
      );
      if (i._first !== null && e < s)
        i = /** @type {Y.ContentType} */
        i._first.content.type, e--;
      else {
        if (e === 1 && i._length === 0 && s > 1)
          return new _n(i._item === null ? null : i._item.id, i._item === null ? Pe(i) : null, null);
        if (e -= s, i._item !== null && i._item.next !== null)
          i = /** @type {Y.ContentType} */
          i._item.next.content.type;
        else {
          if (e === 0)
            return i = i._item === null ? i : i._item.parent, new _n(i._item === null ? null : i._item.id, i._item === null ? Pe(i) : null, null);
          do
            i = /** @type {Y.Item} */
            i._item.parent, e--;
          while (i !== t && /** @type {Y.Item} */
          i._item.next === null);
          i !== t && (i = /** @type {Y.ContentType} */
          /** @type {Y.Item} */
          /** @type {Y.Item} */
          i._item.next.content.type);
        }
      }
    }
    if (i === null)
      throw lt();
    if (e === 0 && i.constructor !== ft && i !== t)
      return af(i._item.parent, i._item);
  }
  return hi(t, t._length, -1);
}, af = (e, t) => {
  let n = null, i = null;
  return e._item === null ? i = Pe(e) : n = _(e._item.id.client, e._item.id.clock), new _n(n, i, t.id);
}, Yt = (e, t, n, i) => {
  const s = Rd(n, e);
  if (s === null || s.type !== t && !Be(t, s.type._item))
    return null;
  let r = s.type, o = 0;
  if (r.constructor === ft)
    o = s.index;
  else if (r._item === null || !r._item.deleted) {
    let l = r._first, c = 0;
    for (; c < r._length && c < s.index && l !== null; ) {
      if (!l.deleted) {
        const a = (
          /** @type {Y.ContentType} */
          l.content.type
        );
        if (c++, a instanceof ft)
          o += a._length;
        else {
          const d = i.get(a);
          if (d == null)
            return null;
          o += /** @type {any} */
          d.nodeSize;
        }
      }
      l = /** @type {Y.Item} */
      l.right;
    }
    o += 1;
  }
  for (; r !== t && r._item !== null; ) {
    const l = r._item.parent;
    if (l._item === null || !l._item.deleted) {
      o += 1;
      let c = (
        /** @type {Y.AbstractType} */
        l._first
      );
      for (; c !== null; ) {
        const a = (
          /** @type {Y.ContentType} */
          c.content.type
        );
        if (a === r)
          break;
        if (!c.deleted)
          if (a instanceof ft)
            o += a._length;
          else {
            const d = i.get(a);
            if (d == null)
              return null;
            o += /** @type {any} */
            d.nodeSize;
          }
        c = c.right;
      }
    }
    r = /** @type {Y.AbstractType} */
    l;
  }
  return o - 1;
}, hf = (e) => {
  const t = Lt.getState(e).undoManager;
  if (t != null)
    return t.undo(), !0;
}, df = (e) => {
  const t = Lt.getState(e).undoManager;
  if (t != null)
    return t.redo(), !0;
}, uf = /* @__PURE__ */ new Set(["paragraph"]), ff = (e, t) => !(e instanceof N) || !(e.content instanceof _t) || !(e.content.type instanceof Kt || e.content.type instanceof rt && t.has(e.content.type.nodeName)) || e.content.type._length === 0, pf = ({ protectedNodes: e = uf, trackedOrigins: t = [], undoManager: n = null } = {}) => new vr({
  key: Lt,
  state: {
    init: (i, s) => {
      const r = B.getState(s);
      let o = n;
      if (!o) {
        const l = r.doc, c = new Set(l ? l._observers.get("destroy") : []);
        o = new Uo(r.type, {
          trackedOrigins: new Set([B].concat(t)),
          deleteFilter: (d) => ff(d, e),
          captureTransaction: (d) => d.meta.get("addToHistory") !== !1
        });
        const a = l ? l._observers.get("destroy") : /* @__PURE__ */ new Set();
        o._yTiptapDocDestroyListeners = Array.from(a || []).filter((d) => !c.has(d));
      }
      return {
        undoManager: o,
        prevSel: null,
        hasUndoOps: o.undoStack.length > 0,
        hasRedoOps: o.redoStack.length > 0
      };
    },
    /**
     * @returns {any}
     */
    apply: (i, s, r, o) => {
      const l = B.getState(o).binding, c = s.undoManager, a = c.undoStack.length > 0, d = c.redoStack.length > 0;
      return l ? {
        undoManager: c,
        prevSel: Ui(l, r),
        hasUndoOps: a,
        hasRedoOps: d
      } : a !== s.hasUndoOps || d !== s.hasRedoOps ? Object.assign({}, s, {
        hasUndoOps: c.undoStack.length > 0,
        hasRedoOps: c.redoStack.length > 0
      }) : s;
    }
  },
  view: (i) => {
    const s = B.getState(i.state), r = Lt.getState(i.state).undoManager;
    return r.on("stack-item-added", ({ stackItem: o }) => {
      const l = s.binding;
      l && o.meta.set(l, Lt.getState(i.state).prevSel);
    }), r.on("stack-item-popped", ({ stackItem: o }) => {
      const l = s.binding;
      l && (l.beforeTransactionSelection = o.meta.get(l) || l.beforeTransactionSelection);
    }), {
      destroy: () => {
        r.destroy();
        const o = r._yTiptapDocDestroyListeners;
        o && r.doc && (o.forEach((l) => r.doc.off("destroy", l)), r._yTiptapDocDestroyListeners = null);
      }
    };
  }
});
function xs(e) {
  return !!e.getMeta(B);
}
function gf(e, t) {
  const n = B.getState(e);
  return Yt(
    n.doc,
    n.type,
    t,
    n.binding.mapping
  ) || 0;
}
function fl(e, t) {
  const n = B.getState(e);
  return Nn(t, n.type, n.binding.mapping);
}
var mn = class pl extends Ml {
  constructor(t, n) {
    super(t), this.yRelativePosition = n;
  }
  /**
   * Creates a CollaborationMappablePosition from a JSON object.
   */
  static fromJSON(t) {
    return new pl(t.position, t.yRelativePosition);
  }
  /**
   * Converts the CollaborationMappablePosition to a JSON object.
   */
  toJSON() {
    return {
      position: this.position,
      yRelativePosition: this.yRelativePosition
    };
  }
};
function mf(e, t) {
  const n = fl(t, e);
  return new mn(e, n);
}
function wf(e, t, n) {
  const i = e instanceof mn ? e.yRelativePosition : null;
  if (xs(t) && i) {
    const o = gf(n, i);
    return {
      position: new mn(o, i),
      mapResult: null
    };
  }
  const s = Ol(e, t), r = s.position.position;
  return {
    position: new mn(
      r,
      i ?? fl(n, r)
    ),
    mapResult: s.mapResult
  };
}
Fe.create({
  name: "collaboration",
  priority: 1e3,
  addOptions() {
    return {
      document: null,
      field: "default",
      fragment: null,
      provider: null
    };
  },
  addStorage() {
    return {
      isDisabled: !1
    };
  },
  onCreate() {
    this.editor.extensionManager.extensions.find((e) => e.name === "undoRedo") && console.warn(
      '[tiptap warn]: "@tiptap/extension-collaboration" comes with its own history support and is not compatible with "@tiptap/extension-undo-redo".'
    );
  },
  onBeforeCreate() {
    this.editor.utils.getUpdatedPosition = (e, t) => wf(e, t, this.editor.state), this.editor.utils.createMappablePosition = (e) => mf(e, this.editor.state);
  },
  addCommands() {
    return {
      undo: () => ({ tr: e, state: t, dispatch: n }) => (e.setMeta("preventDispatch", !0), Lt.getState(t).undoManager.undoStack.length === 0 ? !1 : n ? hf(t) : !0),
      redo: () => ({ tr: e, state: t, dispatch: n }) => (e.setMeta("preventDispatch", !0), Lt.getState(t).undoManager.redoStack.length === 0 ? !1 : n ? df(t) : !0)
    };
  },
  addKeyboardShortcuts() {
    return {
      "Mod-z": () => this.editor.commands.undo(),
      "Mod-y": () => this.editor.commands.redo(),
      "Shift-Mod-z": () => this.editor.commands.redo()
    };
  },
  addProseMirrorPlugins() {
    const e = this.options.fragment ? this.options.fragment : this.options.document.getXmlFragment(this.options.field), t = pf(this.options.yUndoOptions), n = t.spec.view;
    t.spec.view = (r) => {
      const { undoManager: o } = Lt.getState(r.state);
      o.restore && (o.restore(), o.restore = () => {
      });
      const l = n ? n(r) : void 0;
      return {
        destroy: () => {
          const c = o.trackedOrigins.has(o), a = o._observers;
          o.restore = () => {
            c && o.trackedOrigins.add(o), o.doc.on("afterTransaction", o.afterTransactionHandler), o._observers = a;
          }, l != null && l.destroy && l.destroy();
        }
      };
    };
    const i = {
      ...this.options.ySyncOptions,
      onFirstRender: this.options.onFirstRender
    };
    return [
      Ju(e, i),
      t,
      // Only add the filterInvalidContent plugin if content checking is enabled
      this.editor.options.enableContentCheck && new je({
        key: new te("filterInvalidContent"),
        filterTransaction: (r) => {
          if (!xs(r))
            return !0;
          if (this.storage.isDisabled)
            return !1;
          if (!r.docChanged)
            return !0;
          try {
            return r.doc.check(), !0;
          } catch (o) {
            return this.storage.isDisabled = !0, this.editor.emit("contentError", {
              error: o,
              editor: this.editor,
              disableCollaboration: () => {
                var l;
                (l = e.doc) == null || l.destroy();
              }
            }), !0;
          }
        }
      })
    ].filter(Boolean);
  }
});
function hr(e) {
  if (!e.length)
    return ks.empty;
  const t = [], n = e[0].$from.node(0);
  return e.forEach((i) => {
    const s = i.$from.pos, r = i.$from.nodeAfter;
    r && t.push(
      oc.node(s, s + r.nodeSize, {
        class: "ProseMirror-selectednoderange"
      })
    );
  }), ks.create(n, t);
}
function bf(e, t, n) {
  const i = n.isText || n.isAtom ? 0 : 1;
  return {
    start: e + i,
    end: e + t - i
  };
}
function Xn(e, t, n, i = {}) {
  const s = [], r = e.node(0), { extendOnBoundaryOverlap: o = !0 } = i;
  typeof n == "number" && n >= 0 || (e.sameParent(t) ? n = Math.max(0, e.sharedDepth(t.pos) - 1) : n = e.sharedDepth(t.pos));
  const l = new lc(e, t, n), c = l.depth === 0 ? 0 : r.resolve(l.start).posAtIndex(0);
  return l.parent.forEach((a, d) => {
    const h = c + d, u = h + a.nodeSize, f = bf(h, a.nodeSize, a), g = o ? t.pos >= f.start && e.pos <= f.end : t.pos > f.start && e.pos < f.end;
    if (h < l.start || h >= l.end || !g)
      return;
    const m = new Ll(r.resolve(h), r.resolve(u));
    s.push(m);
  }), s;
}
var xf = class gl {
  constructor(t, n, i) {
    this.anchor = t, this.head = n, this.depth = i ?? 0;
  }
  map(t) {
    return new gl(t.map(this.anchor), t.map(this.head), this.depth);
  }
  resolve(t) {
    const n = t.resolve(this.anchor), i = t.resolve(this.head);
    return new Dt(n, i, this.depth);
  }
}, Dt = class Bt extends Ir {
  constructor(t, n, i, s = 1) {
    const { doc: r } = t, o = t === n, l = t.pos === r.content.size && n.pos === r.content.size, c = o && !l ? r.resolve(n.pos + (s > 0 ? 1 : -1)) : n, a = o && l ? r.resolve(t.pos - (s > 0 ? 1 : -1)) : t, d = Xn(
      a.min(c),
      a.max(c),
      i
    ), h = c.pos >= t.pos ? d[0].$from : d[d.length - 1].$to, u = c.pos >= t.pos ? d[d.length - 1].$to : d[0].$from;
    super(h, u, d), this.depth = i;
  }
  // we can safely ignore this TypeScript error: https://github.com/Microsoft/TypeScript/issues/338
  // @ts-ignore
  get $to() {
    return this.ranges[this.ranges.length - 1].$to;
  }
  eq(t) {
    return t instanceof Bt && t.$from.pos === this.$from.pos && t.$to.pos === this.$to.pos;
  }
  map(t, n) {
    const i = t.resolve(n.map(this.anchor)), s = t.resolve(n.map(this.head));
    return new Bt(i, s, this.depth);
  }
  toJSON() {
    return {
      type: "nodeRange",
      anchor: this.anchor,
      head: this.head,
      depth: this.depth
    };
  }
  get isForwards() {
    return this.head >= this.anchor;
  }
  get isBackwards() {
    return !this.isForwards;
  }
  extendBackwards() {
    const { doc: t } = this.$from;
    if (this.isForwards && this.ranges.length > 1) {
      const s = this.ranges.slice(0, -1), r = s[0].$from, o = s[s.length - 1].$to;
      return new Bt(r, o, this.depth);
    }
    const n = this.ranges[0], i = t.resolve(Math.max(0, n.$from.pos - 1));
    return new Bt(this.$anchor, i, this.depth);
  }
  extendForwards() {
    const { doc: t } = this.$from;
    if (this.isBackwards && this.ranges.length > 1) {
      const s = this.ranges.slice(1), r = s[0].$from, o = s[s.length - 1].$to;
      return new Bt(o, r, this.depth);
    }
    const n = this.ranges[this.ranges.length - 1], i = t.resolve(Math.min(t.content.size, n.$to.pos + 1));
    return new Bt(this.$anchor, i, this.depth);
  }
  static fromJSON(t, n) {
    return new Bt(t.resolve(n.anchor), t.resolve(n.head), n.depth);
  }
  static create(t, n, i, s, r = 1) {
    return new this(t.resolve(n), t.resolve(i), s, r);
  }
  getBookmark() {
    return new xf(this.anchor, this.head, this.depth);
  }
};
Dt.prototype.visible = !1;
try {
  Ir.jsonID("nodeRange", Dt);
} catch {
}
function De(e) {
  return e instanceof Dt;
}
Fe.create({
  name: "nodeRange",
  addOptions() {
    return {
      depth: void 0,
      key: "Mod"
    };
  },
  addKeyboardShortcuts() {
    return {
      // extend NodeRangeSelection upwards
      "Shift-ArrowUp": ({ editor: e }) => {
        const { depth: t } = this.options, { view: n, state: i } = e, { doc: s, selection: r, tr: o } = i, { anchor: l, head: c } = r;
        if (!De(r)) {
          const d = Dt.create(s, l, c, t, -1);
          return o.setSelection(d), n.dispatch(o), !0;
        }
        const a = r.extendBackwards();
        return o.setSelection(a), n.dispatch(o), !0;
      },
      // extend NodeRangeSelection downwards
      "Shift-ArrowDown": ({ editor: e }) => {
        const { depth: t } = this.options, { view: n, state: i } = e, { doc: s, selection: r, tr: o } = i, { anchor: l, head: c } = r;
        if (!De(r)) {
          const d = Dt.create(s, l, c, t);
          return o.setSelection(d), n.dispatch(o), !0;
        }
        const a = r.extendForwards();
        return o.setSelection(a), n.dispatch(o), !0;
      },
      // add `NodeRangeSelection` to all nodes
      "Mod-a": ({ editor: e }) => {
        const { depth: t } = this.options, { view: n, state: i } = e, { doc: s, tr: r } = i, o = Dt.create(s, 0, s.content.size, t);
        return r.setSelection(o), n.dispatch(r), !0;
      }
    };
  },
  onSelectionUpdate() {
    const { selection: e } = this.editor.state;
    De(e) && this.editor.view.dom.classList.add("ProseMirror-noderangeselection");
  },
  addProseMirrorPlugins() {
    let e = !1, t = !1;
    return [
      new je({
        key: new te("nodeRange"),
        props: {
          attributes: () => e ? {
            class: "ProseMirror-noderangeselection"
          } : { class: "" },
          handleDOMEvents: {
            mousedown: (n, i) => {
              const { key: s } = this.options, r = /Mac/.test(navigator.platform), o = !!i.shiftKey, l = !!i.ctrlKey, c = !!i.altKey, a = !!i.metaKey, d = r ? a : l;
              return (s == null || s === "Shift" && o || s === "Control" && l || s === "Alt" && c || s === "Meta" && a || s === "Mod" && d) && (t = !0), t && document.addEventListener(
                "mouseup",
                () => {
                  t = !1;
                  const { state: h } = n, { doc: u, selection: f, tr: g } = h, { $anchor: m, $head: w } = f;
                  if (m.sameParent(w))
                    return;
                  const b = Dt.create(
                    u,
                    m.pos,
                    w.pos,
                    this.options.depth
                  );
                  g.setSelection(b), n.dispatch(g);
                },
                { once: !0 }
              ), !1;
            }
          },
          // when selecting some text we want to render some decorations
          // to preview a `NodeRangeSelection`
          decorations: (n) => {
            const { selection: i } = n, s = De(i);
            if (e = !1, !t)
              return s ? (e = !0, hr(i.ranges)) : null;
            const { $from: r, $to: o } = i;
            if (!s && r.sameParent(o))
              return null;
            const l = Xn(r, o, this.options.depth);
            return l.length ? (e = !0, hr(l)) : null;
          }
        }
      })
    ];
  }
});
function yf(e, t) {
  const n = getComputedStyle(e);
  if (t)
    return t.map((s) => s.trim()).filter((s) => s.length > 0).map((s) => `${s}:${n.getPropertyValue(s)};`).join("");
  let i = "";
  for (let s = 0; s < n.length; s += 1)
    i += `${n[s]}:${n.getPropertyValue(n[s])};`;
  return i;
}
function vf(e, t) {
  const n = e.cloneNode(!0), i = [e, ...Array.from(e.getElementsByTagName("*"))], s = [
    n,
    ...Array.from(n.getElementsByTagName("*"))
  ];
  return i.forEach((r, o) => {
    s[o].style.cssText = yf(r, t);
  }), n;
}
var Sf = {
  id: "listItemFirstChild",
  evaluate: ({ parent: e, isFirst: t }) => t && e && ["listItem", "taskItem"].includes(e.type.name) ? 1e3 : 0
}, Cf = {
  id: "listWrapperDeprioritize",
  evaluate: ({ node: e }) => {
    const t = ["listItem", "taskItem"], n = e.firstChild;
    return n && t.includes(n.type.name) ? 1e3 : 0;
  }
}, kf = {
  id: "tableStructure",
  evaluate: ({ node: e, parent: t }) => ["tableRow", "tableCell", "tableHeader"].includes(e.type.name) || t && t.type.name === "tableHeader" ? 1e3 : 0
}, Ef = {
  id: "inlineContent",
  evaluate: ({ node: e }) => e.isInline || e.isText ? 1e3 : 0
}, _f = [
  Sf,
  Cf,
  kf,
  Ef
], dr = {
  edges: ["left", "top"],
  threshold: 12,
  strength: 500
};
function gi(e) {
  return e === void 0 || e === "left" ? { ...dr } : e === "right" ? { edges: ["right", "top"], threshold: 12, strength: 500 } : e === "both" ? { edges: ["left", "right", "top"], threshold: 12, strength: 500 } : e === "none" ? { edges: [], threshold: 0, strength: 0 } : { ...dr, ...e };
}
function Tf(e, t, n) {
  if (n.edges.length === 0)
    return !1;
  const i = t.getBoundingClientRect(), { threshold: s, edges: r } = n;
  return r.some((o) => o === "left" ? e.x - i.left < s : o === "right" ? i.right - e.x < s : o === "top" ? e.y - i.top < s : o === "bottom" ? i.bottom - e.y < s : !1);
}
function Df(e, t, n, i) {
  return !t || n.edges.length === 0 ? 0 : Tf(e, t, n) ? n.strength * i : 0;
}
var Af = 1e3;
function ur(e, t, n, i) {
  let s = Af, r = !1;
  if (t.every((l) => {
    const c = l.evaluate(e);
    return s -= c, s <= 0 ? (r = !0, !1) : !0;
  }), r)
    return -1;
  const o = e.view.nodeDOM(e.pos);
  return s -= Df(i, o, n, e.depth), s <= 0 ? -1 : s;
}
function fr(e, t, n) {
  return Array.from({ length: t }, (s, r) => t - 1 - r).some((s) => n.includes(e.node(s).type.name));
}
function If(e, t, n) {
  if (!Number.isFinite(t.x) || !Number.isFinite(t.y))
    return null;
  const i = e.posAtCoords({ left: t.x, top: t.y });
  if (!i)
    return null;
  const { doc: s } = e.state, r = s.resolve(i.pos), o = [];
  n.defaultRules && o.push(..._f), o.push(...n.rules);
  const c = Array.from({ length: r.depth }, (h, u) => r.depth - u).map((h) => {
    const u = r.node(h), f = r.before(h);
    if (n.allowedContainers && h > 0 && !fr(r, h, n.allowedContainers))
      return null;
    const g = h > 0 ? r.node(h - 1) : null, m = h > 0 ? r.index(h - 1) : 0, w = g ? g.childCount : 1, b = {
      node: u,
      pos: f,
      depth: h,
      parent: g,
      index: m,
      isFirst: m === 0,
      isLast: m === w - 1,
      $pos: r,
      view: e
    }, C = ur(b, o, n.edgeDetection, t);
    if (C < 0)
      return null;
    const y = e.nodeDOM(f);
    return { node: u, pos: f, depth: h, score: C, dom: y };
  }).filter((h) => h !== null), a = r.nodeAfter;
  if (a && a.isAtom && !a.isInline) {
    const h = i.pos, u = r.depth + 1, f = r.parent, g = r.index(), m = f.childCount;
    let w = !0;
    if (n.allowedContainers && (w = fr(r, u, n.allowedContainers)), w) {
      const b = {
        node: a,
        pos: h,
        depth: u,
        parent: f,
        index: g,
        isFirst: g === 0,
        isLast: g === m - 1,
        $pos: r,
        view: e
      }, C = ur(b, o, n.edgeDetection, t);
      if (C >= 0) {
        const y = e.nodeDOM(h);
        y && c.push({ node: a, pos: h, depth: u, score: C, dom: y });
      }
    }
  }
  if (c.length === 0)
    return null;
  c.sort((h, u) => u.score !== h.score ? u.score - h.score : u.depth - h.depth);
  const d = c[0];
  return d.dom ? {
    node: d.node,
    pos: d.pos,
    dom: d.dom
  } : null;
}
function Of(e, t) {
  let n = e;
  for (; n != null && n.parentElement && n.parentElement !== t.dom; )
    n = n.parentElement;
  return (n == null ? void 0 : n.parentElement) === t.dom ? n : void 0;
}
function pr(e) {
  return Number.isFinite(e.top) && Number.isFinite(e.bottom) && Number.isFinite(e.left) && Number.isFinite(e.right) && e.width > 0 && e.height > 0;
}
function Mf(e, t, n, i = 5) {
  if (!Number.isFinite(t) || !Number.isFinite(n))
    return null;
  const s = e.dom, r = s.firstElementChild, o = s.lastElementChild;
  if (!r || !o)
    return null;
  const l = r.getBoundingClientRect(), c = o.getBoundingClientRect();
  if (!pr(l) || !pr(c))
    return null;
  const a = Math.min(Math.max(l.top + i, n), c.bottom - i), d = 0.5, h = Math.abs(l.left - c.left) < d, u = Math.abs(l.right - c.right) < d;
  let f = l;
  h && u && (f = l);
  const g = Math.min(Math.max(f.left + i, t), f.right - i);
  return !Number.isFinite(g) || !Number.isFinite(a) ? null : { x: g, y: a };
}
var ml = (e) => {
  const { x: t, y: n, editor: i, nestedOptions: s } = e, { view: r, state: o } = i, l = Mf(r, t, n, 5);
  if (!l)
    return { resultElement: null, resultNode: null, pos: null };
  const { x: c, y: a } = l;
  if (s != null && s.enabled) {
    const g = If(r, { x: c, y: a }, s);
    return g ? {
      resultElement: g.dom,
      resultNode: g.node,
      pos: g.pos
    } : { resultElement: null, resultNode: null, pos: null };
  }
  const d = r.root.elementsFromPoint(c, a);
  let h;
  if (Array.prototype.some.call(d, (g) => {
    if (!r.dom.contains(g))
      return !1;
    const m = Of(g, r);
    return m ? (h = m, !0) : !1;
  }), !h) {
    const g = r.posAtCoords({ left: c, top: a });
    if (g) {
      const m = o.doc.resolve(g.pos), w = Math.min(m.depth, 1), b = w > 0 ? m.before(w) : m.pos, C = o.doc.nodeAt(b);
      if (C) {
        const y = r.nodeDOM(b);
        return {
          resultElement: y instanceof HTMLElement ? y : null,
          resultNode: C,
          pos: b
        };
      }
    }
    return { resultElement: null, resultNode: null, pos: null };
  }
  let u;
  try {
    u = r.posAtDOM(h, 0);
  } catch {
    return { resultElement: null, resultNode: null, pos: null };
  }
  const f = o.doc.nodeAt(u);
  if (!f) {
    const g = o.doc.resolve(u), m = g.parent;
    return {
      resultElement: h,
      resultNode: m,
      pos: g.start()
    };
  }
  return {
    resultElement: h,
    resultNode: f,
    pos: u
  };
};
function wl(e, t) {
  const n = e.nodeDOM(t);
  if (n instanceof Element && n !== e.dom)
    return n;
  const { node: i, offset: s } = e.domAtPos(t), r = i.childNodes[s];
  return r instanceof Element ? r : i instanceof Element ? i : i.nodeType === Node.TEXT_NODE && i.parentElement ? i.parentElement : null;
}
function Rf(e, t) {
  const n = wl(e, t);
  return (n ? getComputedStyle(n).direction : getComputedStyle(e.dom).direction) || "ltr";
}
function bl(e) {
  var t;
  (t = e.parentNode) == null || t.removeChild(e);
}
function Nf(e, t) {
  return e === "rtl" ? t : 0;
}
function Lf(e) {
  return e ? !e.some((t) => {
    const n = t.trim().toLowerCase();
    return n === "margin" || n.startsWith("margin-");
  }) : !0;
}
function Uf(e, t, n, i) {
  const { doc: s } = t.view.state;
  if (n != null && n.enabled && (i != null && i.node) && i.pos >= 0) {
    const a = i.pos, d = i.pos + i.node.nodeSize;
    return [
      {
        $from: s.resolve(a),
        $to: s.resolve(d)
      }
    ];
  }
  const r = ml({
    editor: t,
    x: e.clientX,
    y: e.clientY,
    nestedOptions: n
  });
  if (!r.resultNode || r.pos === null)
    return [];
  const o = r.resultNode.isText || r.resultNode.isAtom ? 0 : -1, l = s.resolve(r.pos), c = s.resolve(r.pos + r.resultNode.nodeSize + o);
  return Xn(l, c, 0, { extendOnBoundaryOverlap: !1 });
}
function Pf(e, t, n, i, s) {
  const { view: r } = t;
  if (!e.dataTransfer)
    return;
  const { empty: o, $from: l, $to: c } = r.state.selection, a = Uf(e, t, n, i), d = Xn(l, c, 0, { extendOnBoundaryOverlap: !1 }), h = d.some((k) => a.find((D) => D.$from === k.$from && D.$to === k.$to)), u = o || !h ? a : d;
  if (!u.length)
    return;
  const { tr: f } = r.state, g = document.createElement("div"), m = u[0].$from.pos, w = u[u.length - 1].$to.pos, b = Rf(r, m);
  g.setAttribute("dir", b);
  const C = (n == null ? void 0 : n.enabled) && (i == null ? void 0 : i.node), y = u.length === 1;
  let T, z;
  C && y ? (T = r.state.doc.slice(m, w), z = wi.create(r.state.doc, m)) : (z = Dt.create(r.state.doc, m, w), T = z.content());
  const I = Lf(s);
  u.forEach((k) => {
    const D = wl(r, k.$from.pos);
    if (!D)
      return;
    const S = vf(D, s);
    I && (S.style.margin = "0"), g.append(S);
  }), g.style.position = "absolute", g.style.top = "-10000px", document.body.append(g), e.dataTransfer.clearData();
  const E = g.getBoundingClientRect(), O = Nf(b, E.width);
  e.dataTransfer.setDragImage(g, O, 0);
  let Q = !1;
  const st = () => {
    Q || (Q = !0, bl(g), document.removeEventListener("drop", st), document.removeEventListener("dragend", st));
  }, gt = z instanceof wi ? z : void 0;
  r.dragging = { slice: T, move: !0, node: gt }, f.setSelection(z), r.dispatch(f), document.addEventListener("drop", st), document.addEventListener("dragend", st);
}
function gr(e, t, n) {
  let i = 0;
  for (let s = t; s < n; s += 1)
    i += e.child(s).nodeSize;
  return i;
}
function Bf(e) {
  var t;
  return De(e) ? {
    anchorPos: e.from,
    nodeCount: e.ranges.length,
    depth: (t = e.depth) != null ? t : 0
  } : null;
}
function $f(e, t, n, i) {
  const s = e.resolve(t), r = s.node(i);
  let o = s.index(i);
  o >= r.childCount && (o = Math.max(0, r.childCount - n));
  const l = Math.min(n, r.childCount - o);
  if (l <= 0)
    return null;
  const c = s.start(i) + gr(r, 0, o), a = c + gr(r, o, o + l);
  return { anchor: c, head: a, count: l };
}
function zf(e, t, n, i) {
  try {
    const s = $f(e, t, n, i);
    if (!s)
      return null;
    const r = Dt.create(e, s.anchor, s.head, i);
    return r.ranges.length === n ? r : null;
  } catch {
    return null;
  }
}
var mr = (e, t) => {
  const n = e.resolve(t), { depth: i } = n;
  return i === 0 ? t : n.pos - n.parentOffset - 1;
}, wr = (e, t) => {
  const n = e.nodeAt(t), i = e.resolve(t);
  let { depth: s } = i, r = n;
  for (; s > 0; ) {
    const o = i.node(s);
    s -= 1, s === 0 && (r = o);
  }
  return r;
}, mi = (e, t) => {
  const n = B.getState(e);
  return n ? Nn(t, n.type, n.binding.mapping) : null;
}, Vf = (e, t) => {
  const n = B.getState(e);
  return n ? Yt(
    n.doc,
    n.type,
    t,
    n.binding.mapping
  ) || 0 : -1;
}, br = (e, t) => {
  let n = t;
  for (; n != null && n.parentNode && n.parentNode !== e.dom; )
    n = n.parentNode;
  return n;
}, xl = new te("dragHandle"), yl = ({
  pluginKey: e = xl,
  element: t,
  editor: n,
  computePositionConfig: i,
  getReferencedVirtualElement: s,
  onNodeChange: r,
  onElementDragStart: o,
  onElementDragEnd: l,
  nestedOptions: c,
  dragImageProperties: a
}) => {
  const d = document.createElement("div");
  let h = !1, u = null, f = -1, g, m = null, w = null, b = null, C = null, y = null;
  function T() {
    t && (t.style.visibility = "hidden", t.style.pointerEvents = "none");
  }
  function z() {
    if (t) {
      if (!n.isEditable) {
        T();
        return;
      }
      t.style.visibility = "", t.style.pointerEvents = "auto";
    }
  }
  function I(k) {
    const D = (s == null ? void 0 : s()) || {
      getBoundingClientRect: () => k.getBoundingClientRect()
    };
    Hi(D, t, i).then((S) => {
      Object.assign(t.style, {
        position: S.strategy,
        left: `${S.x}px`,
        top: `${S.y}px`
      });
    });
  }
  function E(k) {
    o == null || o(k), Pf(
      k,
      n,
      c,
      { node: u, pos: f },
      a
    ), C = Bf(n.state.selection), t && (t.dataset.dragging = "true"), setTimeout(() => {
      t && (t.style.pointerEvents = "none");
    }, 0);
  }
  function O(k) {
    l == null || l(k), C = null, T(), t && (t.style.pointerEvents = "auto", t.dataset.dragging = "false");
  }
  function Q({ nodeCount: k, depth: D, anchorPos: S }) {
    const j = zf(
      n.state.doc,
      S,
      k,
      D
    );
    j && n.view.dispatch(n.state.tr.setSelection(j));
  }
  function st(k) {
    if (!(!k.target || !n.view.dom.contains(k.target))) {
      if (Rl()) {
        const D = n.view.dom;
        requestAnimationFrame(() => {
          D.isContentEditable && (D.contentEditable = "false", D.contentEditable = "true");
        });
      }
      !C || n.view.state.selection.empty || (y = {
        ...C,
        anchorPos: n.state.selection.from
      }, w = requestAnimationFrame(() => {
        w = null, y && (Q(y), y = null);
      }));
    }
  }
  function gt() {
    t.removeEventListener("dragstart", E), t.removeEventListener("dragend", O), document.removeEventListener("drop", st), m && (cancelAnimationFrame(m), m = null, b = null), w && (cancelAnimationFrame(w), w = null);
  }
  return d.appendChild(t), {
    unbind() {
      gt();
    },
    plugin: new je({
      key: typeof e == "string" ? new te(e) : e,
      state: {
        init() {
          return { locked: !1 };
        },
        apply(k, D, S, j) {
          if (y && k.docChanged) {
            const X = k.mapping.mapResult(y.anchorPos, 1);
            X.deleted ? y = null : y.anchorPos = X.pos;
          }
          const H = k.getMeta("lockDragHandle"), W = k.getMeta("hideDragHandle");
          if (H !== void 0 && (h = H), W)
            return T(), h = !1, u = null, f = -1, r == null || r({ editor: n, node: null, pos: -1 }), D;
          if (k.docChanged && f !== -1 && t)
            if (xs(k)) {
              const X = Vf(j, g);
              X !== f && (f = X);
            } else {
              const X = k.mapping.map(f);
              X !== f && (f = X, g = mi(j, f));
            }
          return D;
        }
      },
      view: (k) => {
        var D;
        return t.draggable = !0, t.style.pointerEvents = "auto", t.dataset.dragging = "false", (D = n.view.dom.parentElement) == null || D.appendChild(d), d.style.pointerEvents = "none", d.style.position = "absolute", d.style.top = "0", d.style.left = "0", t.addEventListener("dragstart", E), t.addEventListener("dragend", O), document.addEventListener("drop", st), {
          update(S, j) {
            if (!t)
              return;
            if (!n.isEditable) {
              T();
              return;
            }
            if (h ? t.draggable = !1 : t.draggable = !0, k.state.doc.eq(j.doc) || f === -1)
              return;
            let H = k.nodeDOM(f);
            if (H = br(k, H), H === k.dom || (H == null ? void 0 : H.nodeType) !== 1)
              return;
            const W = k.posAtDOM(H, 0), X = wr(n.state.doc, W), jt = mr(n.state.doc, W);
            u = X, f = jt, g = mi(k.state, f), r == null || r({ editor: n, node: u, pos: f }), I(H);
          },
          // TODO: Kills even on hot reload
          destroy() {
            gt(), t && bl(d);
          }
        };
      },
      props: {
        handleDOMEvents: {
          keydown(k) {
            return !t || h || k.hasFocus() && (T(), u = null, f = -1, r == null || r({ editor: n, node: null, pos: -1 })), !1;
          },
          mouseleave(k, D) {
            return h || D.target && !d.contains(D.relatedTarget) && (T(), u = null, f = -1, r == null || r({ editor: n, node: null, pos: -1 })), !1;
          },
          mousemove(k, D) {
            return !t || h || (b = { x: D.clientX, y: D.clientY }, m) || (m = requestAnimationFrame(() => {
              if (m = null, !b)
                return;
              const { x: S, y: j } = b;
              b = null;
              const H = ml({
                x: S,
                y: j,
                editor: n,
                nestedOptions: c
              });
              if (!H.resultElement)
                return;
              let W = H.resultElement, X = H.resultNode, jt = H.pos;
              if (!(c != null && c.enabled)) {
                if (W = br(k, W), W === k.dom || (W == null ? void 0 : W.nodeType) !== 1)
                  return;
                const Ce = k.posAtDOM(W, 0);
                X = wr(n.state.doc, Ce), jt = mr(n.state.doc, Ce);
              }
              X !== u && (u = X, f = jt ?? -1, g = mi(k.state, f), r == null || r({ editor: n, node: u, pos: f }), I(W), z());
            })), !1;
          }
        }
      }
    })
  };
};
function vl(e) {
  var t, n;
  return e === !1 || e === void 0 ? {
    enabled: !1,
    rules: [],
    defaultRules: !0,
    allowedContainers: void 0,
    edgeDetection: gi("none")
  } : e === !0 ? {
    enabled: !0,
    rules: [],
    defaultRules: !0,
    allowedContainers: void 0,
    edgeDetection: gi("left")
  } : {
    enabled: !0,
    rules: (t = e.rules) != null ? t : [],
    defaultRules: (n = e.defaultRules) != null ? n : !0,
    allowedContainers: e.allowedContainers,
    edgeDetection: gi(e.edgeDetection)
  };
}
var zi = {
  placement: "left-start",
  strategy: "absolute"
};
Fe.create({
  name: "dragHandle",
  addOptions() {
    return {
      render() {
        const e = document.createElement("div");
        return e.classList.add("drag-handle"), e;
      },
      computePositionConfig: {},
      locked: !1,
      onNodeChange: () => null,
      onElementDragStart: void 0,
      onElementDragEnd: void 0,
      nested: !1,
      dragImageProperties: void 0
    };
  },
  addCommands() {
    return {
      lockDragHandle: () => ({ editor: e }) => (this.options.locked = !0, e.commands.setMeta("lockDragHandle", this.options.locked)),
      unlockDragHandle: () => ({ editor: e }) => (this.options.locked = !1, e.commands.setMeta("lockDragHandle", this.options.locked)),
      toggleDragHandle: () => ({ editor: e }) => (this.options.locked = !this.options.locked, e.commands.setMeta("lockDragHandle", this.options.locked))
    };
  },
  addProseMirrorPlugins() {
    const e = this.options.render(), t = vl(this.options.nested);
    return [
      yl({
        computePositionConfig: {
          ...zi,
          ...this.options.computePositionConfig
        },
        getReferencedVirtualElement: this.options.getReferencedVirtualElement,
        element: e,
        editor: this.editor,
        onNodeChange: this.options.onNodeChange,
        onElementDragStart: this.options.onElementDragStart,
        onElementDragEnd: this.options.onElementDragEnd,
        nestedOptions: t,
        dragImageProperties: this.options.dragImageProperties
      }).plugin
    ];
  }
});
var Hf = (e) => {
  const {
    className: t = "drag-handle",
    children: n,
    editor: i,
    pluginKey: s = xl,
    onNodeChange: r,
    onElementDragStart: o,
    onElementDragEnd: l,
    getReferencedVirtualElement: c,
    computePositionConfig: a = zi,
    nested: d = !1
  } = e, [h] = U(() => typeof document > "u" ? null : document.createElement("div")), u = pt(() => vl(d), [JSON.stringify(d)]);
  return et(() => {
    h && (h.className = t, h.style.visibility = "hidden", h.style.position = "absolute", h.dataset.dragging = "false");
  }, [t, h]), et(() => {
    if (!h || i.isDestroyed)
      return;
    const { plugin: f, unbind: g } = yl({
      editor: i,
      element: h,
      pluginKey: s,
      computePositionConfig: {
        ...zi,
        ...a
      },
      onElementDragStart: o,
      onElementDragEnd: l,
      onNodeChange: r,
      getReferencedVirtualElement: c,
      nestedOptions: u
    });
    return i.registerPlugin(f), () => {
      i.isDestroyed || i.unregisterPlugin(s), g();
    };
  }, [
    h,
    i,
    r,
    c,
    s,
    a,
    o,
    l,
    u
  ]), h ? Ki(n, h) : null;
}, Ff = Hf;
function Jp() {
  var I, E, O, Q, st, gt, k, D;
  const e = G(), t = nt(), { t: n } = Z(), [i, s] = U(null), [r, o] = U(-1), [l, c] = U(!1), a = (E = (I = e == null ? void 0 : e.extensionManager) == null ? void 0 : I.extensions) == null ? void 0 : E.some(
    (S) => (S == null ? void 0 : S.name) === gc.name
  ), d = (Q = (O = e == null ? void 0 : e.extensionManager) == null ? void 0 : O.extensions) == null ? void 0 : Q.some(
    (S) => (S == null ? void 0 : S.name) === fc.name
  ), h = (gt = (st = e == null ? void 0 : e.extensionManager) == null ? void 0 : st.extensions) == null ? void 0 : gt.some(
    (S) => (S == null ? void 0 : S.name) === uc.name
  );
  function u() {
    const S = e.chain();
    S.setNodeSelection(r).unsetAllMarks(), (i == null ? void 0 : i.type.name) !== "paragraph" && S.setParagraph(), S.run();
  }
  function f() {
    e.chain().focus().setNodeSelection(r).run(), document.execCommand("copy");
  }
  function g() {
    e.commands.setNodeSelection(r);
    const { $anchor: S } = e.state.selection, j = S.node(1) || e.state.selection.node;
    e.chain().setMeta("hideDragHandle", !0).insertContentAt(r + ((i == null ? void 0 : i.nodeSize) || 0), j.toJSON()).run();
  }
  function m(S) {
    e.commands.setTextAlign(S);
  }
  function w() {
    const S = Is(e.state.tr, r, 1);
    S.setMeta("hideDragHandle", !0), e.view.dispatch && e.view.dispatch(S);
  }
  function b() {
    const S = Is(e.state.tr, r, -1);
    e.view.dispatch && e.view.dispatch(S);
  }
  function C() {
    e.chain().setMeta("hideDragHandle", !0).setNodeSelection(r).deleteSelection().run();
  }
  const y = R(
    (S) => {
      S.node && s(S.node), o(S.pos), requestAnimationFrame(() => {
        S.editor.commands.focus();
      });
    },
    []
  ), T = (S) => {
    var j;
    if (S.preventDefault(), r !== -1) {
      const H = (i == null ? void 0 : i.nodeSize) || 0, W = r + H, X = (i == null ? void 0 : i.type.name) === "paragraph" && ((j = i == null ? void 0 : i.content) == null ? void 0 : j.size) === 0, jt = X ? r + 2 : W + 2;
      e.chain().command(({ dispatch: Ce, tr: Zn, state: ys }) => Ce ? (X ? Zn.insertText("/", r, r + 1) : Zn.insert(
        W,
        ys.schema.nodes.paragraph.create(null, [ys.schema.text("/")])
      ), Ce(Zn)) : !0).focus(jt).run();
    }
  };
  et(() => (l ? e.commands.setMeta("lockDragHandle", !0) : e.commands.setMeta("lockDragHandle", !1), () => {
    e.commands.setMeta("lockDragHandle", !1);
  }), [l]);
  const z = (S) => {
    t && c(S);
  };
  return /* @__PURE__ */ p(
    Ff,
    {
      className: "richtext-transition-all richtext-duration-200 richtext-ease-out",
      editor: e,
      onNodeChange: y,
      pluginKey: "RichTextBubbleMenuDragHandle",
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-0.5", children: [
        /* @__PURE__ */ p(
          v,
          {
            action: T,
            disabled: !t,
            icon: "Plus",
            tooltip: n("editor.draghandle.insertBlock")
          }
        ),
        /* @__PURE__ */ p(
          v,
          {
            disabled: !t,
            icon: "Grip",
            tooltip: n("editor.draghandle.grip"),
            action: (S) => {
              S.preventDefault(), S.stopPropagation(), z(!l);
            }
          }
        ),
        /* @__PURE__ */ x(cc, { onOpenChange: z, open: l, children: [
          /* @__PURE__ */ p(ac, { className: "richtext-pointer-events-none" }),
          /* @__PURE__ */ x(
            hc,
            {
              align: "start",
              className: "richtext-w-48",
              hideWhenDetached: !0,
              side: "bottom",
              sideOffset: 0,
              children: [
                /* @__PURE__ */ x(
                  Mt,
                  {
                    className: "richtext-flex richtext-gap-3 richtext-bg-opacity-10 hover:richtext-bg-red-400 hover:richtext-bg-opacity-20 focus:richtext-bg-red-400 focus:richtext-bg-opacity-30 focus:richtext-text-red-500 dark:hover:richtext-bg-opacity-20 dark:hover:richtext-text-red-500",
                    onClick: C,
                    children: [
                      /* @__PURE__ */ p(at, { name: "Trash2" }),
                      /* @__PURE__ */ p("span", { children: n("editor.remove") })
                    ]
                  }
                ),
                h ? /* @__PURE__ */ x(
                  Mt,
                  {
                    className: "richtext-flex richtext-gap-3",
                    onClick: u,
                    children: [
                      /* @__PURE__ */ p(at, { name: "PaintRoller" }),
                      /* @__PURE__ */ p("span", { children: n("editor.clear.tooltip") })
                    ]
                  }
                ) : null,
                /* @__PURE__ */ x(
                  Mt,
                  {
                    className: "richtext-flex richtext-gap-3",
                    onClick: f,
                    children: [
                      /* @__PURE__ */ p(at, { name: "Clipboard" }),
                      /* @__PURE__ */ p("span", { children: n("editor.copyToClipboard") })
                    ]
                  }
                ),
                /* @__PURE__ */ x(Mt, { className: "richtext-flex richtext-gap-3", onClick: g, children: [
                  /* @__PURE__ */ p(at, { name: "Copy" }),
                  /* @__PURE__ */ p("span", { children: n("editor.copy") })
                ] }),
                a || d ? /* @__PURE__ */ p(dc, {}) : null,
                a ? /* @__PURE__ */ x(Es, { children: [
                  /* @__PURE__ */ x(_s, { className: "richtext-flex richtext-gap-3", children: [
                    /* @__PURE__ */ p(at, { name: "AlignCenter" }),
                    /* @__PURE__ */ p("span", { children: n("editor.textalign.tooltip") })
                  ] }),
                  /* @__PURE__ */ p(Ts, { children: /* @__PURE__ */ x(Ds, { children: [
                    /* @__PURE__ */ x(
                      Mt,
                      {
                        className: "richtext-flex richtext-gap-3",
                        onClick: () => m("left"),
                        children: [
                          /* @__PURE__ */ p(at, { name: "AlignLeft" }),
                          /* @__PURE__ */ p("span", { children: n("editor.textalign.left.tooltip") })
                        ]
                      }
                    ),
                    /* @__PURE__ */ x(
                      Mt,
                      {
                        className: "richtext-flex richtext-gap-3",
                        onClick: () => m("center"),
                        children: [
                          /* @__PURE__ */ p(at, { name: "AlignCenter" }),
                          /* @__PURE__ */ p("span", { children: n("editor.textalign.center.tooltip") })
                        ]
                      }
                    ),
                    /* @__PURE__ */ x(
                      Mt,
                      {
                        className: "richtext-flex richtext-gap-3",
                        onClick: () => m("right"),
                        children: [
                          /* @__PURE__ */ p(at, { name: "AlignRight" }),
                          /* @__PURE__ */ p("span", { children: n("editor.textalign.right.tooltip") })
                        ]
                      }
                    )
                  ] }) })
                ] }) : null,
                d ? /* @__PURE__ */ x(Es, { children: [
                  /* @__PURE__ */ x(_s, { className: "richtext-flex richtext-gap-3", children: [
                    /* @__PURE__ */ p(at, { name: "IndentIncrease" }),
                    /* @__PURE__ */ p("span", { children: n("editor.indent") })
                  ] }),
                  /* @__PURE__ */ p(Ts, { children: /* @__PURE__ */ x(Ds, { children: [
                    /* @__PURE__ */ x(
                      Mt,
                      {
                        className: "richtext-flex richtext-gap-3",
                        disabled: ((k = i == null ? void 0 : i.attrs) == null ? void 0 : k.indent) >= As.max,
                        onClick: w,
                        children: [
                          /* @__PURE__ */ p(at, { name: "IndentIncrease" }),
                          /* @__PURE__ */ p("span", { children: n("editor.indent.tooltip") })
                        ]
                      }
                    ),
                    /* @__PURE__ */ x(
                      Mt,
                      {
                        className: "richtext-flex richtext-gap-3",
                        disabled: ((D = i == null ? void 0 : i.attrs) == null ? void 0 : D.indent) <= As.min,
                        onClick: b,
                        children: [
                          /* @__PURE__ */ p(at, { name: "IndentDecrease" }),
                          /* @__PURE__ */ p("span", { children: n("editor.outdent.tooltip") })
                        ]
                      }
                    )
                  ] }) })
                ] }) : null
              ]
            }
          )
        ] })
      ] })
    }
  );
}
const xr = {
  plaintext: "Plain Text",
  js: "JavaScript",
  ts: "TypeScript",
  css: "CSS",
  html: "HTML",
  python: "Python",
  bash: "Bash"
};
function Kf({ listLanguages: e }) {
  const { t } = Z(), [n, i] = U(!1), s = G(), r = pt(() => {
    const { selection: l } = s.state, c = l.$from.parent;
    if (c.type.name === "codeBlock") {
      const a = c.attrs.language || "plaintext";
      return xr[a] || a;
    }
  }, [s.state, e]), o = pt(() => {
    let l = e || [];
    return e != null && e.includes("plaintext") || (l = ["plaintext", ...l]), l.map((c) => ({
      label: xr[c] || c,
      value: c
    }));
  }, [e]);
  return /* @__PURE__ */ x(Wi, { modal: !0, onOpenChange: i, open: n, children: [
    /* @__PURE__ */ p(
      Gi,
      {
        asChild: !0,
        className: "hover:richtext-bg-accent data-[state=on]:richtext-bg-accent",
        children: /* @__PURE__ */ x(v, { dataState: !!r, children: [
          r ? /* @__PURE__ */ p(M, { children: r }) : /* @__PURE__ */ p(M, { children: t("editor.paragraph.tooltip") }),
          /* @__PURE__ */ p(
            at,
            {
              className: "richtext-ml-1 richtext-size-3 richtext-text-zinc-500",
              name: "MenuDown"
            }
          )
        ] })
      }
    ),
    /* @__PURE__ */ p(
      Yi,
      {
        align: "start",
        className: "!richtext-w-[initial] !richtext-p-[4px]",
        hideWhenDetached: !0,
        side: "bottom",
        children: o == null ? void 0 : o.map((l) => /* @__PURE__ */ x(
          "div",
          {
            className: "richtext-flex richtext-w-full richtext-items-center richtext-gap-3 richtext-rounded-sm !richtext-border-none !richtext-bg-transparent richtext-px-2 richtext-py-1.5 richtext-text-left richtext-text-sm richtext-text-foreground !richtext-outline-none richtext-transition-colors hover:!richtext-bg-accent",
            onClick: (c) => {
              c.preventDefault(), s.chain().focus().setCodeBlock({ language: l.value }).run(), i(!1);
            },
            children: [
              /* @__PURE__ */ p("div", { className: "!richtext-min-w-[20px]", children: r === l.value && /* @__PURE__ */ p(yi, { size: 16 }) }),
              /* @__PURE__ */ p("div", { className: "richtext-flex richtext-items-center richtext-gap-1", children: l.label })
            ]
          },
          l.value
        ))
      }
    )
  ] });
}
function Xp() {
  const e = nt(), t = G(), n = ji(Qn.name), i = R(({ editor: c }) => c.isActive(Qn.name), []), s = pt(() => {
    var c, a, d;
    return n ? ((d = (a = (c = n.options) == null ? void 0 : c.lowlight) == null ? void 0 : a.listLanguages) == null ? void 0 : d.call(a)) || ["plaintext", "html", "css", "js", "ts"] : [];
  }, [n]), r = R(() => ne(Qn.name, t), [t]), o = () => {
    const { from: c } = t.state.selection, a = t.state.doc.resolve(c);
    for (let d = a.depth; d >= 0; d--) {
      const h = a.node(d);
      if (h.type.name === "codeBlock")
        return h.textContent;
    }
    return "";
  }, l = () => {
    const c = o();
    navigator.clipboard.writeText(c);
  };
  return e ? /* @__PURE__ */ p(
    ct,
    {
      editor: t,
      options: { placement: "bottom", offset: 8, flip: !0 },
      pluginKey: "RichTextBubbleCodeBlock",
      shouldShow: i,
      getReferencedVirtualElement: () => {
        const { from: c } = t.state.selection, d = t.view.domAtPos(c).node.parentElement;
        return d ? {
          getBoundingClientRect: () => d.getBoundingClientRect()
        } : null;
      },
      children: /* @__PURE__ */ x("div", { className: "richtext-flex richtext-items-center richtext-gap-2 richtext-rounded-md !richtext-border !richtext-border-solid !richtext-border-border richtext-bg-popover richtext-p-1 richtext-text-popover-foreground richtext-shadow-md richtext-outline-none", children: [
        /* @__PURE__ */ p(Kf, { listLanguages: s }),
        /* @__PURE__ */ p(
          At,
          {
            className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
            orientation: "vertical"
          }
        ),
        /* @__PURE__ */ p(v, { action: l, tooltip: "Copy", children: /* @__PURE__ */ p(Xl, { size: 16 }) }),
        /* @__PURE__ */ p(
          At,
          {
            className: "!richtext-mx-1 !richtext-my-2 !richtext-h-[16px]",
            orientation: "vertical"
          }
        ),
        /* @__PURE__ */ p(v, { action: r, tooltip: "Delete", children: /* @__PURE__ */ p(Nr, { size: 16 }) })
      ] })
    }
  ) : /* @__PURE__ */ p(M, {});
}
export {
  Lp as RichTextBubbleCallout,
  Xp as RichTextBubbleCodeBlock,
  Up as RichTextBubbleColumns,
  Pp as RichTextBubbleDrawer,
  Bp as RichTextBubbleExcalidraw,
  $p as RichTextBubbleIframe,
  Hp as RichTextBubbleImage,
  Fp as RichTextBubbleImageGif,
  zp as RichTextBubbleKatex,
  Vp as RichTextBubbleLink,
  Jp as RichTextBubbleMenuDragHandle,
  jp as RichTextBubbleMermaid,
  Wp as RichTextBubbleTable,
  Gp as RichTextBubbleText,
  Yp as RichTextBubbleTwitter,
  Kp as RichTextBubbleVideo
};
